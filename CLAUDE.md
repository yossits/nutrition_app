# nutrition_app — הוראות עבודה

אפליקציית תזונה אישית לשוק הישראלי. PWA, פיתוח יחיד.
**התזה הארכיטקטונית:** המודל מרכיב · הקוד מחשב · הקוד מאמת.

---

## 1. אוטונומיה — הכלל החשוב ביותר

**מיישמים רק מה שהוכרע. כל השאר עוצר ושואל.**

לפני שנוגעים במשהו שנראה כמו החלטה — בדוק בסדר הזה:

1. `docs/spec/` — האם זה מאופיין?
2. `docs/decisions.md` — האם זה הוכרע?
3. `docs/open-questions.md` — האם זה **במפורש** עדיין פתוח?

אם התשובה ב-1 או ב-2 היא כן — יישם לפי מה שכתוב.
אם זה מופיע ב-3, או לא מופיע בכלל — **עצור, הצג את הבחירות, ושאל.**

אל תבחר ברירת מחדל סבירה ותמשיך. אל תסמן "לבדיקה" ותמשיך. עצור.

הסיבה: התיעוד הזה הוא לא רקע, הוא מקור האמת. החלטה שנכנסה לקוד בלי לעבור דרכו יוצרת פער בין מה שהמערכת עושה לבין מה שכתוב שהיא עושה — וזה בדיוק מה שכל מבנה ה-`docs/` נבנה כדי למנוע.

---

## 2. שפה

**שיחה איתי — עברית. כל השאר — אנגלית.**

| מה | שפה |
|---|---|
| קוד, שמות משתנים, הערות, docstrings | אנגלית |
| הודעות קומיט ו-PR | אנגלית |
| הערות ב-SQL ובמיגרציות | אנגלית |
| פלט של סקריפטים, לוגים, הודעות שגיאה פנימיות | אנגלית |
| `docs/` | עברית |
| שיחה איתי | עברית |
| פרומפט לקלוד קוד | אנגלית. תוכן עברי שנועד להעתקה מסומן במפורש כתוכן |

### הכלל המבני — זה שאי אפשר לשבור בטעות

**אף מחרוזת שהמשתמש רואה לא נכתבת בתוך קומפוננטה. בשום שפה.**

כל טקסט ממשק עובר דרך מודול המחרוזות. תמיד — גם כשזו מילה אחת, גם כשזה "טעינה...", גם כשזה נראה זמני.

```
❌  <button>החלף מנה</button>
❌  <button>Swap meal</button>
✅  <button>{t('menu.swap')}</button>
```

טקסט תרגום ניתן לבדיקה בחיפוש אחד. שיקול דעת לא.

### שפה אחת

המוצר עברי. אין i18n, אין עמודות _en, אין בורר שפה.
כלל מודול המחרוזות למעלה עומד בפני עצמו — הוא לא תשתית לתרגום
אלא הדרך לשלוט במיקרו-קופי במקום אחד, במיוחד בחסימות בריאותיות
ובחומות תשלום.

זו החלטת היקף למוצר הנוכחי, לא איסור קבוע. שוק אמריקאי
יגיע אם וכאשר יגיע עם מאגר משלו, ואז זה ייפתח מחדש.

---

## 3. תיעוד — כתיבה רק באישור

**אין כתיבה ל-`docs/` בלי אישור מפורש שלי בשיחה.**

זה חל על `PROGRESS.md`, `decisions.md`, `measurements.md` וכל מודול ב-`spec/`.
הצג את השורות המדויקות שאתה רוצה להוסיף, חכה לאישור, ורק אז כתוב.

הפרוצדורה המלאה: `.claude/skills/session-close/`

ארבעה כללים שנשברים בקלות:

1. **סטטוס נכתב במקום אחד** — `PROGRESS.md`. אף פעם לא בתוך `spec/`
2. **החלטה שהשתנתה נכתבת פעמיים** — שורה ב-`decisions.md` (עם תאריך וסיבה) **וגם** עדכון המודול ב-`spec/`. אף פעם רק אחד
3. **`decisions.md` ו-`measurements.md` הם append-only** — לא דורסים, לא מנסחים מחדש, לא מסדרים. החלטה שבוטלה מסומנת כמבוטלת ונשארת
4. **הגדרת סיום** — "כתבתי את הקוד" זה לא הושלם. "הרצתי אותו והוא עבר" זה הושלם

**התיעוד נכתב לפני העבודה, לא רק בסגירתה.** הכרעה שהתקבלה בדיון נכתבת
ל-`docs/` לפני שהבלוק הראשון רץ. `decisions.md` ו-`open-questions.md` הם מה
שקלוד קוד מצליב מולו כשהוא עוצר באמצע בלוק — ואם הם נכתבים רק בסוף, אין מול
מה להצליב. סגירת הסשן נשארת בסוף; ההכרעות מקדימות אותו.

**הצ'אט מנסח, קלוד קוד כותב.** כל כתיבה לקובץ — קוד, SQL ו-`docs/` — עוברת
דרך קלוד קוד. הצ'אט מציג את השורות המדויקות לאישור ולא נוגע בקבצים. שני
שערים: אישור על הנוסח, ועצירה שנייה אם העוגן בקובץ אינו כמתואר.

---

## 4. שער רגרסיה

**כל שינוי בפותר המנות (`spike/portions.py`) או בשכבת הסינון (`spike/filters.py`) מחייב הרצה של מערך המדדים המלא לפני שחרור.**

התוצאה נכנסת ל-`measurements.md` עם תאריך. ראה שם את הדרישה המלאה.

זה לא נוהל פורמלי — זה מה שהוכיח שהארכיטקטורה עובדת. חישוב גרמים על ידי המודל נתן 0% הצלחה; פותר דטרמיניסטי הביא ל-92%. המספרים האלה הם הנכס, והם שווים רק כל עוד מודדים אותם שוב.

---

## 5. בטיחות — קווים שלא חוצים

- **אלרגנים ייחסמו בשכבת הסינון, לעולם לא בבקשה מהמודל.** מזון שלא נכנס לפרומפט לא יכול להופיע בפלט
- **רצפת שומן 0.6 ג'/ק"ג היא אילוץ קשיח בפותר**, לא בדיקה בדיעבד
- פריט `menu_eligible` בלי סקירת כשרות ואלרגנים הוא באג בטיחותי. הסכימה אוכפת זאת כ-CHECK constraint
- `allergens = '{}'` (נבדק, נקי) ו-`allergens_reviewed_at IS NULL` (לא נבדק) הם שני מצבים שונים. לא לאחד
- **מפתח API לעולם לא בקובץ בריפו ולעולם לא בלקוח.** משתנה סביבה בפיתוח, Edge Function בייצור

---

## 6. סביבה

- **PowerShell, לא cmd.** משתני סביבה: `$env:VAR = "value"`. `set` ב-PowerShell הוא alias ל-`Set-Variable` ולא מגדיר משתנה סביבה — הוא נכשל בשקט
- **נתיב מוחלט לפרשן.** כל הרצת פייתון בפרויקט משתמשת בנתיב המלא `D:\workspace\nutrition_app\.venv\Scripts\python.exe`, ולעולם לא ב-`python` חשוף. ה-PATH של הסשן נצרב מ-shell snapshot בפתיחתו ועלול להצביע לפרשן של פרויקט אחר בלי שום סימן — `VIRTUAL_ENV` ריק במקרה כזה. פרומפט שנשען על PATH אינו ניתן לשחזור
- Docker Desktop זמין
- ריפו: `yossits/nutrition_app` · מקומי: `D:\workspace\nutrition_app`
- **סקריפטי `db/` קוראים את ה-DSN מ-`.env` בשורש**, לא ממשתנה סביבה. `db/_env.py` דורס במפורש: `DATABASE_URL` מוגדר ב-User scope וחוזר בכל חלון, ו-`load_dotenv()` בברירת המחדל לא היה עושה כלום
- **`.env` חייב להיות UTF-8.** `... > .env` ב-PowerShell 5.1 מייצר UTF-16 — מלכודת אחות ל-`set` מול `$env:`. המרה בפייתון, לא ב-`printf`: `printf` שם את הסוד בשורת הפקודה, שנרשמת גם כשהפלט ממוסך
- **ה-DSN מוצג ממוסך תמיד.** המארח הישיר `db.<ref>.supabase.co` לא נפתר מהמכונה הזו — ה-pooler עם `postgres.<ref>` עובד

---

## 7. מפת הפרויקט

```
docs/README.md          נקודת כניסה. אינדקס וכללי תחזוקה
docs/PROGRESS.md        ★ מצב העבודה. המקור היחיד ל"מה נשאר"
docs/decisions.md       החלטות סגורות. append-only
docs/open-questions.md  מה עוד לא הוכרע
docs/measurements.md    תוצאות מדודות + דרישת הרגרסיה. append-only
docs/spec/01–12         האפיון. יציב
db/                     סכימת מאגר המזון
spike/                  הליבה המוכחת
```

**בתחילת עבודה:** קרא את `docs/PROGRESS.md` ואת המודול הרלוונטי ב-`docs/spec/`. לא את כל התיקייה.

---

## 8. שני קלודים — חלוקת תפקידים

**קלוד בצ'אט חושב ומנסח. קלוד קוד מיישם ומריץ. אני היחיד שרואה את שניהם.**

| שלב | מי | מה |
|---|---|---|
| **דיון** | צ'אט | הבעיה בקצרה, ושאלה. לא מסמך |
| **תוכנית** | צ'אט | הפרומפט לקלוד קוד, אחרי אישור |
| **יישום** | קלוד קוד | כתיבה והרצה |
| **סקירה** | צ'אט | מה קרה, והאם לחזור לקלוד קוד |
| **מסירה** | קלוד קוד | קומיט, דחיפה, אימות |

**כל הודעה מקלוד בצ'אט פותחת בסימון שלב.** משפט אחד — "זו תוכנית לאישור",
"זו סקירה" — כדי שיהיה ברור מיד אם ההודעה נועדה לקריאה או להעתקה.

**בדיון — קצר.** בעיה, ושאלה. אם התשובה יוצאת מסמך, זה סימן שהשלב לא נגמר.

**בתוכנית — הכרעות פתוחות בראש, לא בסוף.** §1 אומר לעצור ולשאול; זה אומר
איפה. תוכנית שצריך לקרוא עד הסוף כדי לגלות שיש בה שאלה פתוחה נכתבה
בסדר הפוך.

**הפרומפט לקלוד קוד הוא בלוק אחד רציף.** נוסחים, SQL וקבצים בפנים.
"תדביק את זה בהודעה נפרדת" הופך אותי לעורך.

**בסקירה — התשובה בשורה הראשונה.** האם צריך לחזור לקלוד קוד, ולמה.
הפירוט אחרי.

**קלוד בצ'אט לא רואה את הריפו.** הוא קורא תמונת מצב שהועלתה לידע
הפרויקט, לא את הקבצים החיים, והוא לא מריץ כלום. מספר או שורת קוד שהוא
מציג יכולים להיות ישנים או להיות מהזיכרון. כשזה קובע — לבקש מקלוד קוד
להציג את המצב בפועל.

**מה דורש אישור ומה לא.** אישור נדרש על תיעוד לפני הכתיבה (§3).
קומיט, דחיפה ואימות הם חלק מהמשימה ואינם דורשים
אישור נפרד — "תעצור לפני הקומיט" הוא לא ברירת המחדל. ברירת המחדל
היא "תקמט, תדחוף, ותאמת".

**עבודה ישירות על `main`.** פיתוח יחיד, בלי ענפים ובלי PR. הענף היה
אמור לשמש שער סקירה לפני `main`, אבל השערים האמיתיים מקדימים אותו:
§1 אוסר יישום של מה שלא הוכרע, ו-§3 אוסר כתיבה ל-`docs/` בלי אישור
בשיחה. שניהם פועלים לפני הקומיט, ולכן ענף ביניים הוסיף צעד ולא בדיקה.

**הדחיפה המאושרת מראש היא ל-`main` בלבד.** ענף עבודה אינו נדחף
ל-`origin`. "תדחוף" בפסקה שמעל אינו מכסה `push -u origin <ענף>`.

**סשן שרץ ב-worktree מבודד יושב על ענף שנכפה עליו, לא שנבחר.** הוא
שומר את הקומיטים מקומית, ובסגירת הבלוק מאחד אותם: `merge --ff-only`
ל-`main` בריפו הראשי, ואז דחיפת `main`. `--ff-only` ולא `merge`, כדי
שהסתעפות אמיתית תיכשל ברעש במקום להיבלע ב-merge commit.

**נקודות עצירה נכתבות בראש הפרומפט, ממוספרות.** "עצור ושאל" בלי לומר איפה
הוא כלל שלא ניתן לאכוף. בבלוק 1 שלוש נקודות סומנו מראש, ובשלושתן העצירה
הייתה נכונה.

**פרומפט פותח בבקשת שעת שעון קיר בדיוק של שניות.** לבלוק אין קומיט פותח,
ולכן הרפלוג נותן חותמת פעם אחת לסשן ולא לבלוק — ראה `open-questions.md`
#24. בלי השורה הזו הבלוק אינו ניתן למדידה.

**בכל שלב — מה קורה עכשיו, בשפה פשוטה, לפני הפירוט.** יוסי מנהל את הפרויקט
ואינו כותב את הקוד. שלב חדש מתחיל רק באישור מפורש שלו.

**צ'אט מתאר, קלוד קוד בודק.** לצ'אט יש גישה למאגר ואין לו גישה לקבצים.
טענה שלו על נתונים מאומתת לפני שהיא נאמרת; טענה שלו על תוכן קובץ היא
הסקה. לכן פרומפט אינו אומר "שנה את X ל-Y" אלא "מצא את X, הצג אותו, ואמור
אם הוא זז". שלוש עצירות בבלוק 3ג נבעו מהפרת הכלל הזה: היקף כיווץ הרווחים,
ספירת פריטי החלבון, וההנחה ש-`measurements.md` מגדיר את שער הרגרסיה
כ"12 מדדים" — כשהשער הוא שש נקודות על הספייק ושני מופעי ה-12 הם רשומות
היסטוריות שדריסתן הייתה הופכת אותן לשקריות.

**הכלל חל גם על מספרים, לא רק על תוכן קבצים.** לצ'אט יש גישת קריאה למסד,
ולכן הוא מודד — אבל שאילתת חקירה נכתבת מהר ולעיתים עם דפוסים חלקיים או
תנאים רופפים, ומספר שיצא ממנה אינו מדידה לצורך תיעוד. מספר שנכנס
ל-`decisions.md` או ל-`measurements.md` נמדד מחדש בשאילתה שנכתבה לצורך
הרישום, לפני הכתיבה. פער בין הנוסח לנמדד נכתב כפי שנמדד, ומדווח בצ'אט.

**מספר שהופך לשער נספר בשאילתה.** מספר שנכנס לשער עצירה או לאסרציה נמדד
בשאילתה שמחזירה את המספר עצמו, ולא נספר בעין מרשימת שורות. הכלל שמעליו מכסה
תיעוד; זה מכסה שערים, והם מסוכנים יותר — שער שמספרו שגוי יורה על עבודה תקינה.
מקור: ב-3ח נמסר לשער "בדיוק 24 רשומות עם `id` שהשתנה" מתוך מדידה נכונה
שהחזירה 25 שורות. השגיאה נכנסה בספירה בעין בהעתקה מהמדידה לטקסט.

**עוגן נלקח מהמקור, לא מהפלט.** מחרוזת שהצ'אט ראה רק בדוח הרצה עברה עיצוב
— שבירת שורות, יישור, קיצור — ואינה מה שכתוב בקובץ. לכן פרומפט אינו נוקב
בטקסט להחלפה אלא במיקום ובכוונה, וקלוד קוד מציג את הטקסט בפועל לפני שהוא
נוגע בו.
