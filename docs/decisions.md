# יומן החלטות

**append-only.** לא דורסים שורה. החלטה שהתהפכה מסומנת `↩ בוטלה` ונשארת, עם שורה חדשה מתחתיה.
זה מה שמאפשר להבין בעוד חצי שנה למה משהו נראה כמו שהוא נראה — ולא לפתוח מחדש דיון שכבר נסגר.

כל אלה סגורות נכון ל-27.08.2026. אין צורך לפתוח אותן מחדש אלא אם יש סיבה חדשה.

---

## מוצר ועסקים

| נושא | ההחלטה | מודול |
|---|---|---|
| קהל | קהל רחב, בתשלום (B2C) | `spec/01-product.md` |
| פלטפורמה | PWA תחילה, חנויות אפליקציות בהמשך | `spec/09-architecture.md` |
| חבילות | **שתיים בלבד** — חינם ומנוי. אין מדרגה שלישית | `spec/02-business.md` |
| מחיר | 49 ₪ לחודש · 449 ₪ לשנה | `spec/02-business.md` |
| חינם או ניסיון | **חבילת חינם קבועה**, לא ניסיון של 30 יום | `spec/02-business.md` |
| קו החיתוך | חינם נותן **תפריט**. בתשלום מקבלים **תוכנית** | `spec/02-business.md` |
| חינם כולל | תפריט אחד · שלב 1 בלבד · ללא חלופות · ללא שינויים | `spec/02-business.md` |
| מנוי כולל | 3 אפשרויות בחירה לארוחה · כל השלבים · 12 בקשות שינוי בחודש | `spec/02-business.md` |
| אימות | SMS, פרופיל אחד לכל מספר טלפון | `spec/02-business.md` |
| בביטול | חוזר למצב חינם. אין מחיקת מידע | `spec/02-business.md` |
| דליפת תוכן | לא מנסים לחסום צילומי מסך. הנייר מתיישן תוך 6 שבועות | `spec/02-business.md` |

## חוויה

| נושא | ההחלטה | מודול |
|---|---|---|
| אונבורדינג | ↩ בוטלה 10.09.2026 · אשף בן 7 שלבים, סרגל התקדמות, אפס מונחים מקצועיים | `spec/03-functional.md` |
| אונבורדינג | אשף בן **8 שלבים** - נוסף שלב "המזווה שלי" אחרי "האוכל שלך". סרגל התקדמות, אפס מונחים מקצועיים | `spec/03-functional.md` |
| תצוגת תפריט | **במסך האפליקציה בלבד.** אין PDF, אין ייצוא | `spec/03-functional.md` |
| אחסון | הכל בתוך המוצר, מקושר לחשבון | `spec/03-functional.md` |
| מבנה התפריט | ↩ בוטלה 10.09.2026 · **תפריט קבוע** שלא משתנה עד בקשת שינוי או סוף שלב | `spec/03-functional.md` |
| מבנה התפריט | **תפריט קבוע** שלא משתנה עד בקשת שינוי, לחיצה על "עדכן תפריט" (מנוי, פעם ב-24 שעות), או סוף שלב | `spec/03-functional.md` |
| זמני ארוחות | נגזרים משעת קימה ושינה. ראשונה עד שעתיים מהקימה, אחרונה שעתיים לפני שינה | `spec/04-engine.md` |
| יחידות | מוצר שלם ביחידות ("חצי גביע", "3 ביצים"), מוצר שקיל בגרמים | `spec/06-generator.md` |
| קלט | בחירה בקליק כברירת מחדל + טקסט חופשי כתוספת | `spec/03-functional.md` |
| ניסוח | "ארוחה גמישה", לא "ארוחת צ'יט" — סותר את עקרון אי-הענישה | `spec/02-business.md` |

## שלבים ומעקב

| נושא | ההחלטה | מודול |
|---|---|---|
| מהו שלב | תקופה עם יעדים ותפריט קבועים | `spec/03-functional.md` |
| מה מסיים שלב | סטייה של **5%** בין היעד המחושב מחדש לפעיל, **או** קיפאון ב-3 שקילות | `spec/03-functional.md` |
| שקילה שבועית | **עוקבת בלבד.** לא משנה יעדים ולא משנה תפריט | `spec/03-functional.md` |
| מעבר שלב | לא מנצל בקשת שינוי מהמכסה | `spec/03-functional.md` |
| רעש מדידה | משקל הוא הטריגר הראשי, אחוז שומן משני, הכל על ממוצע נע | `spec/03-functional.md` |
| שלבי תחזוקה | אחרי 3–4 שלבי גירעון המערכת מציעה שבוע-שבועיים באיזון | `spec/04-engine.md` |
| התאמה | **תמיד מוצעת ומאושרת, לא מבוצעת אוטומטית** | `spec/03-functional.md` |

## תזונה ובטיחות

| נושא | ההחלטה | מודול |
|---|---|---|
| חלבון טבעונים | **2.0 ג'/ק"ג** — כמו כולם. הורדה ל-1.5 נבדקה והחמירה | `spec/05-food-db.md` |
| איכות חלבון | דירוג 1/2/3. אומניבור מקבל 100% דרגה 3 | `spec/05-food-db.md` |
| תוספים | אבקת חלבון — שייק אחד ביום לכל היותר, לעולם לא חלבון מרכזי | `spec/06-generator.md` |
| רצפת אחוז שומן | גברים 8%, נשים 14% | `spec/04-engine.md` |
| גירעון | מצטמצם ככל שהמשתמש מתקרב ליעד (25% ← 10%) | `spec/04-engine.md` |
| רצפת שומן | 0.6 ג'/ק"ג — **אילוץ קשיח בפותר**, לא רק בדיקה | `spec/06-generator.md` |
| אלרגיות וכשרות | לעולם לא מאחורי תשלום. נאכפות בשכבת הסינון | `spec/07-safety.md` |
| הגנת אלרגנים | בשכבת הסינון, לא בבקשה מהמודל. אלרגן שלא נכנס לפרומפט לא יכול להופיע בפלט | `spec/09-architecture.md` |

## ארכיטקטורה

| נושא | ההחלטה | מודול |
|---|---|---|
| חלוקת עבודה | המודל מרכיב · הקוד מחשב · הקוד מאמת | `spec/06-generator.md` |
| גבול AI/קוד | AI רק היכן שאין תשובה נכונה אחת. שלוש פעולות בלבד | `spec/09-architecture.md` |
| צמצום פרומפט | שלושה שלבים: אצירה → סינון → **דגימה**. לא שניים | `spec/09-architecture.md` |
| מפתח API | צד שרת בלבד, דרך Edge Function | `spec/09-architecture.md` |
| Backend | Supabase — Postgres + Auth + Edge Functions + RLS | `spec/09-architecture.md` |
| היסטוריית פרופיל | append-only. עדכון = שורה חדשה, לא דריסה | `spec/09-architecture.md` |
| RLS במאגר המזון | חמש טבלאות המזון: `SELECT` בלבד ל-`anon`. ארבע טבלאות `src_*`: RLS פעיל בלי מדיניות, רק `service_role` | `spec/09-architecture.md` |
| סביבת ייצור | פרויקט Supabase נפרד `nutrition-app` (פרנקפורט), לא בתוך `ytdigitallab`. הפרדה מתיקון 13 ומ-`auth.users` משותף | `spec/08-legal.md` |

## תיעוד ותהליך

| נושא | ההחלטה | מודול |
|---|---|---|
| שפת הקוד | **קוד, הערות, docstrings ומחרוזות פלט באנגלית.** שמות מזון, תוויות יחידות ושמות רכיבי התזונה נשארים בעברית — הם נתונים, לא קוד | `db/` · `spike/` |
| שפת התיעוד | **התיעוד ב-`docs/` נשאר בעברית.** הוא נכתב לקורא שמכיר את המוצר, לא לתורמים חיצוניים | `docs/README.md` |

---

## החלטות מהסשן הנוכחי — 27.08.2026

| # | ההחלטה | הנימוק |
|---|---|---|
| 1 | הספייק ימשיך לקרוא **קובץ מיוצר**, לא DB, גם אחרי שהמאגר ייטען ל-Postgres | שומר על התכונה ש-`run_spike.py` רץ בלי מפתח ובלי רשת, ומשאיר את בדיקת הרגרסיה זולה מספיק כדי שבאמת תורץ |
| 2 | שער הבטיחות ב-`foods` נכתב כ-**CHECK constraint**, לא כבדיקה בקוד | פריט `menu_eligible` בלי כשרות או סקירת אלרגנים לא יוכל להיכנס לטבלה מלכתחילה |
| 3 | הפרדה בין `allergens = '{}'` (נבדק, נקי) ל-`allergens_reviewed_at IS NULL` (לא נבדק) | בלי ההפרדה, מערך ריק נראה כמו תיוג תקין. זה בדיוק סוג הבאג שמדליף אלרגן |
| 4 | אלרגנים במתכונים **נגזרים** מקובץ הרכב המתכונים, ומאושרים ידנית | מקצר את התיוג מ-~350 פריטים לכמה עשרות מצרכי בסיס |
| 5 | 50 המוצרים המסחריים יאומתו **מול תוויות עדכניות**, לא מהקובץ | קובץ הנתונים עודכן ב-2022; מתכוני יצרנים משתנים |
| 6 | בדיקת השפיות (30 פריטים) תהיה **מרובדת לפי מקור** — USDA / תעשייה / מתכונים | לשלושת המקורות דפוסי שגיאה שונים. דגימה אקראית תפספס |
| 7 | ↩ בוטלה 20.09.2026 · הגדרת סיום לשלב הייבוא: `v_pool_depth` + הרצה חוזרת של 40 ייצורים ב-92% | "נטענו 4,500 שורות" אינה טענה בת-בדיקה |
| 8 | התיעוד עובר למבנה מודולרי. סטטוס יושב במקום אחד בלבד | ראה `README.md` |

---

## החלטות מהסשן — 28.08.2026

| תאריך | ההחלטה | הנימוק | מודול |
|---|---|---|---|
| 28.08.2026 | אף מחרוזת שהמשתמש רואה לא נכתבת בקומפוננטה — הכל דרך מודול מחרוזות | שליטה במיקרו-קופי במקום אחד, במיוחד בחסימות בריאותיות ובחומות תשלום | `spec/12-ui-ux.md` |
| 28.08.2026 | המוצר עברי בלבד. אין i18n, אין עמודות שפה | המאגר ישראלי ולכן אין מוצר לתת למשתמש בחו"ל. שפה אינה החסם — המאגר הוא | `spec/10-release.md` |
| 28.08.2026 | קלוד קוד מיישם רק מה שהוכרע. כל השאר עוצר ושואל | החלטה שנכנסה לקוד בלי לעבור דרך `docs/` יוצרת פער בין המערכת לאפיון | `CLAUDE.md` |
| 28.08.2026 | כתיבה ל-`docs/` רק באישור מפורש | `decisions.md` הוא append-only; diff בעברית לא נסקר באמת | `.claude/skills/session-close/` |
| 28.08.2026 | `foods.name_en` ו-`nutrients.name_en` נשארות בסכימה | הן נתוני מקור מקובץ משרד הבריאות, לא i18n. הכלל "אין עמודות `_en`" חל על טקסט ממשק שאנחנו יוצרים | `db/01_food_db_schema.sql` |
| 28.08.2026 | `kcal` **נגזר מהמאקרו**. ערך הקובץ נשמר ב-`kcal_source` ואינו נמחק | במאגר שני מקורות קלוריות שאינם מסכימים — עד 24% בקטניות. הפותר רודף אחרי יעד קלורי ויעדי מאקרו שלא יכולים להתקיים יחד, והוולידטור מעניש אותו על סתירה בנתונים | `spec/05-food-db.md` · `db/01_food_db_schema.sql` |
| 28.08.2026 | נוסחת הגזירה: **P×4 + F×9 + C_זמינות×4 + סיבים×2** | אלה המקדמים הרגולטוריים על תוויות מזון בישראל. משתמש שיבדוק גביע במקרר יראה את אותו מספר | `spec/05-food-db.md` |
| 28.08.2026 | בדיקת Atwater הופכת מבדיקה חד-פעמית ל**שער אצירה קבוע** — ולידטור שלישי לצד `v_eligible_missing_tags` | פריט שגוי אינו מזיק כל עוד אינו `menu_eligible`. מצמצם מניקיון של 4,620 פריטים לשער על 300–400 | `spec/05-food-db.md` |
| 28.08.2026 | תשעת הפריטים הבלתי-מוסברים מאומתים **בזמן אצירה**, במסלול 50 המוצרים המסחריים — לא כתיקון נקודתי | תיקון שורה אחת עכשיו יוצר שיטה שונה לפריט הראשון ולשמונת הבאים | `spec/05-food-db.md` |
| 28.08.2026 | הנוסחה כוללת גם **כוהל × 7**. פוליאולים נשארים בחוץ | כוהל הוא מקדם באותה רגולציה ונשמט מהניסוח המקורי. מנקה 5 מ-7 אפסים שגויים. פוליאולים נמדדו: 45 פריטים מתוך 325, כולם ממתקים שלא ייאצרו | `spec/05-food-db.md` |
| 28.08.2026 | `kcal = NULL` כשאין בסיס מאקרו ו-`kcal_source > 0` | פריט שמצהיר 0 קלוריות ובו 390 הוא כשל שקט. `NULL` נכשל ברעש | `spec/05-food-db.md` |
| 28.08.2026 | 15 שדות האצירה עוברים מ-`foods` לטבלה `food_curation`, **על `source_code` ולא על `foods.id`** | `04_transform.py` עושה `TRUNCATE ... RESTART IDENTITY CASCADE`. `foods` החזיקה נתונים נגזרים ונתונים שנוצרו ביד באותה טבלה, ורק אחד מהשניים ניתן לבנייה מחדש. `RESTART IDENTITY` ממספר מחדש את `foods.id`, ולכן אצירה על המפתח התחליפי הייתה חוזרת ומצביעה על מזון אחר — בשקט, בלי שגיאה ובלי יתום | `db/06_split_curation.sql` · `spec/05-food-db.md` |
| 28.08.2026 | **בלי FOREIGN KEY** מ-`food_curation` ל-`foods`. שלמות נאכפת ב-`v_curation_orphans` שחייב 0 שורות | `TRUNCATE ... CASCADE` מרוקן כל טבלה שמפנה לטבלה שברשימה. FK כאן היה **מוליך** את המחיקה ולא מונע אותה — כלומר משחזר בדיוק את הבאג, תוך שהוא נראה כמו התוספת הזהירה | `db/06_split_curation.sql` |
| 28.08.2026 | **כלל ה-JOIN:** view שתפקידו להצביע על מה שטרם נאצר משתמש ב-**LEFT JOIN** ל-`food_curation` בצד הנבדק | אחרת ה-view מדווח "נקי" על מה שאיש לא הסתכל עליו — שורה חסרת אצירה נעלמת מה-INNER JOIN דווקא כשהיא הממצא. נקבע פעמיים באותו יום: `v_kcal_outliers` (שער האצירה חייב לעבוד על מאגר לא אצור) ו-`v_recipe_unreviewed_components` (רכיב בלי שורת אצירה כלל הוא הלא-נסקר החמור ביותר) | `db/01_food_db_schema.sql` · `db/06_split_curation.sql` |
| 28.08.2026 | `by_weight` ו-`tags` עוברים ל-`food_curation`; `complete` **נשאר** על `foods` | `CHECK` לא חוצה טבלאות, ו-`whole_only_requires_a_unit` קורא גם את `whole_only` וגם את `by_weight`. `complete` נגזר מתשע חומצות האמינו ב-`04_transform.py` — בנייה מחדש היא בדיוק מה שאמור לשחזר אותו | `db/06_split_curation.sql` |
| 28.08.2026 | שתי בדיקות ב-`04_transform.py` הן **תנאי עצירה** ולא בדיקות אינפורמטיביות: `v_curation_orphans = 0`, וספירת `food_curation` לפני ואחרי. כישלון מגלגל את ההרצה אחורה | הן מחליפות את ה-FK. תנאי עצירה שרק מדפיס שווה כלום — עד שמישהו קורא את הדוח, המחיקה כבר התחייבה | `db/04_transform.py` |
| 28.08.2026 | **תיקון להחלטה 2 מ-27.08:** שלושת ה-CHECK של שער הבטיחות עברו מ-`foods` ל-`food_curation`, ללא שינוי בתוכנם | ההחלטה עצמה עומדת — שער הבטיחות הוא אילוץ ולא בדיקה. רק הטבלה שעליה הוא יושב השתנתה, כי העמודות שהוא שומר עליהן עברו. אומת בייצור: שלושתם דוחים בפועל | `spec/05-food-db.md` §4 |
| 28.08.2026 | אצירת שלב ב' רצה ב**מסלול צר**: שומן וחלבון עד רצפת ההיתכנות (~60 פריטים), ייצור מוקדם כמבחן עשן, ורק אז הרחבה ל-300–400 | שני תנאי שער היציאה דורשים כמויות עבודה בסדר גודל שונה. 40 הייצורים הם המפגש הראשון של הפותר, הניסוח העברי ומידות ה-`Mida` האמיתיות — כישלון שם מחייב תיוג מחדש, ועדיף לגלותו אחרי 60 פריטים ולא אחרי 350. התיוג עצמו אינו נעשה פעמיים: ה-60 הם תת-קבוצה של ה-350. בנוסף, שומן וחלבון הם הצוואר הצר (17% ו-20% באחוזון 10) — "פחות עבודה קודם" ו"העבודה הכי שווה קודם" מצביעים לאותו כיוון | `spec/05-food-db.md` §8 · `work/2026-08-28-curation.md` |
| 28.08.2026 | ארבעת המצרכים שהוחרגו — פילה בקר 638, דגי דניס 1134 / 9691 / 9784 — מוחזרים עם `carb_g = 0` | בבשר שריר ובדג טהור פחמימה ריקה במקור פירושה אפס, לא נתון חסר. שיפוט תזונתי חתום, לא תיקון נתונים. סוגר את שאלה פתוחה #14 | `spec/05-food-db.md` |
| 28.08.2026 | תוכנית עבודה עם הערכות זמן יושבת ב-`work/<תאריך>-<נושא>.md`, ולא בשורש `docs/` | קובץ תוכנית בשורש היה מקור אמת שני על "מה נשאר" לצד `PROGRESS.md` — בדיוק מה שהמבנה המודולרי נבנה למנוע. קובץ `work/` נפתח כתוכנית ונסגר כיומן: ההערכה נכתבת מראש, הזמן בפועל לצידה, והפער מכייל את ההערכה הבאה. הסטטוס נשאר ב-`PROGRESS.md` בלבד | `docs/README.md` |
| 28.08.2026 | עבודה ישירות על `main`. בלי ענפי עבודה, בלי `merge --no-ff` | פיתוח יחיד. הענף היה אמור לשמש שער סקירה לפני `main`, אבל השערים האמיתיים מקדימים אותו: §1 אוסר יישום של מה שלא הוכרע, ו-§3 אוסר כתיבה ל-`docs/` בלי אישור בשיחה. שניהם פועלים לפני הקומיט, ולכן הענף הוסיף צעד ולא בדיקה | `CLAUDE.md` §8 |
| 29.08.2026 | **כלל #15 מאומץ:** פריט בקטגוריית בשר או דג, בלי שורות ב-`food_recipe_components`, עם `carb_g` ריק — מקבל 0 | נמדד על 245 פריטים. הזנב השלילי (חציון −3.36%, 185 מתחת ל-−2%) נראה כמו פחמימה מוסתרת, והתברר כהפרש בין מערכות מקדמים: הפער גדל מונוטונית עם חלק החלבון באנרגיה — תלות שפחמימה חסרה לא הייתה מייצרת. במקדמי בשר (4.27/9.02) נשארים 6 בלתי-מוסברים מתוך 245, ושלושה מהם כבר בתור האצירה | `spec/05-food-db.md` · `open-questions.md` #15 |
| 29.08.2026 | **מקדמי Atwater ייעודיים הם מכשיר אבחון, לא נוסחה.** הגזירה נשארת P×4 + F×9 + C×4 + סיבים×2 + כוהל×7 | 4/9 הם המקדמים הרגולטוריים על התווית בישראל, וזו ההחלטה מ-28.08. 4.27/9.02 משמשים רק לשאלה "האם הפער אמיתי" — הם הפרידו ממצא מארטיפקט ב-#15 | `spec/05-food-db.md` §7 |
| 29.08.2026 | **קובץ המקור מערבב שתי קונבנציות חישוב.** פער של 3–5% בין נגזר למוצהר בפריט בשר הוא נורמלי | 14 מתוך 20 החורגים מדויקים ב-4/9 ומוגזמים ב-4.27/9.02 — כלומר חושבו במקדמים כלליים. מתיישב עם ריבוד ה-`makor`. הסף של שער Atwater (12%) גבוה מספיק ולא מושפע, אבל בלי הרישום הזה סשן עתידי ירדוף אחרי −3% | `spec/05-food-db.md` §7 |
| 29.08.2026 | **#13 נסגרת:** `COALESCE(0)` על `fiber_g` עומד, **בתוספת בדיקת אצירה** — פריט צמחי עם `fiber_g` ריק שנכנס ל-`menu_eligible` מאומת מול תווית | מתוך 180 בשומן וחלבון, 165 מתחת ל-6.6% פער. אבל בבשר, דג, חלב וביצה אפס סיבים נכון, ובקטניות ואגוזים שגוי — טמפה, קמח סויה, גרעיני אבטיח, חמאת אגוזים, שהם מקורות המסלול הצר. אותו דפוס של שער Atwater: לא ניקוי של 387, אלא בדיקה על מה שנכנס | `spec/05-food-db.md` §5 |
| 29.08.2026 | **חמשת פריטי `fat_g = 0`:** 1576 (חלבון ביצה) תקין ונשאר · 4498 (MSG) ו-4839 (אבקת תה) מודרים עם סיבה · 1699 (סייטן) ו-8623 (צ'אך) לתור האצירה | 1576 באמת נטול שומן. 4498 ו-4839 נושאים חנקן שאינו חלבון — ארטיפקט של המרת קילדל; 46.8 ג' "חלבון" באפס שומן הוא הפריט האידיאלי לכל מיון לפי רזון, אותה מלכודת שאבקות החלבון יצרו. 8623 הוא נתח שומני עם 0 שומן, ומצטרף לתשעת החשודים | `spec/05-food-db.md` |
| 29.08.2026 | **התיעוד נכתב לפני העבודה, לא רק בסגירת סשן.** הכרעה שהתקבלה בדיון נכתבת לפני שהבלוק הראשון רץ | הכרעות שלב ב' נכתבו ב-`fb9200c` לפני שנגענו במאגר. הרווח התגלה בבלוק 1: קלוד קוד עצר שלוש פעמים והצליב מול `decisions.md` ו-`open-questions.md` — מסמכים שלא היו קיימים אילו התיעוד היה מחכה לסוף. הסגירה נשארת בסוף; ההכרעות מקדימות | `CLAUDE.md` §3 |
| 29.08.2026 | **הצ'אט מנסח את נוסח התיעוד; קלוד קוד כותב אותו לקבצים.** כל כתיבה לקובץ — קוד, SQL ו-`docs/` — עוברת דרך קלוד קוד | שני שערים במקום אחד: הצ'אט מציג את השורות המדויקות לאישור, וקלוד קוד עוצר שוב אם העוגן בקובץ אינו כמתואר. בסשן הזה השער השני תפס שורת טבלה ב-`CLAUDE.md` §8 שהצ'אט פספס, והייתה סותרת את הפסקה מתחתיה | `CLAUDE.md` §8 · `.claude/skills/session-close/` |
| 29.08.2026 | **מאגר התפריט מחזיק צורות נאכלות בלבד.** פריט שקיים גם ביבש וגם במוכן — המוכנה היא זו שנאצרת. אבקות שנצרכות כאבקה הן היוצא מן הכלל, ומוסדרות ב-`supp` ובתקרת §5.4. האכיפה בסימון ובהכרעה אנושית באצירה, לא בהדרה אוטומטית | 128 פריטים עם `protein_g ≥ 10` הם צורה יבשה, אבקה או קמח, והם מדורגים מעל המקבילה המוכנה בכל מיון לפי חלק החלבון — עדשים יבשות 0.39 מול מבושלות 0.35. זו המלכודת של §5.4 בשנית. החלופה — לאצור יבש עם `prep` ותקרה על משקל יבש — הייתה משנה את חוזה הניסוח של המחולל, שיושב מאחורי שער הרגרסיה, ומוסיפה לקנרית משתנה שני שיטשטש כל כישלון. מתכונים מוקפאים, ולכן פריט הדורש בישול אינו שורת תפריט במוצר הזה. העלות נמדדה ואפסית: 59 לא-יבשים מול מכסה של 10. התאמת שם חלשה בשני הכיוונים — "חלבון אפונה" נתפס כיבש והוא לגיטימי, ואבקה בת 80.7 ג' חלבון נשמטה כי אין בשמה "אבקה" | `spec/05-food-db.md` §5 |
| 29.08.2026 | **תיקון לנוסח כלל #15.** התנאי "בלי שורות ב-`food_recipe_components`" יורד מהנוסח. המימוש נשאר רשימה מפורשת, **בתוספת תנאי עצירה** ב-`04_transform.py`: פריט מקור עם פחמימה ריקה שאינו ברשימה מגלגל את ההרצה אחורה | שלושה מארבעת הפריטים שהכלל מכסה כן נושאים שורת רכיב — 9691 ו-9784 עם רכיב יחיד (1134, הדג הגולמי, שגם בו `carb_g = 0`), ו-638 כרכיב של עצמו ב-100 ג'. הנתונים נכונים, הנוסח לא. אין באג היום, כי המימוש הוא רשימה ולא הכלל. הבאג עתידי: `carb_g` הוא `NOT NULL`, ולכן פריט בשר חדש בעדכון מקור ייפלט **בשקט**. תנאי עצירה הוא הדפוס הקיים — `kcal = NULL` נבחר כי NULL נכשל ברעש, ושתי הבדיקות ב-`04_transform.py` מגלגלות אחורה ולא מדפיסות | `spec/05-food-db.md` §5.0.1 · `db/04_transform.py` |
| 29.08.2026 | **מיפוי `class_code` → קטגוריית אצירה הוא הכרעה מתועדת, לא פרט מימוש.** 25 משפחות המקור, כל אחת עם רצפה ומכסה, עוברות מקוד `07_curation_candidates.py` לטבלה ב-`spec/05-food-db.md` §5 | הפיצולים ברמת p6 הם שיפוט תזונתי ולא טרנספורמציה: 7551 מכיל זיתים לצד חמוצים ופלפלים, 4310 מכיל טחינה לצד גרעינים אחרים, ו-81 מערבב חמאה עם שומן חזיר. רצפת הקטניות (7 ג' חלבון, 0.20 חלק, 80 קק"ל) נקבעה כי רצפת 10 ג' מחקה בדיוק את מה ש-§5 מחייב | `spec/05-food-db.md` §5 |
| 29.08.2026 | **הדחיפה המאושרת מראש היא ל-`main` בלבד.** ענף עבודה אינו נדחף ל-`origin`. סשן שרץ ב-worktree מבודד שומר קומיטים מקומית, ובסגירת הבלוק: `merge --ff-only` ל-`main` בריפו הראשי ואז דחיפת `main` | Claude Code מבודד כל סשן ב-worktree, ו-git מסרב לחבר שני worktrees לאותו ענף — כלומר הענף נכפה וההחלטה מ-28.08 ("עבודה ישירות על main") לא הייתה ניתנת לאכיפה. הענף **המרוחק** לעומת זאת היה בחירה: `push -u origin claude/...` במקום `push origin HEAD:main`, אותו fast-forward בדיוק. §8 אישר "דחיפה" מראש בלי לומר לאן, ושתי השורות יחד נקראו כ"תדחוף בלי לשאול". `--ff-only` ולא `merge`, כדי שהסתעפות אמיתית תיכשל ברעש | `CLAUDE.md` §8 |
| 29.08.2026 | **תיקון להחלטת 28.08 על מיקום תוכניות העבודה:** רשימת המשימות של השלב הפעיל, כולל הערכות ו"בפועל", יושבת ב-`PROGRESS.md` בלבד. קובץ `work/` הוא יומן קפוא של בלוקים שנסגרו | טבלת "מה נשאר" בקובץ העבודה הפכה למקור שני של "מה נשאר" וסטתה מ-`PROGRESS.md` בתוך יום — 1b הוצג כפתוח במלואו כששני שלישיו בוצעו. ההחלטה המקורית נועדה למנוע בדיוק את הכפילות הזו | `docs/README.md` · `PROGRESS.md` |
| 29.08.2026 | **תנאי העצירה של כלל #15 חל על ארבעת שדות המאקרו**, לא על פחמימה בלבד. הבדיקה האינפורמטיבית הקיימת הפכה לתנאי עצירה שלישי, בלי שאילתה חדשה | לשון ההחלטה דיברה על פחמימה, אבל הנימוק — "נפלט בשקט" — חל זהה על חלבון, שומן ואנרגיה. הבדיקה כבר סרקה את ארבעתם והודפסה עם "Expected: none"; הפיכתה לתנאי עצירה מצמצמת מנגנונים במקום להרחיב היקף | `db/04_transform.py` · `spec/05-food-db.md` §5.0.1 |
| 29.08.2026 | **תנאי עצירה נגזר מהפרדיקט שהוא שומר עליו, ולא מנוסח מחדש לצידו** | הבדיקה בדקה `IS NULL` וה-`INSERT` סינן ברג'קס — שתי הגדרות שונות ל"חסר". שורה שנפלטה בגלל מחרוזת ריקה הייתה עוברת את השער ומודפסת עם `missing` ריק, הודעה שאינה אומרת דבר. אותה משפחה של כלל ה-LEFT JOIN ושל הסייג על `v_eligible_missing_tags`: שער שנוסח בנפרד מהדבר שהוא שומר עליו מדווח "נקי" על מה שלא בדק | `db/04_transform.py` |
| 29.08.2026 | **משפחת אבקות ותוספי החלבון מורחבת ל-`p6` 414401 לצד 414400.** 1726 (אבקת סויה תעשייתית, 41 ג') נתפסת במשפחה ונופלת ברצפת 50 ג' | 414400 תפס רק את 1720, שנופלת ברצפה, ולכן 1724 (80.7 ג') ו-8274 (63.6 ג') נפלו לקטניות ותפסו 2 מ-10 מקומות — הקטגוריה שבדיוק בגללה הורדה רצפת הקטניות ל-7 ג'. שלוש השורות ב-414401 נבדקו ואף אחת אינה מזון רגיל. החשיפה לאבקות ירדה מ-9.2% ל-6.2% מ-65 והתרכזה במשפחה אחת במקום להתחבא בשתיים | `spec/05-food-db.md` §5.5 · `db/07_curation_candidates.py` |
| 29.08.2026 | **מבחין הצורה היבשה הוא שני אותות שחייבים להסכים:** שם עברי המעיד על צורה יבשה **וגם** `moisture < 20`. משפחת האבקות פטורה, ו"קלייה יבשה" מוחרג במפורש | כל אות לבדו נמדד ונפסל. לחות לבדה מדליקה 40 מ-119, ובהם 32 מ-54 פריטי השומן — שמן זית 0.0, טחינה 0.0, אגוזים 1–4, כולם צורות נאכלות. שם לבדו מדליק 11 ובהם 4 שגויים: המקור נוקב בחומר הגלם ואז בהכנה ("עדשים יבשים, מבושלים"), ו"קלייה יבשה" היא שיטת בישול ולא צורה. הצירוף מדליק 7, כולם בחלבון | `spec/05-food-db.md` §5.0.2 · `db/07_curation_candidates.py` |
| 29.08.2026 | **תוספת לשער העצירה הכפול:** נקודת עצירה על **אמת עובדתית** חייבת לנקוב מה לעשות אם היא נכשלת — לא רק "עצור ושאל" | נקודה 4 בפרומפט התיעוד בדקה הנחה עובדתית של הצ'אט על הרכב משפחת הסייטן, ואמרה מראש "אם אינם שני הפריטים — אל תכתוב את המשפט השני". ההנחה אכן הייתה שגויה: 1699 יושב ב-`class_code` 41420209 ולא במשפחה, ובן הזוג האמיתי הוא 9851 — סייטן מוכן, לא מסומן `dry?`. בלי ההוראה המפורשת הכישלון היה עוצר קומיט באמצע; איתה הוא ייצר השמטה נקייה. הצ'אט לא רואה את הריפו (§8), ולכן כל עובדה שהוא מציג בנוסח לכתיבה היא הנחה עד שנבדקה. השער השני תפס שלושה דברים בסשן הזה: שורת טבלה ב-`CLAUDE.md` §8, ההבדל בין `IS NULL` לרג'קס ב-`04_transform.py`, וזה | `CLAUDE.md` §8 |
| 29.08.2026 | **יחידת ההגשה למחולל נבחרת בכלל דטרמיניסטי בסקריפט הייצוא**, עם מילון חריגים מפורש באותו קובץ. לא `is_default` בטבלה, ולא עמודה חדשה ב-`food_curation` | `food_servings` נבנית מחדש ב-`TRUNCATE ... CASCADE`, ולכן סימון ידני בה הוא בדיוק הבאג מ-28.08 — נתון שנוצר ביד בטבלה בת-בנייה-מחדש. עמודה ב-`food_curation` הייתה שורדת, אך מוסיפה שדה אצירה לפני שידוע שהכלל אינו מספיק. אוצר המילים סגור: 88 תוויות ביחס 1:1 ל-`mida_code`, ו"אריזה" (305 שורות) אינה מנה. הדפוס הוא כלל בתוספת רשימת שיפוט מפורשת, כמו `CARB_ZERO_BY_JUDGEMENT` ב-#15 | `spec/05-food-db.md` §5.0.3 |
| 29.08.2026 | **`label_he_plural` נמלא ב-`04_transform.py` ממילון `mida_code` → ריבוי, 88 ערכים.** לא בייצוא ולא ידנית | הריבוי אינו נתון מקור אלא מורפולוגיה עברית שאנחנו מייצרים, ולכן מקומו בקוד שנבנה מחדש ולא בטבלה שנכתבת ביד. כל 9,864 השורות ריקות היום, והניסוח העברי היה מוציא "2 כף" ו-"3 כוס". מילון על `mida_code` מתקן את המאגר כולו פעם אחת ושורד בנייה מחדש | `spec/05-food-db.md` §5.0.3 · `db/04_transform.py` |
| 29.08.2026 | **`name_he` נכנס לפרומפט כמות שהוא בקנרית.** אין שם-תצוגה, אין קיצור מכני. שיעור ההעתקה המדויקת נמדד על 25 הפריטים | 3,677 מ-4,624 השמות מכילים פסיק, וכלל 11 בפרומפט דורש העתקה מדויקת. קיצור בפסיק הראשון הופך "עדשים יבשים, מבושלים ללא מלח" ל"עדשים יבשים" — היפוך משמעות, ודווקא על הפריט שנתפס ב-3ב. עמודת שם-תצוגה היא שינוי סכימה לפני שיש מספר. אם ההעתקה נשברת, התיקון הוא שהמודל יחזיר קוד ולא שם — שינוי בגנרטור, מאחורי שער הרגרסיה, ולכן לא בבלוק 3 | `spec/05-food-db.md` §5.0.3 |
| 30.08.2026 | **R6 יורה על תחום הגרמים בלבד, לא על תקרת הקלוריות.** פריט שאיבד יחידה אנושית ב-R2 יוצא `by_weight`; פריט שאיבד אותה ב-R3 שומר את הכף | נפילה ב-R2 אומרת שהמנה האנושית גדולה פיזית, ולכן כף תשקר — יוגורט 45 היה מקבל "12 כפות". נפילה ב-R3 אומרת שהמזון מרוכז, ולכן הכף היא-היא המנה — שמן זית 4443 מקבל כף = 10 ג'. הנוסח המקורי ירה על שניהם, והתגלה בהרצת העשן כשהוא סותר את טבלת הציפייה שאושרה. מכריע נוסף: `unit=None` עם `max_g=NULL` שולח את `_options` ל-`range(10, 401, 10)`, כלומר 400 ג' שמן זית — משפחת הבאגים של "400 ג' אבוקדו" | `db/09_export_menu_foods.py` · `spec/05-food-db.md` §5.0.3 |
| 30.08.2026 | **`menu_eligible` דורש `prep`, `price` ו-`by_weight` מפורשים, ב-CHECK נפרד מ-`eligible_requires_safety_tagging`.** ברירת המחדל של `by_weight` משתנה מ-`true` ל-`NULL` | `filters.py` קורס על `prep`/`price` שהם NULL, ו-`portions.py` בודק `by_weight` לפני `unit` — כלומר `DEFAULT true` הופך יחידה שנבחרה לנתון מת ומנסח את התפריט בגרמים **בשקט**. זה הדפוס ש-`kcal = NULL` נבחר כדי להימנע ממנו: כשל ברעש עדיף על השלמה שקטה. אילוץ נפרד ולא הרחבה של אילוץ הבטיחות — אילוץ בטיחות שאוכף גם מחיר הוא אילוץ ששמו משקר | `db/10_menu_fields_check.sql` |
| 30.08.2026 | **#23 נסגרת: פרוסת רוחב ב-3ד.** 4 פריטי `carb` ו-4 פריטי `veg` לצד ה-25, סה"כ 33. `fruit` ו-`drink` אינם נאצרים בשלב הזה | `pool_health()` נמדד: 12 פריטים ולא 60 — protein 4 · carb 3 · veg 3 · fat 2. `fruit` ו-`drink` אינם נבדקים ולכן אינם חוסמים, ומסלול הפותר שהם היו מפעילים מופעל ממילא על ידי הירקות. 4 ולא 3 כי ישיבה על הרצפה הופכת סינון פריט יחיד לקנרית שלא רצה, וכישלון על דלות אינו מעיד על הצינור. החלופה — מאגר מעורב עם `foods.py` — נפסלה כמשתנה שני בקנרית, אותו נימוק שדחה אצירת צורות יבשות ב-29.08 | `spec/05-food-db.md` §5 |
| 30.08.2026 | **`quality` אינו שדה נגזר.** הקוד אינו מציע אותו; כל 119 המועמדים חזרו ריקים. שורת בלוק 5 ב-`PROGRESS.md` מתוקנת | §3 מגדיר את הדירוג כתיוג ידני ו-§5.3 מגדיר את שלוש הדרגות; אין במאגר שדה שמדרג ערך ביולוגי. `07_curation_candidates.py` פוסל במפורש את `complete` כתחליף, ובצדק: שלמות אינה דרגה. ב-`spike/foods.py` טופו הוא `complete=True, quality=2` ואבקת מי גבינה `complete=True, quality=3` — אותו ערך בשדה הנגזר, שתי דרגות שונות. **אישוש, ולא הסיבה:** גם הצעה ברמת משפחה לא הייתה עובדת, שכן משפחה 21 "קטניות וסויה" מחזיקה יחד את דרגה 2 (סויה) ואת דרגה 1 (עדשים, אפונה, תורמוס) בלי פיצול `class_code`. המנגנון הזה לא נוסה ולא נפסל בהרצה; הקוד נמנע לפניו | `spec/05-food-db.md` §3 · §5.3 |
| 30.08.2026 | **עוגן נלקח מהמקור, לא מהפלט.** פרומפט אינו נוקב בטקסט להחלפה אלא במיקום ובכוונה. `CLAUDE.md` §8 מורחב בהתאם | שלב 6 בפרומפט 3ד1 נוסח כ"ישן X / חדש Y" — הפרה ישירה של §8 — ועגן על המחרוזת כפי שהיא מודפסת בדוח. במקור היא מפוצלת על שתי שורות, והעוגן החזיר 0 התאמות. **השער עבד** והתיקון עצר לפני הכתיבה. אבל הצ'אט אינו רואה את הריפו, וכל מחרוזת שהוא מצטט מפלט תישא את אותו סיכון: עיצוב הדוח אינו עיצוב המקור | `CLAUDE.md` §8 |
| 30.08.2026 | **אלרגנים נקבעים מזהות המזון, לא ממקור הנתונים.** פריט שהשם קובע את הרכבו נאצר מהקובץ; מוצר מורכב שהרכבו נקבע ביצרן דורש תווית ואינו נאצר במסלול הצר. 4291 ממרח זיתים טפנד יוצא מבלוק 3 ועובר לבלוק 7 | `makor` אינו המבחין: 1898 טחינה גולמית היא `makor = 3` ונאצרה, כי טחינה היא שומשום בכל צנצנת. ל-4291 `english_name` מסחרי בלי רשימת רכיבים, `makor = 3`, ואפס שורות ב-`food_recipe_components` — אין נתיב, לא מהקובץ ולא מההורשה. **והצעת הכשרות אינה ראיה על האלרגן:** המשפחה החזירה `parve`, וזה נכון גם עם אנשובי, כי דג הוא פרווה. `kosher` ו-`allergens` הם שני מבחנים נפרדים ואינם מכסים זה את זה | `spec/05-food-db.md` §3 · בלוק 6 |
| 30.08.2026 | **`max_g` במקטע השומן נלקח מטבלת `MAX_G` ב-`spike/foods.py` היכן שיש מקבילה.** 5 מ-8 זהים; קנולה בהיקש משמן זית, אבוקדו נגזר מחדש, חמאה חדשה | הטבלה בספייק היא שיפוט אנושי שכבר עבר 40 ייצורים ב-92%, ומספר חדש היה זורק את הכיול הזה. אבוקדו סוטה כי היחידה שלנו היא `יחידה בינונית = 114` מול 200 בספייק; 150 קשור ליחידה שלנו ונותן בדיוק אחד תחת `whole_only` | `spec/05-food-db.md` §5.0 |
| 30.08.2026 | **מפתח המיון ב-`07_curation_candidates.py` פגום ויתוקן כבלוק נפרד לפני בלוק 4.** הכיוון שנקבע: `complete` יוצא מהמפתח, `src_rank` ו-`servings_gate` יורדים מתחת ל-`share`, `outlier` נשאר למעלה | שלושת המפתחות העליונים הם מטא-נתונים על איכות הרשומה במאגר ולא על איכות המזון, ושני המפתחות התזונתיים מוכרעים רק בתוך פס שכבר נסגר. בעוף הפס הראשון מכיל 74 פריטים על מכסה 12, כלומר 141 מ-215 המועמדים אינם נגישים מבנית. 1347 טונה מבושלת, בעלת ה-`share` הגבוה במשפחת הדגים (0.958), נחתכה על `complete=false` בעוד 1132 סורימי לתעשייה נכנס למכסה. האסימטריה מכריעה: מועמד גרוע בגיליון עולה שורת סקירה אחת, ומזון טוב שלא הגיע לגיליון הוא בלתי-נראה לצמיתות ואין דוח שמונה אותו | `db/07_curation_candidates.py` · `spec/05-food-db.md` §5 |
| 30.08.2026 | **הגיליון הוא הצעה ולא רשימה סגורה.** 805, 944 ו-1347 נוספו ביד ואינם מבין 119 המועמדים | פעולה משלימה להוצאת 4291 באותו יום. שלושתם עוברים את ארבעת תנאי הסינון של הבורר — `POOL_SQL`, `is_excluded`, `family_of`, `passes_floor` — ו-`flags_of` מחזיר ריק לכולם. מה שהדיר אותם היה `rows[:quota]` בלבד, כלומר מכסה ולא פסילה | `db/11_curation_sheet.py` · `spec/05-food-db.md` §5 |
| 30.08.2026 | **`max_g` במקטע החלבון — 2 מ-10 נלקחו מ-`MAX_G` בספייק** (8608 מקבל 75, 8712 מקבל 60), ו-8 נגזרו מהיחידה שנבחרה במאגר | "יש מקבילה בספייק" אינו "יש ערך ב-`MAX_G`". הטבלה מחזיקה 12 רשומות בלבד ו-`MAX_G.get(name)` מחזיר `None` לכל השאר — קוטג', טופו ועדשים בכללם, אף שלשלושתם יש מקבילה בשם. כלל 30.08 חל על שניים ולא על חמישה, כפי שהצ'אט הניח לפני המדידה | `spike/foods.py` · `spec/05-food-db.md` §5.0 |
| 30.08.2026 | **`price` יורד מהמוצר.** ההסרה היא בלוק נפרד אחרי הקנרית ולא לפניה. עשר שורות החלבון נכתבו עם ערכי `price` אמיתיים ולא עם מציין מקום | החלטת מוצר של הבעלים. ההסרה נוגעת בחמישה מקומות — `03-functional.md` §2.5, `09-architecture.md` §4.2, `10_menu_fields_check.sql`, `01_food_db_schema.sql`, ו-`filters.py` — והאחרון יושב מאחורי שער הרגרסיה של 40 הייצורים. משתנה שני בקנרית מטשטש כל כישלון; אותו נימוק שדחה אצירת צורות יבשות ב-29.08 ומאגר מעורב ב-30.08. ערכים אמיתיים ולא מציין מקום, כדי שעשר שורות לא ישקרו ל-`filters.py` אם ההסרה תידחה | `spec/02-business.md` · `spec/03-functional.md` §2.5 · `spec/09-architecture.md` §4.2 · `db/10_menu_fields_check.sql` |
| 30.08.2026 | **מספר שנכנס לקובץ append-only נמדד בשאילתה שנכתבה לצורך הרישום.** שאילתת חקירה אינה מקור לתיעוד | ארבעה מספרים שהצ'אט מסר ב-30.08 לא שרדו מדידה חוזרת. "8193 נושא 7 שורות חומצות אמינו" — הגיע משאילתת אוריינטציה עם רשימת דפוסי `ILIKE` עברית שהייתה ידועה כחלקית באותו רגע; הנמדד 9. "ארבעה מ-65 מועמדי החלבון נושאים לא מבושל" — נספר בעין וכלל את 8803, שאינו בין ה-65 אלא בדירוג 77; הנמדד 3. "חמש מקבילות בספייק" — `MAX_G` מחזיקה 12 רשומות ו-`MAX_G.get` מחזיר `None` לקוטג', לטופו ולעדשים; הכלל חל על שתיים. ו-`max_g = 220` ל-1267, שאינו כפולה שלמה של יחידת 150 הגרם שנבחרה לו. הכשל אינו במדידה הרופפת — היא לגיטימית ומועילה לאוריינטציה; הכשל הוא במעבר ממנה לתיעוד בלי מדידה חוזרת. §8 כבר מכסה קבצים ("צ'אט מתאר, קלוד קוד בודק"), אבל לצ'אט יש גישה למסד, ולכן זהו נתיב שני שהניסוח הקיים לא סגר. הכלל מקבע התנהגות שכבר קרתה: ארבעת המקרים נתפסו לפני שנכתבו — שניים על ידי אימות יזום של קלוד קוד ב-3ד4, אחד בדוח 3ד3א, ואחד בצ'אט | `CLAUDE.md` §8 |
| 30.08.2026 | **פרוסת הרוחב — 4 `carb` ו-4 `veg` — נבחרת בשאילתה על המסד ולא מגיליון המועמדים** | לפחמימה ולירק אין גיליון כלל; `11_curation_sheet.py` בנוי למשפחות השומן והחלבון בלבד. ובאותו יום התברר שמפתח המיון של הבורר פגום, ולכן הרחבת הגיליון הייתה מייצרת מועמדים לפי אותו דירוג שנפסל. המחיר: מתוך חמשת הדגלים ארבעה ניתנים בשאילתה — `dry?`, `kcal_outlier?`, `fiber?`, `verify_queue?` — ואילו `container_21?` דורש את `choose_unit` בפייתון ואינו זמין. השער שהוא מגן עליו קיים בנפרד: `09_export_menu_foods.py --dry-run` ירה בפועל כאימות 8 ב-3ד3ב והחזיר 18 עם יחידה ו-0 ב-`problems` | `db/11_curation_sheet.py` · `spec/05-food-db.md` §5 |
| 30.08.2026 | **יחידת התפריט נקבעת לפי קטגוריה, ולא לפי מה שהמקור מציע.** `veg` ביחידות שלמות · `carb` בכוס · לחם בפרוסה. כף וכפית פסולות כיחידת תפריט לפחמימה. פריט שאין לו יחידה סבירה בקטגוריה שלו נאצר עם `by_weight = true` ו-`max_g`. המנגנון הוא `UNIT_BY_JUDGEMENT` פר פריט. `fat` ו-`protein` אינם מושפעים | `choose_unit()` מדרג לפי "קיימת שורת הגשה", לא לפי "האם אדם יכול לדמיין את המנה". נמדד בייצור: לעגבנייה 3663 שורת פרוסה 30 ג' לצד יחידות 90/160/230 · לאורז 2721 שורת כף 11 ג' · והיחידה השלמה היחידה של חסה 3808 היא 547 ג', של כרובית 3777 עד 996 ג' ושל כרוב אדום 3775 עד 1594 ג'. שני כשלים הפוכים שמייצרים אותה תוצאה — שורת תפריט תקפה טכנית וחסרת ערך מעשי: "3 פרוסות עגבנייה" או "ראש כרוב אחד". **`whole_only` אינו פותר זאת** — הוא מאלץ כפולות שלמות של היחידה שכבר נבחרה, ולכן אם נבחרה פרוסה, "שלם" הוא פרוסות שלמות. חלופת הגרמים היא מה שהופך את הכלל לכולל: בלעדיה מאגר הירק מצטמצם למלפפון, פלפל ואספרגוס | `spec/05-food-db.md` §5.0 · `db/09_export_menu_foods.py` · `open-questions.md` #21 |
| 30.08.2026 | **פרומפט לקלוד קוד נכתב באנגלית.** תוכן עברי שנועד להעתקה מילה במילה מסומן במפורש. מעל כל פרומפט — תקציר עברי של מה שהוא עושה ומה בו בלתי-הפיך | §2 מגדיר שפה לקוד, לקומיטים, ל-SQL, לפלט, ל-`docs/` ולשיחה, ואינו מזכיר פרומפטים — ולכן הבחירה נעשתה פר-פרומפט ובלי עקביות. לפי הלוגיקה של §2 פרומפט אינו שיחה אלא הוראת עבודה, כלומר "כל השאר". **טענת ביצועים לא נבדקה ואינה חלק מהנימוק.** מה שכן: הוראה באנגלית ותוכן בעברית אינם ניתנים לבלבול, וזה רלוונטי לפרומפטים שנושאים תוכן ל-`docs/`. **המחיר:** סקירת הבעלים לפני ההדבקה היא שער אמיתי, והתקציר העברי אינו תחליף לקריאת הבלוק כשיש בו כתיבה ל-append-only | `CLAUDE.md` §2 |
| 30.08.2026 | **תיקון לכלל יחידת התפריט מאותו יום: נוספת שורה שלישית לטבלת §5.0 — `carb` — ירקות עמילניים (תפוח אדמה, בטטה) → יחידה שלמה** | הכלל כפי שנוסח החזיק שתי שורות פחמימה בלבד — דגנים מבושלים ולחם — ותפוח אדמה מבושל אינו אף אחת מהן. נמדד ב-3ד5: `choose_unit()` בחר ל-3459 את `103 יחידה בינונית, 120 ג'`, שעוברת נקי את מבחן "מנה שאדם יכול לדמיין" של אותו סעיף עצמו. כלומר טבלת הקטגוריות ומבחן הסבירות הצביעו לכיוונים הפוכים, והמבחן הצר יותר סימן VIOLATES על פריט תקין. **הכלל היה חסר, לא הפריט שגוי.** החלופה — תפוח אדמה ב-`by_weight` — הייתה מייצרת "137 גרם תפוח אדמה", בדיוק מה שהכלל נכתב כדי למנוע | `spec/05-food-db.md` §5.0 |
| 31.08.2026 | **`09_export_menu_foods.py` מאפס את `unit` כשה-`by_weight` דלוק.** שורה שיוצאת במשקל אינה נושאת יחידה בפלט כלל | נמדד ב-3ד6 על 3807 חסה, הפריט הראשון שדרך על נתיב `by_weight`: הייצוא החזיר `by_weight: True` ולצידו `unit: {he: 'עלה', grams: 20.0}`. `choose_unit()` נקרא בלי לראות את `by_weight`, ותוצאתו מוצבת ללא תנאי. שני השדות אינם מדברים זה עם זה. **הצרכן הקריטי אינו הפותר אלא המודל:** `menu_foods.py` המיוצר הוא מה שהמודל רואה בעת ההרכבה, ואי אפשר לאלץ עליו `if by_weight`. הסתמכות על כך שכל צרכן — היום ובעתיד — יבדוק שדה אחד לפני שדה אחר היא בדיוק המשמעת ש-`CLAUDE.md` §5 החליט לא להסתמך עליה כשקבע ששער הבטיחות הוא `CHECK` ולא בדיקה בקוד. **והבעיה אינה נקודתית:** הכלל מ-30.08 שולח ל-`by_weight` את כל מי שהיחידה השלמה שלו היא ראש שלם — כרוב, כרובית, חציל — ולכן עקיפה על חסה בלבד הייתה נפגשת שוב בבלוק 8 בעשרות פריטים | `db/09_export_menu_foods.py` · `spec/05-food-db.md` §5.0 |
| 31.08.2026 | **תיקון לנימוק החלטת `by_weight` מאותו יום. ההחלטה עומדת; הנימוק צומצם.** התיקון מגן על צרכני `unit`, ואינו מגן על המודל | הנימוק המקורי אמר שהמודל קורא את `unit` ב-`menu_foods.py`. נמדד ב-3ד6: `generator.py` בונה את הפרומפט מ-`servings`. המודל לא קרא את השדה שתוקן ואינו קורא אותו היום. **החשיפה נותרה בשדה אחר** — 3807 מוצג למודל כ-`עלים = 20.0g`. שאלת ה-`servings` נפרדת ולא הוכרעה | `db/09_export_menu_foods.py` · `spike/generator.py` |
| 31.08.2026 | **קוטג' — נאצרים 3% ו-5% ולא 0.5%.** 493 (0.5%, תנובה) יוסר מ-`menu_eligible`, ובמקומו ייאצרו 500 (3%, תנובה) ו-494 (5%, תנובה). **הכתיבה בפועל היא בלוק נפרד בסשן הבא** | 0.5% אינו מוצר שנמצא על המדף, ובחירת רמת השומן שייכת למשתמש ולא למאגר. 493 נבחר באצירת מקטע החלבון ב-30.08 והגיע לדגימה בשתי ריצות הקנרית, כלומר הוא כבר משפיע על תפריטים בפועל. אצירת שתי רמות שומן של אותו מזון פותחת שאלה שלא הייתה קיימת: מה בוחר ביניהן — ראה `open-questions.md` #32 | `food_curation` · `spec/05-food-db.md` §5 |
| 31.08.2026 | **אין שער "האם המוצר עדיין מיוצר".** המאגר מציע חלופות והמשתמש בוחר | השוק המסחרי משתנה בקצב שהמאגר לא יכול לרדוף אחריו, ושער כזה היה הופך כל אצירה למשימת תחזוקה מתמשכת. **אינו מבטל את בלוק 7:** נכונות ערכי 2022 עבור מוצר שכן קיים על המדף נשארת לאימות מול תוויות עדכניות. ההחלטה מפרידה בין "המוצר קיים" — לא נבדק — לבין "הערכים נכונים" — כן נבדק | `spec/05-food-db.md` §6 · `spec/11-risks.md` |
| 31.08.2026 | **בלוק 3ה פוצל לשישה תת-בלוקים ולא רץ כבלוק אחד** | כל תת-בלוק בודד משתנה אחד — חיווט, סביבה, ריצה, מדידה, דיווח — ולכן כישלון בכל שלב הצביע על סיבה יחידה. שלוש עצירות אמיתיות אירעו בפועל: אין נקודת כניסה שקוראת את הייצוא, הפרשן הוא של פרויקט אחר, ומפתח ה-API נדחה. בבלוק אחד שלושתן היו מתערבבות. המחיר: כ-34 דקות קלוד קוד על פני סשן של כ-3 שעות, כי כל תפר דורש הכרעה בצ'אט | `CLAUDE.md` §8 · `measurements.md` |
| 31.08.2026 | **`.venv` ייעודי בשורש הפרויקט, מ-Python 3.12.10** | `python` ב-PATH הצביע ל-`.venv` של `yt_drone_fleet` — פרויקט אחר, 38 חבילות ובהן `opencv` ו-`pymavlink`, ובלי `requests`. כל סקריפטי `db/` רצו שם במקרה, כי אותו venv זר החזיק `psycopg`. התקנה לתוכו הייתה משנה סביבה של פרויקט שאינו זה. הסביבה החדשה הוכחה נייטרלית בשער רגרסיה בית-בית: מסלול הזרע 4,150 בתים וברירת המחדל של `run_generation.py` 6,376 בתים, זהים. **`psycopg` אינו ב-`requirements.txt`**, ולכן ה-venv החדש אינו יכול להריץ את הייצוא — נרשם לבלוק 3ז | `requirements.txt` · `.gitignore` |
| 31.08.2026 | **מפתח ה-API מצומצם ל-workspace יחיד, לא "All workspaces"** | הריצה הראשונה חזרה `HTTP 400: anthropic-workspace-id`. `generator.py` שולח שלוש כותרות בלבד — `x-api-key`, `anthropic-version`, `content-type` — ו-`grep -i workspace` על כל `spike/*.py` אינו מחזיר דבר, כלומר הכשל אינו מגיע מהקוד. מפתח מרובה-workspaces מחייב כותרת שהקוד אינו שולח. **הפתרון הוא בהגדרת המפתח ולא בשינוי קוד**, וזה מה שהפריד בין תקלת הרשאות לבין ממצא על המאגר | `spike/generator.py` · `.env` |
| 31.08.2026 | **`--show-menu` ו-`--trace` הם דיווח בלבד, מאחורי דגלים** | בלוק 3ה4 הפיק שורת ראיה אחת לכישלון של שלושה ניסיונות. התפריט לא הודפס כי `generate()` מחזירה `None`; פסקי שני הניסיונות הראשונים לא נשמרו; ו-`_info` של הפותר, ובו `kcal_err` ו-`protein_err`, נקלט ונזרק. כל השלושה כבר היו בזיכרון של `run_generation.py` — `vf` מקבלת את אובייקט התפריט של כל ניסיון ומשנה אותו במקום, בדיוק כפי ש-`show_menus.py` לוכד עם `store`. **`generate()` לא נגעה, אף סף לא זז, וברירת המחדל זהה בית-בית ל-HEAD.** בשימוש הראשון הדיווח החדש חשף שלוש שגיאות במקום אחת, את `UNKNOWN_FOOD` על 2659, ואת החזרות בין הארוחות | `spike/run_generation.py` |
| 01.09.2026 | **החלפת הקוטג' — 493 יוצא, 500 ו-494 נכנסים.** 493 (קוטג' 0.5%, תנובה) יוצא מ-`menu_eligible`; 500 (3%) ו-494 (5%) נאצרים במקומו | 0.5% אינו מוצר מדף. שלושת הפריטים זהים במבנה ההגשה — כף 40 ג' · כפית 12 ג' · גביע 250 ג' — ולכן שדות האצירה הועתקו כלשונם: `category` protein, `kosher` dairy, `allergens` {Milk}, `quality` 3, `supp` false, `prep` 0, `price` 1, `by_weight` false, `whole_only` false, `max_g` 250. ההחלפה היא ברמת השומן בלבד. 493 נשאר ב-`food_curation` עם `excluded_reason` NULL במכוון: ל-enum יש רק `ffq` ו-`non_protein_nitrogen`, ואף אחד מהם אינו מתאר "אינו מוצר מדף" — נרשם כשאלה [#35](open-questions.md). שש אסרציות בתוך הטרנזקציה (V1–V6), אף אחת לא ירתה | `food_curation` (ייצור) · `db/12_cottage_swap.sql` |
| 01.09.2026 | **קובץ `requirements.txt` אחד בשורש המאגר.** `db/requirements.txt` נמחק | הפיצול נועד למנוע ממי שרוצה רק להריץ את הספייק למשוך דרייבר Postgres. בפועל הוא גרם לכך ש-`.venv` של הפרויקט, שהותקן מקובץ השורש ב-3ה3, החזיק 8 חבילות ולא יכול היה להריץ את הייצוא. הנימוק הגן על מפתח שני שאינו קיים בפרויקט; המחיר שולם על ידי המפתח היחיד שכן. שער רגרסיה בית-בית עבר על שלושה מדדים — מסלול הזרע 4,150 ב', `run_generation.py` 6,376 ב', וייצוא המאגר 27 רשומות — כולם זהים לבסיסיהם | `requirements.txt` · `db/requirements.txt` (נמחק) |
| 01.09.2026 | **כל הרצת פייתון בפרויקט משתמשת בנתיב מוחלט ל-`.venv` של הפרויקט.** לעולם לא `python` חשוף | ה-PATH של סשן Claude Code נצרב מ-shell snapshot בפתיחת הסשן. ב-3ח הפרשן הפעיל היה ה-`.venv` של `yt_drone_fleet` — פרויקט אחר — בלי שום סימן חיצוני: `VIRTUAL_ENV` היה ריק, והנתיב לא הופיע ב-PATH הקבוע של המשתמש או של המכונה. הכלל מנטרל את התלות ב-PATH לחלוטין. בלוק 3י הוא ההוכחה: הוא נכתב עם נתיב מוחלט ורץ נכון למרות שה-PATH היה שגוי לכל אורכו | `CLAUDE.md` §6 |
| 02.09.2026 | **הבורר מדרג לפי המזון, לא לפי הרשומה.** `sort_key()` ב-`07_curation_candidates.py`, שני הענפים: `is_outlier` שוקע · המדד התזונתי (`fat_g` לשומן, `share` לחלבון) · ואז מקור, יחידת הגשה ו-`source_code` כשוברי שוויון בלבד. `complete` יצא מהמפתח ונשאר עמודה. `is_outlier` נשאר ראשון בכוונה | שלושת המפתחות העליונים היו מטא-נתונים על הרשומה ולא על המזון, ולכן 1347 טונה — `share` 0.958, ראשון בדגים — נחתך על `complete=false` בעוד 1132 סורימי נכנס (אבחון 30.08). ב-3ו הוחלפו 48 מ-119 בלי שינוי בספירות המשפחות: 1347 ראשון בדגים, 944 שני בעוף, 1132 יצא. `is_outlier` נשאר ראשון כי 8623 (`fat_g = 0`, `share` 0.985) יושב ב-`v_kcal_outliers` — בלי המפתח הזה היה ראשון בבקר עם נתון בלתי אפשרי; נמדד: 159 מ-163 | `db/07_curation_candidates.py` · `db/11_curation_sheet.py` |
| 02.09.2026 | **פס שני בבורר לפני בלוק 4 (3ו2), שלושה כללים שנמדדים בנפרד:** (1) מתכונים שוקעים מתחת לכל חומר גלם — חומר גלם לפני מנה; ingredient מול industry נשאר שובר שוויון · (2) תקרה לתת-משפחה (p4) בתוך משפחה, המספר נקבע בתוכנית 3ו2 אחרי מדידת פיזור ה-p4 · (3) דגל `raw?` לצורות לא מבושלות, כמו `dry?` — מסמן, לא מדיר | 3ו חשף ש-`share` מודד רזון ולא "מזון טוב": בעוף 10 מ-11 הנכנסים הודו, ו-963·964·967·969 הם חזה הודו בארבע שיטות בישול (p6 אחד); 805 חזה עוף, 30.1 ג' חלבון, במקום 19 מ-215. בקטניות 6 מ-8 הנכנסים תחליפי בשר, ויצאו 8 קטניות שלמות ובהן 8221 ו-9793 שכבר נאצרו. 9 מתכונים נכנסו, היו 0 — המפתח הישן הטביע אותם בכוונה וזה ירד יחד עם כל השאר. אותו דפוס שרשם #32 בספייק. **נדחה:** להשאיר את הבורר ולסנן ביד בבלוק 4 — הופך את #17 לחובה ומפיל את הערכת 1.5 ש'. **לא נבחר:** מדד `protein_g` במקום `share` — נשאר פתוח ב-#40 | `PROGRESS.md` · `open-questions.md` #39 #40 |
| 02.09.2026 | **פרמטרי הפס השני בבורר (3ו2ב).** (1) מתכון שוקע כמפתח ראשון, מעל `is_outlier`. (2) תקרת תת-קבוצה רכה: במשפחה עם מכסה ≥ 10 הפס הראשון לוקח עד 4 לכל p4 (4 הספרות הראשונות של `class_code`), ופס שני משלים לפי הדירוג — המכסה תמיד מתמלאת. `P4_CAP = 4`, `P4_CAP_MIN_QUOTA = 10`. (3) `raw?` מסמן ולא מדיר: "לא מבושל" בכל משפחה חוץ מאבקות חלבון, והמילה השלמה "טרי" בעוף, דגים ובקר | נמדד ב-3ו2א לפני ההכרעה: בשבע משפחות שומן יש p4 אחד בכל הבריכה, ולכן תקרה קשיחה הייתה מוחקת אותן; בביצים (מכסה 5) תקרה הייתה כופה 3 פריטים מתת-קבוצות שוליות; האשכול נמדד בעוף (11 מ-12), גבינות (5 מ-10) ודגים (5+4 מ-12). סמנים: "גולמי" תופס רק טחינה גולמית, "חי" כמילה שלמה 0, "אדום אדום" הוא מותג ושני פריטיו נושאים "טרי". **נדחה:** תקרה קשיחה; תקרה ⌈מכסה/3⌉ בכל המשפחות | `db/07_curation_candidates.py` · `db/11_curation_sheet.py` · `spec/05-food-db.md` §5.0.2, §5.5 |
| 02.09.2026 | **מתכון של רכיב יחיד לא מקבל חידוד בבורר.** ההצעה — מתכון עם רכיב אחד, אחרי התעלמות ממלח ומים, מדורג כחומר גלם — נמדדה ב-3ו2ג ונדחתה לעת עתה | 25 מתכונים כאלה במשפחות עם מכסה (8 בלי ההתעלמות ממלח 4497 ומים 4949). הסימולציה החליפה 3 מ-119 (825↔799, 1206↔1223, 1638↔1704), ספירות זהות, `raw?` 16→16. המחיר: join ל-`food_recipe_components` עם רשימת התעלמות קשיחה — מנגנון בשביל שלוש שורות. ההשערה שהצורות המבושלות בקובץ הן מתכונים הופרכה: רובן שורות `ingredient`. פתוח ב-#41 לבלוק 8; בלוק 4 רשאי להוסיף את השלושה ביד | `open-questions.md` #41 |
| 02.09.2026 | **קפוא-לא-מבושל הוא צורה גולמית.** `raw?` תופס גם "קפוא" בלי "מבושל" בשם, בעוף, דגים ובקר בלבד; "לא מבושל" מקבל שומר שמאלי `(?<!\w)` | 1213 ו-1273 (פילה קפוא, תנובה) ישבו בגיליון בלי סימון; במאגר 103 שמות עם "קפוא", 70 בלי "מבושל". השאלה שהדגל שואל היא "נאכל כמו שהוא?", ופילה קפוא לא. נמדד ב-3ו2ד: 16→18, בדיוק שניהם, אפס תזוזה בחברות. השומר לא הסיר אף שם היום — הגנה על המאגר הרחב מפני "מלא מבושל" | `db/07_curation_candidates.py` · `db/11_curation_sheet.py` |
| 03.09.2026 | **3ז פוצל לארבעה תת-בלוקים, ו-3ז4 לשלושה משלו** | הערות בקוד, שינוי בפלט של סקריפט, ושינוי במסד ובשכבת הסינון הם משתנים שונים. ריצה אחת שנכשלה לא הייתה אומרת מה נשבר. הפיצול השתלם בפועל: ארבע עצירות אמיתיות אירעו, כל אחת בתת-בלוק אחר, וכל אחת הצביעה על סיבה יחידה. אותו נימוק שפיצל את 3ה לשישה ב-31.08 | `CLAUDE.md` §8 · `measurements.md` |
| 03.09.2026 | **פריט (5) של 3ז — רוחב השורה ב-`11_curation_sheet.py` — נשאר כפי שהוא** | 3ז2 הוגדר "הערות בלבד, פלט זהה ביט-ביט". "Line width 207" אינו הערה בקוד אלא **פלט מחושב**: 11:537 מדפיס `len(HEAD_1)`. תיקונו פירושו שינוי הכותרת, ושינוי הכותרת משנה את חתימת הגיליון של בלוק 4. שני משתנים בבלוק אחד. נדחה ל-3ז' | `PROGRESS.md` |
| 03.09.2026 | **שורת `Food source` מודפסת תמיד ב-`run_generation.py`** | ההתניה הגנה על שער — 6,376 בתים לברירת המחדל — שאינו ניתן למדידה בלי מפתח ובלי רשת, ולכן אינו שער. `run_spike.py` מדפיס את אותה שורה ללא תנאי מאז ומעולם, וריצת ייצור שאינה אומרת מול איזה מאגר רצה אינה ניתנת למיקום בדיעבד. נמדד: שורה אחת נוספת ב-stdout (0 → 49 בתים), `stderr` זהה בית-בית, קוד יציאה זהה, מסלול ה-db זהה בית-בית | `spike/run_generation.py` |
| 03.09.2026 | **"`price` יורד מהמוצר" כולל את הסרת שאלת התקציב מהאשף** | השדה `budget` קיים בפרופיל רק כדי להזין את המסנן ב-`filters.py`, והצרכן היחיד שלו בכל הריפו נמדד: `spike/filters.py:25`. הפרומפט למודל מעולם לא ראה תקציב — `build_prompt()` מקבל את הפרופיל ואינו נוגע בו. שאלה שנשאלת ואינה משנה דבר היא שאלה שגובה מחיר מהמשתמש ואינה משלמת | `spec/03-functional.md` §2.5 · `spec/09-architecture.md` §4.2 |
| 03.09.2026 | **השדה `budget` נשאר ב-`spike/profiles.py` ואינו נקרא** | מחיקת שורת הייצור שלו מזיזה את זרם ה-PRNG לכל שדה שאחריה. נמדד ב-3ז4א: 95 מ-100 הפרופילים משתנים, וחמשת הזהים הם בדיוק העוינים 96–100 שנכתבו ביד. מחיקה הייתה מחליפה את מערך הבדיקה כולו ומאיינת כל השוואה לשער הרגרסיה | `spike/profiles.py` |
| 03.09.2026 | **שער הרגרסיה ל-3ז4 נמדד לפני ואחרי באותו יום, 40 ייצורים כל אחד, וההשוואה על 39 הפרופילים המשותפים.** הקריטריון: מעבר בניסיון ראשון לא נמוך מהבסיס, ואפס כישלון מלא. הפרה — עצירה לסקירה, לא ביטול אוטומטי | ה-92% מ-27.08 אינו בר-השוואה: המודל עשוי היה להשתנות מאז, `temperature` אינו נשלח כלל, והרעש על 40 דגימות הוא כמה נקודות. בסיס שנמדד באותו יום, על אותו קוד פרט לשינוי הנבדק, הוא ההשוואה היחידה שאומרת משהו. הקריטריון "לא נמוך" ולא "שווה" מאותה סיבה | `measurements.md` · `open-questions.md` #42 |
| 03.09.2026 | **כישלון #35 סווג כחשיפה ולא כרגרסיה, והקומיט בוצע** | #35 לא רץ בריצה שלפני כלל — הוא נחסם ב-`pool_health` עם 19 פריטים ו-`protein=1/4`. הסרת מסנן ה-`price` הגדילה את מאגרו והוא עבר את השער, כלומר הפרופיל נבדק בפעם הראשונה. המנגנון שהפיל אותו הוא [#20](open-questions.md) ו-[#31](open-questions.md) הידועים: `feasible()` אינו מכיר את רצפת השומן, ו-`run_generation.py` אינו מעביר `fat_floor` לפותר. עיכוב השינוי עד לפתרונם היה מערבב שני משתנים בבלוק אחד | `open-questions.md` #20, #31 |
| 03.09.2026 | **מיגרציות שהוחלו (06, 10, 12) אינן נערכות. 01 נערך כסכימה הנוכחית, ומיגרציה חדשה 13 מיישמת את השינוי על הייצור** | עריכת מיגרציה שכבר רצה משכתבת היסטוריה ואינה משנה דבר בייצור. 01 הוא לא היסטוריה אלא תיאור הסכימה הנוכחית, ולכן הוא כן נערך. **28 ערכי ה-`price` שנמחקו נשמרו כבלוק הערה בכותרת של 13** — שלושה בלבד שרדו בקבצים במעקב (500 ו-494 ב-12, 493 ב-`decisions.md`), ו-`spike/menu_foods.py` שהחזיק 27 מהם אינו בגיט | `db/13_drop_price.sql` · `open-questions.md` #44 |
| 03.09.2026 | **בנייה מחדש של `view` נעשית לפי האידיום של הפרויקט: `REVOKE ALL` מ-`anon` ומ-`authenticated` לפני ה-`GRANT`, ו-`security_invoker = on`** | החזרה הגנרלית של 13 נכשלה ב-V7 בדיוק על זה: ברירות המחדל של Supabase מעניקות ל-`anon` ול-`authenticated` **את כל שבע ההרשאות** על כל אובייקט חדש, ולכן `CREATE VIEW` חשוף מחזיר את ה-view **רחב יותר** ממה שהחליף — `anon` עם INSERT, UPDATE, DELETE ו-TRUNCATE על view שקודם נתן לו SELECT בלבד. ה-RLS על `food_curation` היה חוסם בפועל, אבל זו שכבה שנייה ולא כוונה. הזוג `REVOKE`/`GRANT` כבר קיים ב-`01:431-432` וב-`06:318-319` על כל שבעת ה-views | `db/13_drop_price.sql` · `CLAUDE.md` §5 |
| 03.09.2026 | **חתימת הגיליון של בלוק 4 מתעדכנת ל-`181d2174…`. וכלל: בסיס בכניסה לבלוק, לקובץ שאינו בגיט, הוא ריצה טרייה — לא קובץ שנשאר על הדיסק מבלוק קודם** | הציפייה `03e28125` התיישנה ברגע ש-3ז4ב נדחף: 11 מצטט קטע מ-`spike/filters.py` **עם מספרי שורות**, ושלוש השורות שנמחקו שם הזיזו 17 מספרים ב-3. תוכן זהה, 51,109 בתים, 375 שורות. שער שנשען על קובץ שנוצר לפני שינוי אחר מודד את השינוי האחר | `PROGRESS.md` · `CLAUDE.md` §4 |
| 03.09.2026 | **כל מיגרציה לייצור עוברת חזרה גנרלית מלאה עם `ROLLBACK` לפני ה-`COMMIT`. המריץ מאמת `BEGIN;`/`COMMIT;` בקובץ, גוזר אותם, מריץ את הגוף בטרנזקציה שלו, ודורש דגל מפורש** | קובץ שנושא `COMMIT;` בגופו, מורץ ב-psycopg בתוך "חזרה גנרלית", מקמט בפועל. ומריץ שמקמט כברירת מחדל נכשל לכיוון המסוכן: `--apply` כברירת מחדל פירושו שכל שגיאת הקלדה בדגל מוחקת עמודה בייצור. המריץ עוצר על דגל חסר, שגוי, כפול או בכתיב שונה. החזרה הגנרלית הצדיקה את עצמה בפעם הראשונה שהופעלה — היא תפסה את באג ההרשאות | `CLAUDE.md` §5 |
| 03.09.2026 | **דפוס עריכה לשינוי רוחב: סימולציה ב-scratch, ואז דרישת זהות ביט-ביט מהעריכה האמיתית** | 3ז4ב נעצר ב-S2 כי ספירה בפרומפט הניחה שכל 63 שורות הפריט ב-`foods.py` נושאות `price=N, ` — בפועל 29 בלבד, ו-34 נושאות `price=N)` בסוף. הסימולציה ב-`foods_sim.py` הוכיחה את כלל ההסרה המתוקן לפני שנגעו בקובץ, וההרצה החוזרת הפיקה `sha256 44031cc5…` זהה לה. שער שמאמת נגד תוצאה שחושבה מראש תופס שגיאת ניסוח לפני שהיא נכנסת לקובץ | `CLAUDE.md` §8 |
| 04.09.2026 | **`build_prompt` מאבדת את הפרמטר `profile` שאינו נקרא.** החתימה: `build_prompt(day_targets, meal_targets, meal_names, pool)` | נמדד ב-3ז'1: הגוף (`generator.py:63–89`) אינו מזכיר `profile`, אושר גם ב-AST, ואתר הקריאה יחיד (`:127`). הפרומפט למודל מעולם לא ראה את הפרופיל — הכרעת 03.09 על `budget` נשענה בדיוק על כך — והחתימה צריכה לומר זאת במקום להבטיח את ההפך. השער: טקסט הפרומפט לפרופיל #1 על מאגר הזרע זהה בית-בית לפני ואחרי, 3,272 בתים · `483b2f3c…` | `spike/generator.py` |
| 04.09.2026 | **שורת "Line width" בגיליון מדווחת את רוחב השורה הרחבה ביותר בגיליון המוגמר**, ולא `len(HEAD_1)`. סוגר את פריט (5) של 3ז שנדחה ב-03.09 | נמדד ב-3ז'1: `len(HEAD_1)` = 207, שורות הכותרת בפועל 209 (הזחה), עשר שורות נתונים 226 — הרוחב תלוי בתוכן ואינו קבוע, ולכן מקורו היחיד הוא הגיליון הבנוי. המימוש: מציין-מקום בשורה 537, והחלפה אחרי שה-`StringIO` הושלם ולפני הכתיבה וההדפסה. הגיליון של בלוק 4 משתנה בשורה אחת בדיוק (103), באותו גודל, וחתימתו החדשה נרשמת ב-`PROGRESS.md`; בלוק 4 ממילא פותח בריצה טרייה, הכרעת 03.09 | `db/11_curation_sheet.py` · `PROGRESS.md` |
| 04.09.2026 | **התיקייה `.claude/worktrees/block-2-curation-candidates-91ec21` נמחקת** | נמדד ב-3ז'1: אינה worktree — אין בה `.git` ואין רישום ב-`.git/worktrees/`, ולכן `git worktree prune` אינו נוגע בה (הרצה יבשה: ריק). תוכנה קובץ יחיד, `.claude/settings.local.json`, 127 בתים, שני דפוסי הרשאה מקומיים של Claude Code, blob שאינו בגיט — מצוטט ב-`measurements.md`. הענף שלה אוחד ל-`main` ב-fast-forward ונמחק (רפלוג). אין מה לשמר | `measurements.md` |
| 05.09.2026 | **#17 נסגר: מילוי מחדש אחרי דחייה — ידני מהדירוג המלא (חלופה ג), בלי שינוי קוד.** הגיליון לא שימש בבלוק 4: המועמדים נשלפו בשאילתות קריאה על המסד — בשומן כל המשפחה מעל הרצפה, בחלבון 6–7 הראשונים בכל תת-קבוצה | הבורר לוקח `rows[:quota]` ודחייה אינה מעלה את הבא בתור. מנגנון בקוד — רזרבות בגיליון או הרצה חוזרת — היה בלוק לפני בלוק 4, וההרצה החוזרת דרשה גם ערך `excluded_reason` שאינו קיים (#35). המספר שהמנגנון היה משרת נמדד רק אחרי הסקירה: 39 כן מ-178 בשומן, 141 מ-323 בחלבון — הרזרבה נצרכה ישירות מהמסד. **נדחה:** רזרבות מדורגות בגיליון; הרצה חוזרת אחרי דחיות | `work/2026-09-05-block-4-curation.md` |
| 05.09.2026 | **#18 נסגר: משפחת הסייטן היא 9851 לבדו.** 8834 (גלוטן חיטה יבש) נדחה כצורה | §5.0.2 — הצורה הנאכלת היא שנאצרת; מכסה 2 על מאגר שאחד מפריטיו יבש היא מכסה 1 בפועל. 9851 נושא חריג kcal ואריזה 500 בלבד — הבעלים פסק שהנתון תקין, והיחידה הולכת למסלול גרמים | `work/2026-09-05-block-4-curation.md` |
| 05.09.2026 | **הרחבה של הכרעת 30.08: מוצר תעשייתי מתובל או מעובד נאצר במסלול הצר אם הבעלים מביא תווית לסקירת האלרגנים בבלוק 6.** הוכרע על 802, 889, 910, 923 והוחל על 33 פריטים | 30.08 הוציאה את 4291 לבלוק 7 כי אין נתיב לאלרגנים — לא מהקובץ ולא מההורשה. התווית היא הנתיב, והבעלים מכיר את המוצרים ומסוגל להביאה. השער נשמר: בלי תווית — אין `allergens_reviewed_at`, ואין `menu_eligible` | `spec/05-food-db.md` §3 · `work/2026-09-05-block-4-curation.md` |
| 05.09.2026 | ↩ בוטלה 20.09.2026 · **שלושה פריטי שומן כשירים מ-3ד יוצאים מ-`menu_eligible`: 4279 זיתים ירוקים, 4387 חמאה, 4446 קנולה.** הכתיבה בבלוק 4ג נפרד, בדגם 3ח | הכרעת הבעלים בסקירה מחדש, במילותיו: "עדיפות לשומנים איכותיים יותר" · "עדיפות לשומן צמחי איכותי" · "שומן גרוע". משפחת הזיתים כולה נדחתה איתם. הצילום יזוז 27 → 24 ושומן 8 → 5 — מעל רצפת `pool_health()` (2). השורות יישבו בלי `excluded_reason` — מופע נוסף של #35 | `work/2026-09-05-block-4-curation.md` |
| 05.09.2026 | **מריץ המיגרציות נכנס לריפו: `db/_apply_migration.py` — נתיב ה-SQL כארגומנט, `--apply` כדגל מפורש, אותה סמנטיקה בדיוק של המריץ מ-3ז4ג** | CLAUDE.md §5 מחייב חזרה גנרלית לכל מיגרציה, אבל הכלי שמבצע אותה ישב ב-`_scratch`, קידד קשיח את נתיב ה-SQL, והועתק לכל מיגרציה — `apply_13.py` ב-3ז4ג, `apply_14.py` ב-4ג, שני שינויי נתיב. כלל בטיחות שהכלי שלו אינו בריפו הוא מופע של #38, ובלוקים 5–6 יכתבו כ-180 שורות במיגרציות. האימות: מסלול חיובי על קובץ no-op; מסלול שלילי על 14 — חייבת ליפול ב-V0 כפי שתוכנן, בחזרה גנרלית וגם ב-`--apply`, בלי לגעת בשורה | `db/_apply_migration.py` · `CLAUDE.md` §5 · `open-questions.md` #38 |
| 05.09.2026 | **המריץ: חזרה גנרלית היא ברירת המחדל, `--apply` הוא הדגל היחיד, `--rehearse` בוטל. מבטל את סעיף "דגל חסר עוצר" בהחלטה מ-03.09, ומתקן את "אותה סמנטיקה בדיוק" בשורת 4ה שמעל** | המריץ המקורי (`apply_13.py`, `apply_14.py`) דרש אחד משני דגלים ועצר על דגל חסר. הפרומפט ל-4ה הגדיר "`--apply` אופציונלי בלבד" בלי לדעת על `--rehearse`, וכך סתר את "כל דחייה של המקורי נשארת"; קלוד קוד הלך אחרי העיצוב המפורש ודיווח. נבחר להשאיר: הכיוון המסוכן — קומיט — עדיין דורש את הטוקן המדויק `--apply`, וכל כתיב אחר נדחה (8 מקרי דחייה נמדדו ב-4ה, exit 1); המסלול הבטוח אינו צריך דגל, ודגל אחד פשוט משניים. CLAUDE.md §5 — "דורש דגל מפורש ואינו מקמט כברירת מחדל" — נכון כפי שהוא. **נדחה:** להחזיר `--rehearse` כחובה — בלוק קוד נוסף בלי רווח בטיחות | `db/_apply_migration.py` · `decisions.md` 03.09 |
| 11.09.2026 | **תיוג בלוק 5 רץ לפי משפחות, לא לפי פריטים.** הצ'אט מציע ברירת מחדל למשפחת מקור (§5.5) ל-`max_g`, `prep`, ובחלבון `quality`; הבעלים מאשר או מתקן משפחה-משפחה; החריגים נדונים אחד-אחד - 18 פריטי הגרמים, 3 המותנים בסיבים, 2 הגולמיים, 6 האבקות. הקוד מציע כמו ב-3ד1 - כשרות, יחידה, `by_weight`, `whole_only`, `supp` - ואינו מציע `quality`: הכרעת 30.08 עומדת, ההצעה באה מהצ'אט | המדידה היחידה שיש: 3ד2, 8 פריטי שומן כולל מילוי שדות ב-24 דק', ~3 דק' לפריט. 180 × 3 = 9 שעות, עוד לפני `quality` ל-141 פריטי חלבון. בלוק 4 עצמו רץ לפי משפחות ונסגר | `PROGRESS.md` · `spec/05-food-db.md` §3 |
| 11.09.2026 | **בלוקים 5 ו-6 רצים לפי קטגוריה, לא לפי שדה: 5-שומן → 6-שומן → 5-חלבון → 6-חלבון.** המספור נשאר, ובכל תת-בלוק משתנה אחד. מיגרציה נפרדת לכל תת-בלוק; #46 נסגר בתת-בלוק משלו לפני הראשונה | השומן (39 כן, ~34 שורות חדשות לפי היומן, נספר ב-5ג) עובר את כל הצינור ראשון ומעלה את המאגר הכשיר 24 → ~58, כך ששער הדלתא נבחן על דלתא קטנה לפני ~130 שורות החלבון - הכתיבה הגדולה ביותר עד היום; 14 נגעה ב-3. אותה קנרית של 3ד ושל בלוק 4 | `PROGRESS.md` |
| 11.09.2026 | **שער הרגרסיה תלוי-המסד הופך משער זהות לשער דלתא.** לכל מיגרציה שנוגעת ב-`food_curation`: ספירת הרשומות בייצוא אחרי = לפני + N בדיוק; הרשומות שהיו קיימות זהות שדה-שדה לפי `source_code`; קבוצת החדשות = בדיוק קודי המיגרציה. מיגרציה שאינה נוגעת בכשירות - N = 0 ושני השערים זהים בית-בית. הצילום הקנוני לא משתנה | "ה-sha לא זז" אינו יכול לשרוד גידול מכוון, וגידול הוא כל מטרת בלוקים 5–8. זו לוגיקת `_pre` שבתוך המיגרציה, מוחלת על התוצר שמחוצה לה. ההשוואה לפי `source_code` ולא לפי `id` המיקומי: #36 נעקף, לא נסגר. בסיס ה"לפני" הוא ריצה טרייה בפתיחת אותה מיגרציה - כלל 03.09 | `measurements.md` · `open-questions.md` #36 |
| 11.09.2026 | **זמן הבעלים נמדד מבלוק 5.** כל מקטע הכרעה בצ'אט נפתח ונסגר בשעת שעון קיר שהבעלים מקליד; הזוג נרשם ביומן הבלוק | בבלוק 4 זמן הבעלים לא נמדד. ההערכות שמחליקות הן של שיפוט אנושי (כיול 29.08), ובלי המספר הזה אין כיול לבלוקים 5–8 | `work/2026-09-11-block-5-tagging.md` |
| 11.09.2026 | **המריץ מדפיס שורת סיכום קריאה כשהמסד דוחה מיגרציה, ויוצא 1.** `db/_apply_migration.py` תופס `psycopg.Error` בלבד - אסרציה (`RAISE EXCEPTION`), אילוץ `CHECK`, וכל דחייה אחרת של המסד - ומדפיס אחרי הרולבק שורה אחת: ✘, הודעת המסד, ואז rolled back, nothing committed. חריגה שאינה של המסד ממשיכה עם traceback. `mask_dsn` שלא היה בשימוש הוסר | כישלון אסרציה הוא תוצאה צפויה של מיגרציה, וב-5ה, 5ז ובבלוק 6 המיגרציות מלאות אסרציות; traceback בלבד קובר את ההודעה שהאסרציה נכתבה כדי לומר. באג במריץ עצמו אינו תוצאה צפויה, ושורה ידידותית הייתה מסתירה אותו. סוגר את #46 | `db/_apply_migration.py` · `open-questions.md` #46 |
| 11.09.2026 | **שורת הסיכום של המריץ פותחת ב-STOP:, לא ב-✘.** תוכן השורה כפי שהוכרע ב-#46; רק הסימן הוחלף | ✘ בקונסולת cp1252 עם הפניה לקובץ, בלי PYTHONIOENCODING, זורק UnicodeEncodeError בתוך ה-except. הרולבק כבר קרה ו-exit 1 נשמר, אבל השורה הופכת ל-traceback בדיוק בתרחיש ש-#46 נועד לו - לוג. שוחזר ותוקן ב-5ב'. STOP: היא הקידומת שהמריץ כבר משתמש בה לדחיות argv, ו-`_env.py` משתמש ב-ASCII בלבד מאותה סיבה | `db/_apply_migration.py` |
| 11.09.2026 | **ב"רק מהבית", כשהמזווה לא עובר את בדיקת המספיקות, ההודעה נוקבת גם במה חסר.** `pool_health()` כבר מחזירה איזו קטגוריה מתחת לרצפה; ההודעה מציגה אותה - "חסר מקור חלבון" - לצד שתי האפשרויות הקיימות: להוסיף למזווה או לחזור ל"העדפה". המלצה ברמת משפחה (§5.5) - תוספת אפשרית, לא בהיקף הזה | הכרעת 10.09 קבעה את ההודעה בלי לומר מה חסר; המידע כבר מחושב, ולהציג אותו זול. הבעלים, 11.09: "תגיד לו איזו משפחה של מזונות מומלץ להוסיף" | `spec/03-functional.md` §10.1 |
| 11.09.2026 | **אגוזים, בוטנים וגרעינים נאצרים טבעיים, לא קלויים.** 1872 גרעיני דלעת קלויים יוצא מרשימת "כן" של בלוק 4; 1870, אותו גרעין לא קלוי, נשאר. חמאות אגוזים אינן בכלל הזה | הבעלים, 11.09: חום הקלייה מחמצן את השומן הבלתי-רווי. הכלל נוסח ב-5ד על 8263 והוחל על כל הרשימה | `spec/05-food-db.md` §5.0.2 · `work/2026-09-11-block-5-tagging.md` |
| 11.09.2026 | **`max_g` לשומן נקבע לפי משפחה, עם חריגים לפריט:** שמנים 40 · טחינה ושומשום 45 · גרעינים 45 · אגוזים שלמים 45 · חמאות אגוזים 48 · אבוקדו 150; חריגים: ברזיל וחמאת ברזיל 15, פשתן 30, צ'יה 30, פרג 15, צנוברים 30 | ברירות המחדל הן ערכי `MAX_G` של הספייק והשורות הקיימות מ-3ד; החריגים הם שיפוט תזונתי של הבעלים - סלניום בברזיל, זרעים כתוספת ולא כמנה | `work/2026-09-11-block-5-tagging.md` |
| 11.09.2026 | **אלרגנים מתויגים לפי זהות המרכיב בלבד. "עלול להכיל" אינו מתויג.** חל על המסלול הצר כולו - פריט בעל רכיב יחיד, כולל חמאות אגוזים ממותגות | הבעלים, 11.09: "אם המוצר לא נחשב אלרגני אז הוא לא אלרגני". אזהרת מפעל היא עניין של תווית, לא של זהות המזון; המשמעות למשתמש נכתבת ב-`07-safety.md` | `spec/05-food-db.md` סעיף האלרגנים · `spec/07-safety.md` |
| 11.09.2026 | **אלרגני השומן לפי משפחה:** שמן זית, שמן אבוקדו, אבוקדו, וגרעינים (חמניות כולל חמאת 1874, דלעת, פשתן, צ'יה, פרג) - ריק · שמני שקדים ואגוזי מלך, אגוזים וחמאותיהם - Tree nuts · שמן שומשום, טחינה וגרעיני שומשום - Sesame · שמן בוטנים, בוטנים וחמאות בוטנים - Peanuts. "Peanuts" נכנס לאוצר המסד לראשונה, מהספייק. שמני אגוזים ובוטנים מסומנים אף ששמן מזוקק לרוב נסבל - הזיקוק אינו ידוע | עומק נטול אלרגנים אחרי 6ש: 11 מ-38, כפי שבלוק 4 ספר. 1826 חמאת ברזיל נשארת לא נסקרת עד תווית (#13) | `work/2026-09-11-block-6-allergens.md` |
| 11.09.2026 | **תיוג החלבון לפי משפחה, עם חריגים לפריט.** `quality` לפי §5.3, ומוצרי "מן הצומח" על בסיס סויה - 2. `max_g` 175 לבשר, עוף ודג; נקניק, מעושן ושמנת 100; קופסה = תכולתה; יחידה שלמה מעל התקרה - התקרה היא היחידה, אחרת `whole_only` חוסם. `prep` 0 למוכן, 1 למבושל שרק מחממים, 2 לדורש בישול | ברירות המחדל מהשורות הקיימות של 3ד ו-3ח; החריגים שיפוט הבעלים. הערכים לפריט ב-`work/2026-09-11-block-5-tagging.md` | `work/2026-09-11-block-5-tagging.md` |
| 11.09.2026 | **`prep` מוגדר במפורש: 0 מוכן לאכילה · 1 חימום או הכנה מהירה · 2 דורש בישול.** נכתב ב-`spec/05-food-db.md` §3 | עד היום ההגדרה ישבה רק בספייק (`spike/foods.py`), וה-CHECK בסכימה קיים בלי משמעות כתובה. 130 ערכים נכתבים ב-5ז לפיה | `spec/05-food-db.md` §3 |
| 11.09.2026 | **זמן הבעלים נמדד כחסם עליון: מחותמת ה-author של קומיט הסגירה הקודם עד שעת הפתיחה של הבלוק הבא. מחליף את "הבעלים מקליד שעה" מ-11.09** | השעה לא הוקלדה בשלושת מקטעי ההכרעה של היום (5ד, 6ש-ב, 5ו-ב). החסם העליון כולל את זמן הצ'אט, אבל הוא מדידה; ניחוש אינו | `work/2026-09-11-block-5-tagging.md` |
| 11.09.2026 | **הערכות מ-6ח: תת-בלוק סוכן מוגדר מראש 5–15 דק'; מקטע הכרעה של הבעלים לפי משפחות 15–30 דק' לכ-150 פריטים** | כיול בלוק 5 (`measurements.md`, 5ח): שבעה תת-בלוקי סוכן רצו 3–43 דק' ברוטו מול הערכות של 5–60, ורק המגלה חרג; שני מקטעי הבעלים - ~15 ו-≤14 דק' מול שעות. בלוק שמגלה משהו עדיין חורג, וזה לא משתנה | `measurements.md` |
| 12.09.2026 | **אלרגני החלבון לפי משפחה:** ביצים Egg · חלב, גבינות ואבקות מי גבינה Milk, גם מחלב עזים, כבשים ובופאלו · דגים Fish · סויה על צורותיה Soy · סייטן Gluten · עוף, הודו, בקר, עגל, כבש, קטניות וחלבון אפונה ריק | זהות המרכיב לפי הכלל מ-11.09. חלבון החלב זהה בכל היונקים, ולכן רגישות לחלב פרה חלה גם על עזים וכבשים | `work/2026-09-11-block-6-allergens.md` |
| 12.09.2026 | **מתכון מתויג ישירות לפי רשימת רכיביו ב-`food_recipe_components`**, בלי לפתוח שורות אצירה לרכיבים. 8 המתכונים של בלוק 4 הופכים לכשירים ב-6ח-ג עם התיוג הזה | 13 מ-14 רכיבי השמונה הם בלי שורת אצירה, וההורשה של §3.1 עיוורת להם. הרכב המתכון הוא נתון של המסד, לא הנחה, והסינון קורא את שורת המתכון. פתיחת שורות למלח, למים ולבצל היא משתנה שני בבלוק. המחיר: `v_recipe_unreviewed_components` אינו 0 אחרי ההפיכה - #49 | `spec/05-food-db.md` §3.1 · `open-questions.md` #49 |
| 12.09.2026 | **Lupin לא נכנס לאוצר בבלוק 6; 1630 תורמוס נשאר לא כשיר ולא מתויג** | האוצר הוא אוצר האשף: תווית שהמשתמש לא יכול לסמן בשלב 7 לא חוסמת בסינון. אלרגן תשיעי הוא שינוי מוצר - אשף, סינון, אפיון - לא שינוי נתונים. ריק אינו אפשרות, כי ריק פירושו נבדק ונקי | `open-questions.md` #48 · `spec/05-food-db.md` §4 |
| 12.09.2026 | **תיקון לבלוק 4: 561 גבינה מותכת עוברת לתנאי תווית; 680 בקר טחון עם חלבון צמחי יוצא** | 561: "סלמון" הוא אחד הטעמים שבשם - דג כרכיב, לא "עלול להכיל", כמו 539 ו-556. 680: המקור לא נוקב במקור החלבון הצמחי ואין תווית להביא - לא מסלול זהות ולא מסלול תווית. נרשם גם ביומן בלוק 4 | `work/2026-09-05-block-4-curation.md` · `db/block6_protein_label_codes.txt` |
| 12.09.2026 | ↩ בוטלה 12.09.2026 · **הערכות מבלוק 7: תת-בלוק תיעוד רב-קבצים 30–90 דק' לסוכן; מיגרציה, גיליון ומדידה 5–15; הבעלים לפי משפחות 15–30 לכ-150 פריטים, נמדד כחסם עליון** | שורת 5–15 מ-11.09 נגזרה מתת-בלוקי מיגרציה וגיליון (5ג 5:12 · 5ה 8:26 נטו · 5ז 6:53 · 6ש-ג 5:49 · 6ח-ג 12:10) ולא תפסה תיעוד: 5א 43 ברוטו/21 נטו, 6ח-א 96:40 על 7 קבצים ו-13 עוגנים. הערכה שנכתבה נשארת; מבלוק 7 מפצלים לפי סוג העבודה | `measurements.md` |
| 12.09.2026 | **בלוק 8 נפתח לפני בלוק 7, בפחמימה ואז בירק - 8פ, 8י. כל קטגוריה עוברת את כל הצינור לפני הבאה - מדידת משפחות, מועמדים בשאילתה, סקירה בצ'אט, תיוג, אלרגנים - כמו 5-שומן → 6-שומן. משפחות הפחמימה והירק אינן ב-§5.5 ונגזרות בפתיחת כל קטגוריה מהמסד, כהכרעה מתועדת ל-§5.5 (כלל 29.08). #47 מוכרע ב-8פ-ב על מדידת 8פ-א; #40 · #41 · #48 · #49 נדחים למקטע החלבון והשומן של 8; #45 מחכה עם בלוק 7** | בלוק 7 מוסיף חלבון בלבד (32 תנאי תווית) ושומן אחד (1826), וחלבון 105 ושומן 37 רחוקים מרצפות `pool_health()` (4, 2) - הוא אינו מזיז את הצוואר הצר ולא את `v_pool_depth`. פחמימה וירק יושבות על הרצפה (4 מול 3), ופחמימה נטולת אלרגנים 2 מתחתיה - פרופיל עם אלרגיה לגלוטן אינו מקבל תפריט היום, ובלוק 7 אינו מתקן זאת. שער היציאה של שלב ב' (בלוק 9) נמדד על הצוואר הצר, ולכן 9 אינו עובר כל עוד פחמימה וירק על הרצפה; 7 רץ בכל שלב לפניו, ואיסוף התוויות הוא עבודת בעלים מחוץ למחשב. פחמימה ראשונה כי היא הצרה יותר. 8פ אינו נוגע ב-`07_curation_candidates.py`, שאין לו ענף פחמימה - המועמדים נמדדים בשאילתה, קריאה בלבד - ולכן #40 ו-#41, שעניינם ענף החלבון של הבורר, אינם על דרכו | `PROGRESS.md` · `open-questions.md` #40 · #41 · #45 · #47 · #48 · #49 · `work/2026-09-12-block-8-carb.md` |
| 12.09.2026 | ↩ בוטלה 13.09.2026 · **"תת-בלוק תיעוד רב-קבצים 30–90 דק'" מהכרעת 12.09 שמעל ↩ בוטלה. ההערכה לכל תת-בלוק סוכן, גם תיעוד: 5–15 דק'. תת-בלוק שדורך על קרקע חדשה - עוגנים חדשים, שאילתות חדשות, מסלולי shell חדשים - מסומן כך בשורת PROGRESS לפני הריצה; פיצול לפי סוג עבודה נכתב רק אם יחרוג פעם שנייה. הבעלים: 15–30 דק' לכ-150 פריטים, חסם עליון - ללא שינוי** | "תיעוד 30–90" נבנה על מדידה אחת, 6ח-א, ו-6ח-ד - תיעוד סגירה - הפיל אותו ב-4:30 (מ-16:01:14 עד חותמת `2b3ca2c`). הפיצול "בונה כלי חדש מול מריץ דגם קיים" אינו מחזיק ברשומה גם הוא: 6ש-א בנה כלי חדש עם סוויטה של שש ריצות ב-3:47, ו-5א רץ ~21 נטו מול הערכה 25–35. מה שהרשומה מראה: 11 מ-12 תת-בלוקי הסוכן של בלוקים 5–6 שנמדדו רצו 3:20–~21 דק' נטו; האחד שחרג, 6ח-א ×10, נפל על שלוש מלכודות - CRLF, heredoc, "08" אוקטלי - וכל אחת מהן היא כלל מאז | `measurements.md` · `PROGRESS.md` · `CLAUDE.md` §8 |
| 12.09.2026 | **#47, חלק הפחמימה: היעד הוא כיסוי המצרכים הביתיים, לא מספר - פחמימה כשירה ≥ 30, נטולת גלוטן ≥ 10, ≥ 2 בכל משפחת מצרך. ירק מוכרע ב-8י באותו עיקרון; מדידת מאגר מסונן ב-40 הייצורים עוברת לפתיחת בלוק 9, עם #42** | מזווה של בית רגיל עובר את רצפת 3 רק אם לחם, פיתה, אורז, פסטה ותפוח אדמה קיימים במאגר כדי שאפשר יהיה לסמן אותם; המספר נגזר מהכיסוי. היום 4 כשירות ו-2 נטולות גלוטן - אורז ותפוח אדמה - ופרופיל גלוטן אינו מקבל תפריט. מועמדים נטולי גלוטן נראים במדידת 8פ-א - אורז, אטריות אורז, כוסמת, תפוח אדמה, בטטה, תירס, לחם ללא גלוטן - והסקירה תספור | `open-questions.md` #47 · `spec/05-food-db.md` §5.5 · `work/2026-09-12-block-8-carb.md` |
| 12.09.2026 | **משפחות הפחמימה ל-§5.5 - שמונה עם מכסה, שש במכסה 0 - עם רצפה `carb_g ≥ 15` וגם `carb_g×4/kcal ≥ 0.5`, חריגה `carb_g ≥ 10` לדגני בוקר (30). מכסות: לחם 15 · לחמים שטוחים 3 · מצות ופריכיות 6 · דגנים, אורז ופסטה 18 · דגני בוקר 6 · תפוחי אדמה 8 · בטטה 3 · תירס 3 = 62 מועמדים** | המשפחות נגזרו ממדידת 8פ-א (קבוצות p2/p4 ברף A); ה-p2 בלעדי, ולכן שורות מכסה 0 הן הכרעה כתובה ולא מלכודת סדר. חלק 0.5 מפיל צ'יפס ופיצה ומשאיר את 8348 (0.54). 62 ולא 44: הבעלים בחר להגדיל היכן שיש עומק - לחם, דגנים, תפוחי אדמה, פריכיות, דגני בוקר - ולא 100 שטוח, כי בטטה, תירס ולחמים שטוחים כבר מוצו והתוספת הייתה גרסאות של אותו דבר. מה שלא מוצג בסקירה נשאר זמין לסבב שני. הגיליון: `db/20_carb_candidates.py`, מעתיק את מנגנוני 07 בלי לגעת בו | `spec/05-food-db.md` §5.5 · `work/2026-09-12-block-8-carb.md` · `db/20_carb_candidates.py` |
| 12.09.2026 | **כלל הצורה היבשה (§5.0.2) אינו חל על מזון שנאכל יבש - משפחות 28 ו-30; `dry?` שם צפוי ואינו מדיר. הדירוג בפחמימה: `(outlier, -carb_share, src_rank, servings_gate, source_code)`, מתכונים מתחת לכל חומר גלם, עד 4 ל-p4 בפס הראשון ב-26 וב-29** | מצה, קרקר, פריכית וקורנפלקס נאכלים כפי שהם - אין צורה "מוכנה" שלהם; §5.0.2 נכתב על יבש מול מבושל. חלק הפחמימה מהאנרגיה מדרג את הרזה מתוספות ראשון - אורז מבושל לפני "אורז עם בצל בשמן" (25 מתכונים ב-5623) - ואינו המדד השנוי במחלוקת של #40, שעניינו רזון החלבון. סיבים ריקים (5 ב-51, 12 ב-56 במדידת 8פ-א) מסומנים `fiber?`; #13 מחייב תווית לפני כשירות | `spec/05-food-db.md` §5.0.2 · §5.5 |
| 12.09.2026 | **תיקון להכרעת הדירוג בפחמימה (השורה שמעל): צורה יבשה היא פס שלישי. במשפחות המבושלות - 26 · 27 · 29 · 31 · 32 · 33 - פריט עם לחות מתחת ל-20 (NULL אינו יבש, כמו ב-07) יורד מתחת לכל הלחים; הפס הראשון עם תקרת ה-p4 והפס השני רצים על הלחים בלבד, והיבשים נלקחים לפי דירוג רק אם המכסה לא התמלאה מהם. ב-28 וב-30 אין פס יבש. המפתח: `(dry, recipe, outlier, -carb_share, src_rank, servings_gate, source_code)`. `dry?` ו-`raw?` נשארים סימון ואינם מדירים. שם משפחה 27 מתוקן ל"לחם אחיד, קובנה וטורטיות"** | הגיליון הראשון (8פ-ג, `65ad7d3`) יצא כמנגנון: 29 - 11 מ-18 יבשים (קורנפלור, קמח טפיוקה, אבקת פודינג, פסטה לא מבושלת), 26 - 3, 31 - 2; אורז לבן 2721, הכשיר היום, נדחק החוצה, ואורז מלא, בורגול, קוסקוס, קינואה וכוסמת לא הופיעו. שלוש סיבות: חלק האנרגיה של עמילן טהור הוא 1.0; `dry?` לפי 07 דורש "יבש" בשם, ולכן קורנפלור בלחות 13 אינו מסומן; תקרת 4 ל-p4 מסרה את הפס הראשון של 5620 לארבע אבקות. אותו דפוס של 3ו: המדד ממלא את המשפחה בצורה המרוכזת. התיקון הראשון - רכיב `dry` במפתח בלבד - נעצר ב-S5: תקרת ה-p4 חצתה את פס היובש ולקחה שלוש פסטות לא מבושלות (5610, לחות 7–10) לפס הראשון של 29 לפני 77 לחים שדולגו בגלל התקרה. "שוקע" חייב להיות גבול, לא רק סדר. הפתרון הוא פס, כמו למתכונים ב-3ו2, ולא הדרה - §5.0.2 מכריע בסימון. p2 = 52 מחזיק לחם אחיד (5240 · 5241), קובנה (5242), טורטיות (5221) ומצה בריי (5230), לא לחמים שטוחים | `db/20_carb_candidates.py` · `spec/05-food-db.md` §5.5 · `measurements.md` |
| 13.09.2026 | **גיליון התיוג (15) מקבל את משפחות הפחמימה מ-20: קוד בלי משפחה ב-07 נשאל ב-20, קבוצת התירס של 20 מתמלאת בתוך 15, והמצב הקומפקטי רץ על שתי רשימות המשפחות. ברירות מחדל לפי משפחה: כשרות פרווה לכל שמונה (26–33); טבעוני כן ל-27 · 28 · 29 · 31 · 32 · 33, ריק ל-26 לחם ול-30 דגני בוקר - ביצה, חלב ודבש מוכרעים פריט-פריט. היחידה מוצעת מ-`choose_unit()` בעמודת `unit`; כף היא הצעה פסולה לפחמימה (הכרעת 30.08) ומוחלפת ב-8פ-ו. `max_g` ריק בכוונה ומוכרע ב-8פ-ו** | הגיליון של 8פ-ה יצא ריק: המצב הקומפקטי של 15 מדפיס לפי `FAMILIES` של 07, ולפחמימה אין משפחה שם. החלופה - להכריע על 40 פריטים בלי גיליון - מזיזה את העבודה לשאילתות חקירה, ומיגרציית התיוג בדגם 5ה נבנית מהגיליון. השער: גיליונות השומן והחלבון לפני ואחרי הנגיעה זהים בית-בית - הראיה שהנגיעה לא שינתה את מה שכבר עבד. 07 ו-20 לא נוגעים; `CORN_CODES` של 20 מתמלא רק ב-`main()`, ולכן 15 מריץ את `CORN_SQL` בעצמו | `db/15_tagging_sheet.py` · `db/20_carb_candidates.py` · `measurements.md` |
| 13.09.2026 | **תיוג הפחמימה - הקונבנציה נלקחת מארבע הכשירות של 3ד: דגן מבושל ותפוח אדמה `prep` 2, לחם ודגני בוקר `prep` 0; פרוסה, יחידה שלמה וכוס לפי כלל 30.08, ואין יחידה סבירה - `by_weight`. תקרות: פרוסות 3 × הפרוסה; יחידות שלמות 1–8 לפי משקל היחידה; דגנים מבושלים 240 כמו אורז ופסטה; תפוחי אדמה 242; בטטה 248 (2 × 124); דגני בוקר 60, קוואקר 80; תירס 240 בכוס. כשרות פרווה ל-40; טבעוני ל-39, לא לגרנולה 2849 (דבש); `quality` ריק; `supp` false. מקבילה ב-`MAX_G` של הספייק גוברת לפריט שאינו `whole_only`; לפריט `whole_only` כלל הכפולה השלמה גובר (תקדים אבוקדו 150). שיפוט יחידה אחד ל-09: 3962 תירס קפוא - כוס 165, לא "יחידה בינונית 100"** | שבעה פריטים בלי יחידה סבירה - 1932 בגט (פרוסה 6 ג' ויחידה 360), 9643 פתיתים (כף בלבד), 8552 פסטה ו-3456 · 3464 תפוחי אדמה ("מנה" בלבד), 9620 ברנפלקס ("מנה" בלבד), 2849 גרנולה (כוס 140 ג' = 595 קק"ל) - `by_weight` לפי כלל 30.08. הצ'אט מדד את שורות ההגשה במסד, קריאה בלבד. ההכרעות פריט-פריט ב-`work/2026-09-12-block-8-carb.md`. תצפית שנפתחה כ-#50: 8348 ו-3459 נושאים תקרה שאינה כפולה שלמה של היחידה | `work/2026-09-12-block-8-carb.md` · `db/20_tag_carb.sql` · `open-questions.md` #50 |
| 13.09.2026 | **חזרה גנרלית שנופלת על שגיאת תחביר בקובץ המיגרציה שנכתב באותו צעד - תיקון אחד, ריצה חוזרת, דיווח; חזרה גנרלית שנופלת על אסרציה או על מצב המסד - עצירה. שער הקומיט בודק את הריצה החוזרת ואת ההחלה, לא את הריצה שנפלה על תחביר** | ב-8פ-ז החזרה הגנרלית הראשונה יצאה 1 על `both`, מילה שמורה ב-PL/pgSQL; שום דבר לא נכתב, הקובץ תוקן ל-`wo_bad` (השם של 18) ורץ נקי. החזרה הגנרלית קיימת בדיוק כדי לתפוס את זה לפני `--apply`, ולכן זו השלמת כתיבת הקובץ ולא כשל שער. הגבול: תחביר מול אסרציה - אסרציה שנופלת אומרת שהמסד או ההנחות אינם מה שחשבנו, וזו הכרעה של הבעלים | `db/_apply_migration.py` · `measurements.md` |
| 13.09.2026 | **אלרגני הפחמימה - לפי זהות המרכיב, בלי "עלול להכיל": גלוטן לחיטה, שיפון, שעורה, בורגול, קוסקוס ולשיבולת שועל לפי התקן; מתכון לפי רכיביו - 10127 קוסקוס גלוטן, 2773 קינואה נקי. מוצר שהשם נוקב במותג מתויג לפי הרכב המוצר הזה: 2831 קורנפלקס קלוגס - גלוטן (תמצית לתת שעורה היא רכיב), 2838 ברנפלקס תלמה - גלוטן (סובין חיטה). 2849 גרנולה גנרית - גלוטן ואגוזי עץ, קריאה שמרנית. 24 גלוטן · 1 גלוטן ואגוזי עץ · 15 נקיים. שומשום, סויה וחלב בלחמים תעשייתיים אינם בזהות ואינם מתויגים - זה מה שבלוק 7 יוסיף מתוויות** | האוצר: Egg · Fish · Gluten · Milk · Peanuts · Sesame · Soy · Tree nuts (הכרעת 11.09). ההבחנה בין "מתויג בזהות" ל"עלול להכיל" חלה גם על מותג: רכיב ידוע של המוצר הנקוב בשם הוא זהות, טופינג אפשרי אינו. אחרי ההפיכה: 44 פחמימות כשירות, 17 נטולות אלרגנים - היעד (#47) היה 30 ו-10 | `work/2026-09-12-block-8-carb.md` · `db/21_flip_carb.sql` · `spec/05-food-db.md` §3 |
| 13.09.2026 | **שלושת חריגי ה-kcal בפחמימה - 2064 · 2838 · 8342 - הופכים לכשירים עם השאר. הכלל למקרה: מוצר "קל" עתיר סיבים שהיצרן מצהיר עליו קלוריות בלי לספור סיבים, בעוד `v_kcal_outliers` גוזר עם 2 קק"ל לגרם סיבים - הפער הוא בדיוק 2 × `fiber_g`, ובגודל מנה (פרוסה 30 ג', כוס דגנים 30 ג') הוא 7–22 קק"ל. כשיר; הדגל שוקע בדירוג ואינו מדיר (02.09). ומכאן כלל לתבנית: דגל K בגיליון התיוג מוכרע בתת-בלוק התיוג, לא מתגלה בשער ההפיכה** | §7 אומר שפריט ב-`v_kcal_outliers` אינו כשיר עד שהוכרע - זו ההכרעה. המדידה: 2064 מוצהר 150.0 מול Atwater 4/9/4 150.2 - היצרן נאמן לנוסחה בלי סיבים, וה-view מוסיף 2 × 11.5 = 23.0; 2838 286.3 + 2 × 23.8; 8342 170.7 + 2 × 11.0. הגדרת ה-view אינה גוזרת קלוריות בעצמה - היא משווה את `foods.kcal` ל-`foods.kcal_source` ומסמנת סטייה יחסית מעל 12% שגם עולה על 5 קק"ל - והנוסחה שמייצרת את `kcal` יושבת ב-`db/05_derive_kcal.sql`: `P*4 + F*9 + C*4 + fiber*2 + ethanol*7`, כלומר כן עם 2 קק"ל לגרם סיבים, והפער בשלושת החריגים נמדד 2.000 לגרם סיבים בדיוק. החשד שה-view מנסח - פוליאולים או כוהל - אינו המקרה, והמנגנון הזה אינו בטבלת §7. תקדים: 9440 צ'יה ו-9851 סייטן כשירים עם אותו דגל מ-11.09. שלושת הדגלים K עמדו בגיליון של 8פ-ה2 ועברו את 8פ-ו ו-8פ-ח בלי הכרעה - הצ'אט לא קרא את עמודת הדגלים; S4 של 8פ-ט תפס | `spec/05-food-db.md` §7 · `work/2026-09-12-block-8-carb.md` · `db/21_flip_carb.sql` |
| 13.09.2026 | **שיפוט יחידה ל-3962 תירס גרעינים קפוא: כוס 165 ג' ב-`UNIT_BY_JUDGEMENT` של 09, במקום `יחידה בינונית` 100 ג' ש-`choose_unit()` בוחר. `whole_only` נשאר false במסד - השיפוט הוא בייצוא, לא באצירה** | שורת "יחידה" של תירס במקור היא קלח, לא מנה של גרעינים; "יחידה בינונית אחת" של גרעינים קפואים אינה מנה שאדם מדמיין - אותו מבחן של כלל 30.08 שהעלה את פרוסת העגבנייה ואת ראש הכרוב. 3963 תירס משומר קיבל כוס מ-`choose_unit()` בעצמו, ולכן השיפוט מיישר בין שני התירסים. שער: הייצוא משתנה ברשומה אחת, בשדה היחידה בלבד - 0 נוספו, 0 נעלמו, 1 שונתה | `db/09_export_menu_foods.py` · `spec/05-food-db.md` §5.0 · `open-questions.md` #21 |
| 13.09.2026 | **משפחות הירק ל-§5.5 - תשע עם מכסה, שבע במכסה 0 - עם מבחן כניסה שהוא תקרה ולא רצפה: `carb_g < 15` וגם `kcal ≤ 80` וגם `fat_g ≤ 2`. מכסות: ירקות טריים 14 · עלים ירוקים 8 · ברוקולי 3 · ירקות מבושלים 14 · לקטים 5 · גזר 3 · דלעת 3 · עגבניות 5 · אפונה וגזר 1 = 56 מועמדים. המשפחות נמנות ב-p4 ולא ב-p2, ושורת שארית סוגרת את p2 72–76** | הרפים לבדם אינם מסננים כלום - רף `kcal ≤ 50` תופס 609 שורות בבריכה, ובהן קפה (86), חלב (23), מיצים (23), מים (7), מלח, בירה, ומחית תינוקות, ו-26 שורות FFQ. המשפחה היא שמגדירה את הירק והמבחן עובד רק בתוכה, ולכן ההיקף הוא p2 72–75 בלבד ו-76 מלכודת. p2 אינו בלעדי: 7340 בטטה יושב בתוך 73, קודי התירס בתוך 7510/7521, הזיתים והכבושים בתוך 7551 - שלושתם כבר תפוסים במשפחות 32 · 33 · 4 · 14. ב-p2 75 ספרת העשרות מקודדת הכנה ולא מצרך (751x טרי · 752x מבושל · 753x תבשיל · 754x מטוגן · 755x כבוש · 756x מרק), ולכן המנייה ב-p4. | `spec/05-food-db.md` §5.5 · `work/2026-09-13-block-8-veg.md` · `db/22_veg_candidates.py` |
| 13.09.2026 | **#47, חלק הירק: כשירים ≥ 25 · `no_prep` ≥ 10 · ≥ 2 בכל משפחה עם מכסה. הקריטריון השני הוא הכנה ולא אלרגנים, וקפוא אינו צורה שלישית** | בפחמימה הקריטריון השני היה גלוטן, כי שם הוא הצוואר - 2 מ-4. בירק האלרגנים ריקים מעצם זהות המזון, וכל 4 הכשירות היום נטולות אלרגנים, ולכן קריטריון אלרגנים היה עובר בלי לומר דבר. מה שכן צר: כל 4 הכשירות הן `no_prep`, ומזווה ביתי מחזיק גם סלט וגם ירק מבושל. קפוא הוא אופן אחסון ולא צורה - ירק קפוא נאכל מבושל ומקבל `prep` 2, כמו 3962 ב-8פ. הכיסוי הטרי מובטח במדידה: 72 מ-75 השורות במשפחה 40 בלי מילת בישול בשם. | `open-questions.md` #47 · `spec/05-food-db.md` §5.5 |
| 13.09.2026 | **הדירוג בירק: `(recipe, outlier, kcal, src_rank, servings_gate, source_code)`, ותקרת תת-קבוצה של 1 ל-p6 בפס הראשון בכל משפחה עם מכסה; פס שני משלים לפי הדירוג כשהמכסה לא התמלאה** | בפחמימה תקרת ה-p4 הייתה 4 ורק במשפחות עם מכסה ≥ 10, כי `carb_share` כבר דירג רזון והתקרה רק פיזרה. בירק אין מדד רזון - ירק הוא ירק - ומה שצריך הוא כיסוי מצרכים, כלומר בדיוק מה ש-p6 מודד. העומק תומך: 34 קודי p6 במשפחה 40 מול מכסה 14, 39 במשפחה 43 מול 14, 13 במשפחה 41 מול 8. משפחה 42 היא היחידה עם 2 בלבד, והפס השני משלים שם. `kcal` עולה מדרג את הצורה הפשוטה לפני המתובלת, כפי ש-`carb_share` עשה ב-8פ. | `spec/05-food-db.md` §5.5 · `db/22_veg_candidates.py` |
| 13.09.2026 | **#50 מוכרע לחדשים: לפריט `whole_only` התקרה היא כפולה שלמה של היחידה, ללא יוצא מן הכלל (תקדים אבוקדו 150). חל על 8י-ז. חמש השורות הקיימות מתוקנות בתת-בלוק נפרד אחרי 8י, יחד עם #51** | מדידת 8י-א הראתה ש-#50 אינו שני מופעים אלא חמישה, וכולם אותו דפוס - 3663 עגבניה יחידה 160 ותקרה 322 · 3793 מלפפון 82 ו-166 · 3837 פלפל 109 ו-220, כלומר 2.01 · 2.02 · 2.02 יחידות, לצד 8348 ו-3459 מ-8פ. הכלל מ-30.08 אומר שתקרה שאינה כפולה שלמה שוברת את `whole_only`, ובלי הכרעה 8י-ז היה משכפל את הדפוס על כ-40 שורות. התיקון לקיימות הוא שינוי ברשומות קיימות, ולכן הוא נופל בדיוק על #51 - `_compare_export.py` יוצא 1 על כל שינוי מכוון - ומשתנה אחד לבלוק מחייב להפריד אותו מ-8י. | `open-questions.md` #50 · #51 · `spec/05-food-db.md` §5.0 |
| 13.09.2026 | **ההערכה לתת-בלוק סוכן מתפצלת לפי סוג העבודה: דגם קיים 5–15 דק' · קרקע חדשה 15–45 · תת-בלוק שכותב יותר מארבע שאילתות חדשות או נוגע ביותר מחמישה קבצים מתפצל לשניים במקום לקבל טווח רחב. הבעלים: 15–30 דק' לכ-150 פריטים, חסם עליון - ללא שינוי** | החריגה השנייה הגיעה, כפי שהכרעת 12.09 הגדירה מראש. 8פ-ה2 רץ 20:07 מול 5–15 עם סימון קרקע חדשה, ו-8י-א רץ 71:49 מול 5–15 עם אותו סימון - שש שאילתות חדשות, ארבע עריכות תיעוד וקובץ נוף חדש בתת-בלוק אחד. הסימון הבחין נכון בשני המקרים אבל הטווח לא. תת-בלוקי דגם קיים ממשיכים להחזיק: 8פ-ה 6:11 · 8פ-ט2 ו-8פ-י יחד 5:58 · 8פ-ט 11:13 מהחידוש · 8פ-ז 17:21. | `measurements.md` · `PROGRESS.md` · `CLAUDE.md` §8 |
| 13.09.2026 | **גיליון התיוג (15) מקבל את משפחות הירק מ-22, באותו דגם שבו קיבל את משפחות הפחמימה מ-20: קוד בלי משפחה ב-07 נשאל ב-20, ואם אין - ב-22. ברירות מחדל לפי משפחה: כשרות פרווה לכל תשע (40–48) וטבעוני כן לכל תשע - אין בירק משפחה שברירת המחדל שלה ריקה, כי כל המתכונים הבשריים והחלביים נדחו ב-8י-ה. אין מקבילה ל-`CORN_SQL`: כל משפחת ירק היא מבחן p4 או p2, ולכן 15 אינו מריץ SQL משלו. תווית משפחה 52 נושאת קידומת `veg:` כדי לא להתנגש ב-`FAMILY_NO` של 11 עם משפחת השומן 14** | זו עריכה ידועה מראש ולא חזרה על 8פ-ה2, שם הגיליון יצא כותרת בלבד כי המצב הקומפקטי רץ על `FAMILIES` של 07. השער: גיליונות השומן, החלבון והפחמימה לפני ואחרי הנגיעה זהים בית-בית - הראיה שהנגיעה לא שינתה את מה שכבר עובד. גיליון הפחמימה נגזר מהמסד ונושא עמודת `eligible`, ולכן ההשוואה היא ריצה טרייה משני הצדדים באותו סשן ולא חתימה מהרשומה. | `db/15_tagging_sheet.py` · `db/22_veg_candidates.py` · `measurements.md` |
| 13.09.2026 | **תיוג הירק - היחידה לפי כלל 30.08 בשלושה מסלולים: יחידה שלמה כשפריט אחד הוא מנה (17 פריטים), כוס או מנה לירק קצוץ ומבושל (50), ו-`by_weight` כשהיחידה היחידה היא עלה, פרוסה או כף (7). `max_g` לפריט `whole_only` הוא כפולה שלמה של היחידה (#50). `prep` 0 לטרי ולמשומר שנפתח ונאכל (33), 2 לכל מה שהמשתמש מבשל כולל קפוא-מבושל (41). כשרות פרווה וטבעוני כן לכל 74; `quality` ריק, `supp` false. שני פריטים ירדו מ-76 ל-74: 3794 חצילים טריים ו-3766 כרוב ניצנים טרי, צורות שאינן נאכלות שהצורה המבושלת שלהן כבר ברשימה** | ארבע היחידות שנפסלו כ"כף" ב-8י-ז - 3570, 3594, 3595, 3735 - אין להן מנה אחרת - לשלוש הראשונות אין שורת הגשה אחרת כלל, ול-3735 יש רק כפית 6 ג' - ולכן משקל. 3774 כרוב אדום למשקל כי בין עלה 32 ג' לראש 542 ג' אין מנה. הכרעת 8י-ה בדקה צורה עם פסולת מול צורה נאכלת, ולא בדקה אם הצורה הגולמית נאכלת בכלל - חציל חי ונבט בריסל חי אינם נאכלים, וזה §5.0.2. `no_prep` אחרי ההפיכה יעמוד על 37 מול יעד 10 של #47 | `work/2026-09-13-block-8-veg.md` · `db/22_tag_veg.sql` · `open-questions.md` #21 · #50 |
| 13.09.2026 | **סעיף ה-≥2 ביעד הירק מתוקן: "≥ 2 בכל משפחה עם מכסה שיש בה יותר מקוד p6 אחד במאגר". ההפיכה רצה כפי שנכתבה, בלי להוסיף פריט** | הכרעת 8י-ב כללה שתי טענות שאינן יכולות להתקיים יחד - מכסה 1 למשפחה 48, שנמדדה מ-p6 יחיד ב-p4 7311, וסעיף "≥ 2 בכל משפחה עם מכסה" שהועתק מיעד הפחמימה. הסתירה עמדה מ-8י-ב ועד תחזית S3 של 8י-י, ונתפסה לפני כתיבה בלתי הפיכה. הנוסח שנמדד עומד והמועתק מתוקן. החלופה - לתייג את 3646 אפונה וגזר קפואים במיגרציה נוספת - נדחתה: אותו מצרך, אותו p6 731112, ותוספת כדי לספק שער היא בדיוק מה שכלל "אל תטען בלי למדוד" נועד למנוע. השליפות של 3595 ו-4023 בסקירה נעשו על שיקול מזווה לפני שהיה שער ולא אחריו | `open-questions.md` #47 · `spec/05-food-db.md` §5.5 · `work/2026-09-13-block-8-veg.md` |
| 13.09.2026 | **שער הייצוא של 8י-י נכשל אחרי ההחלה, והממצא הוא ש-09 אינו דטרמיניסטי. ההפיכה עומדת, לא נרשם בסיס חדש, ו-09 מתוקן ב-8י-כ לפני שנרשם בסיס כלשהו** | המיגרציה עברה את כל שערי המסד - V0–V6 שותקים, הצילום 423 · 264, `v_pool_depth` ירק 78 ו-37 `no_prep`, #47 מסופק. מה שנפל הוא ההשוואה בין שני ייצואים, על רשומה אחת שנתוניה לא השתנו ורק סדר שתי שורות ההגשה שלה התהפך. רישום `219,458 · 36f84dd8…` כבסיס היה רישום של מספר שאינו בר-שחזור, ולכן לא נרשם דבר. כלל 03.09 - "בסיס לקובץ שאינו בגיט הוא ריצה טרייה" - מניח שהריצה הטרייה חוזרת על עצמה, וההנחה הזאת נשברה | `open-questions.md` #52 · `db/09_export_menu_foods.py` · `db/_compare_export.py` |
| 13.09.2026 | **הייצוא של 09 הופך לפונקציה של המסד: שובר שוויון ב-`ORDER BY` של `SQL_SERVINGS`. הבסיס לשער הבא נרשם רק אחרי שלוש ריצות זהות** | אותה שאילתה קיבלה שתי תוכניות - עם 190 הקודים שלפני 8י-י `Incremental Sort` מעל `Nested Loop` על האינדקס `(food_id, label_he)`, ועם 264 `Sort` מעל `Hash Join` ו-`Seq Scan` - ובשתיהן `ORDER BY f.source_code, s.grams` אינו קובע סדר בין שתי שורות הגשה באותו משקל. בשאילתה: 4 מ-264 הפריטים המיוצאים נושאים שוויון כזה, ובשלושה מהם שתי השורות הן יחידות תפריט שנכנסות לשדה `servings` - 2828 · 2838 · 9595. `s.mida_code` כבר נבחר בשאילתה, יציב, ואינו תלוי בקולציה כמו תווית עברית; על הנתונים הוא סוגר את הסדר - 0 כפולים של מזון · משקל · `mida_code` ב-9,864 השורות - אף שהסכימה אינה אוכפת זאת. ההיקף אינו השער: `spike/menu_foods.py` הוא מה שהמודל קורא, סדר שורות ההגשה נכנס לפרומפט, ושער הרגרסיה של 40 הייצורים ב-92% נשען על כך שאותו מסד מייצר אותו טקסט. נדחו שתי חלופות - קבלת exit 1 בדגם #51, שמשאירה שני תיקו חיים שיפילו שער עתידי אחרי כתיבה בלתי הפיכה; והשוואה חסרת-סדר ב-`_compare_export.py`, שאינה פותרת את בדיקת החתימה בית-בית מול הבסיס הרשום. כלל 03.09 - "בסיס לקובץ שאינו בגיט הוא ריצה טרייה" - מניח שהריצה הטרייה חוזרת על עצמה, וההנחה נשברה כאן; מכאן הכלל שנוסף: בסיס נרשם רק אחרי שהריצה שהפיקה אותו שוחזרה | `db/09_export_menu_foods.py` · `open-questions.md` #52 · `measurements.md` |
| 14.09.2026 | **הדרת 28 שורות מוסכמת המשפחה ממיגרציית #50 (8ת-ז) עומדת; הנימוק שלה נפסל ומוחלף.** הנימוק שנפסל: התקרות באו "מטבלת `MAX_G` של הספייק, ונכתבו לפני שנודעה היחידה". הנימוק במקומו: שתי מוסכמות `max_g` חיות בריפו ומתנגשות - מוסכמת היחידה (30.08; 11.09 "יחידה שלמה מעל התקרה - התקרה היא היחידה") ומוסכמת המשפחה (טבלת הבעלים מ-11.09, בגרמים ובלי תלות ביחידה) - ואיזו גוברת היא שאלה #53 | הנימוק הקודם לא נמדד, והמדידה ב-8ת-א סותרת אותו: `MAX_G` ב-`spike/foods.py` מחזיקה 12 רשומות לפי שם, בלי 175 · 150 · 15, ואבוקדו בה 200 מול 150 במסד. את השורות כתבו `18_tag_protein.sql` (5ז, 26) ו-`16_tag_fat.sql` (5ה, 2). ו-30.08 מתעד ששמונה מעשר תקרות החלבון של 3ד נגזרו מהיחידה שנבחרה - כלומר גם "לפני שנודעה היחידה" אינו מחזיק | `measurements.md` בלוק 8ת · `open-questions.md` #50 · #53 · `work/2026-09-13-block-8t-fixes.md` |
| 14.09.2026 | **היקף #50 נדחה למדידת 8ת-ו - האם `whole_only` כופה על הפותר יחידות שלמות - ונגזר ממנה ולא מטיעון.** חמש שורות נושאות את החתימה "כפולה שלמה של היחידה ועוד 2.0 ג' בדיוק" (8348 · 3459 · 3663 · 3793 · 3837), ו-805 נושא אותה חתימה, ולכן הגבול הוא חמש או שש. אם `whole_only` אינו כופה יחידות שלמות, הכלל מ-30.08 שתקרה שאינה כפולה שלמה "שוברת את `whole_only`" נשען על הנחה שלא נמדדה, ו-#50 מנוסח מחדש ולא מתוקן | שלושה נימוקים רצופים לגבול נפסלו מול מדידה ב-8ת-א: הספירה - #50 נוסח על 5 ונמדדו 38; מקור `MAX_G` - נסתר בריפו (השורה הקודמת); ועלות ההצמדה - 7 מ-33 השורות שמחוץ לחמש עולות ≤ 2 ג', ו-805 עולה 2.0 בדיוק כמו החמש. גבול שנפסל שלוש פעמים כשנוסח לפני המדידה נגזר מהמדידה | `measurements.md` בלוק 8ת · `open-questions.md` #50 · `PROGRESS.md` 8ת-ו |
| 14.09.2026 | **#45, החצי הראשון, נסגר: התווית נרשמת בשתי עמודות חדשות ב-`food_curation` - מקור הנתון ותאריכו. המיגרציה נכתבת ב-7ת. החצי השני של #45 - מה קורה כשהיצרן משנה את הרכב המוצר אחרי שהתווית נרשמה - עובר לשאלה פתוחה חדשה, #58** | `curated_by` שמור למי שאצר, ומקור הנתון אינו נטען עליו במשמעות שנייה. `allergens_reviewed_at` אומר שהאלרגנים נסקרו, לא מאיפה בא הערך - ההערה בסכימה: "'{}' = reviewed and clean · NULL here = not reviewed" (`db/01_food_db_schema.sql:165`). שתי העמודות רושמות מקור ותאריך; מנגנון אימות מחדש אינו חלק מההכרעה | `open-questions.md` #45 · #58 · `work/2026-09-14-block-7-labels.md` |
| 14.09.2026 | **ארבעה יוצאים מתור התווית: 625 · 649 · 683 · 9589. התור 38 → 34** | 625 "בשר בקר, רוסטביף, אפוי, צלוי" · 649 "בשר בקר, מיובש" · 683 "בשר בקר, רוסטביף, כתף, מעושן" הם תיאורים גנריים שסווגו `industry` (`makor` 3) בלי יצרן בשם - אין מוצר על מדף שאפשר לצלם את תוויתו. 9589 "טמפה (פולי סויה מותססים), מבושל" הוא שורת מקור אמריקאית: `makor` 1, ש-`db/04_transform.py:49` מפענח כ-USA (NDB, FNDDS), `ingredient`. 1698 "טמפה, מזון אינדונזי שורשי, קפוא, כפרי בריא" הוא טמפה מסחרית עם יצרן בשם, `makor` 3, `industry`, ונשאר בתור. השניים אינם אותו פריט: קפוא מול מבושל, שומן 7.5 מול 11.4 ג' ופחמימה 16 מול 9.3 ג' ל-100 ג' | `work/2026-09-14-block-7-labels.md` · `db/block6_protein_label_codes.txt` · `db/source/moh_mitzrachim.csv` |
| 14.09.2026 | **#16 נסגר בלי מכסת שומן לבלוק 7; יעד עומק השומן בשער היציאה מתעדכן במקומה** | נמדד ב-7א: 11 נטולי אלרגנים מ-37 שומנים כשירים; לפי אלרגן Tree nuts 17 · Sesame 5 · Peanuts 4, ואף שורה אינה נושאת יותר מאלרגן אחד. האלרגיה הבודדת הגרועה, אגוזי עץ, משאירה 20 מול `MIN_PER_CAT["fat"] = 2` (`spike/filters.py:107`) - אין חסימה. 20 הוא המקרה הגרוע לאלרגיה אחת בלבד: שלוש האלרגיות יחד משאירות 11, ו-`eligible()` מסנן גם לפי דיאטה, כשרות ורמת בישול (`spike/filters.py:33-42`), כך שפרופיל שמצרף אותם יכול להישאר עם פחות. מה ש-#16 תיאר בשומן הוא חובת האצירה של §5 ויעד עומק, לא רצפת `pool_health()`; הרצפה שב-#16 - בתוספת 30.08.2026, "מול רצפת `pool_health()` של 4" - היא של החלבון הטבעוני | `open-questions.md` #16 · #57 · `spec/05-food-db.md` §5 · `spike/filters.py` |
| 14.09.2026 | **680 נשאר עם #35 ואינו נוגע בבלוק 7** | 680 נכנס לא-כשיר במיגרציה 18 (`db/18_tag_protein.sql:147`) ומעולם לא הפך לכשיר - בשונה מ-493, שהוצא מכשירות במיגרציה 12, ומ-4279 · 4387 · 4446, שהוצאו במיגרציה 14. `excluded_reason` שלו NULL. הנימוק כתוב בשורת 12.09.2026 "תיקון לבלוק 4: 561 גבינה מותכת עוברת לתנאי תווית; 680 בקר טחון עם חלבון צמחי יוצא" בקובץ הזה, ביומני בלוקים 4 ו-6 ובהערת הכותרת של `db/19_flip_protein.sql` | `open-questions.md` #35 · `db/19_flip_protein.sql` |
| 14.09.2026 | **בלוק 7 מתפצל לשני תת-בלוקים: 7ת - הפריטים שכבר ב-`foods` וממתינים לתווית; 7מ - מוצרים מסחריים עדכניים לפי §6 ב-`spec/05-food-db.md`** | 7ת עובד על 34 פריטים שכבר ב-`foods`: 31 עם שורת אצירה, בדגם ההפיכה של 17/19/21/23, ו-3 בלי - 2807 · 8966 · 8969 - בדגם התיוג וההפיכה של 20+21. 7מ דורש נתיב כניסה שאינו קיים (#56). אלה שני משתנים; העיקרון, משתנה אחד לכל תת-בלוק, כתוב בשורות 31.08.2026 ו-11.09.2026, ובנוסח "משתנה אחד לבלוק" בשורת #50 מ-13.09.2026 בקובץ הזה | `PROGRESS.md` · `open-questions.md` #56 |
| 14.09.2026 | ↩ בוטלה 14.09.2026 · **ההכרעה D1 של 7ב, כלשונה ביומן: "מוצר מסחרי חדש נכנס כקובץ מקור חמישי ב-`db/source`, ש-03 טוען לצד ארבעת קובצי המשרד."** | נסתרה במדידה ב-7ג, לפני שנכתבה לקובץ. `classify` מזהה קובץ לפי כותרות (`db/03_load_source.py:90`), ו-`if kind in seen:` (255) עם `continue` (258) מדלג על קובץ שני מאותו סוג - קובץ `foods` נוסף מחליף את קובץ המשרד או מדולג, לפי סדר המיון בלבד. כל `INSERT` ל-`src_*` יושב ב-03 (110 · 120 · 131 · 140), ואין `COPY` ואין מצב הוספה. תנאי העצירה שדרש לאמת את המנגנון מול הקוד לפני הכתיבה תפס זאת. D3 מתייתר איתה - כלשונו ביומן: "ה-50 של §6 כוללים את 38 הממתינים." נתיב הכניסה נפתח כשאלה #56 | `db/03_load_source.py` · `work/2026-09-14-block-7-labels.md` · `open-questions.md` #56 |
| 15.09.2026 | **פריט צמחי בתור של 7ת: ל-31 עם שורת אצירה - `food_curation.tags` מכיל `vegan`; ל-3 הפחמימות, שאין להן שורה - לפי הקטגוריה** | הכרעת הבעלים ב-7ת-א, בעצירה S3b. היא נשענת על אוצר התגים ש-16 ו-18 כתבו - `{vegan}` או `{}` ותו לא (V2 ב-16, V2e ב-18) - ו-`db/_audit_block7t.py` מודד אותו מחדש על ה-31 ועוצר אם אינו מחזיק. ב-15.09 נרשמה ב-`measurements.md` ובכותרת הסקריפט, ולא כאן | `db/_audit_block7t.py` · `measurements.md` |
| 15.09.2026 | **סיבים מהתווית נאספים לפריט צמחי בתור ש-`fiber_g` שלו ריק - כלשון #13, ולא לכל פריט בלי `fiber_g`** | #13 נסגר ב-29.08.2026: `COALESCE(0)` עומד, בתוספת בדיקת אצירה על "פריט צמחי עם `fiber_g` ריק שנכנס ל-`menu_eligible`". `measurements.md`, 7ת-א: "סעיף הסיבים הרחיב את #13 מעבר ל"פריט צמחי", וצומצם ל-#13 כלשונו." `fiber_g` ריק ב-6 מ-34 - צמחיים 4 (1698 · 1826 · 2807 · 8969), לא צמחיים 2 (1307 · 8584) | `PROGRESS.md` 7ת-ב · `open-questions.md` #13 |
| 15.09.2026 | ↩ בוטלה 17.09.2026 · **7ת רץ בשלוש מיגרציות, אחת לכל משתנה: 24 הסכימה · 25 תיוג בלי כשירות · 26 הפיכה** | כלשון שורת 7ת ב-`PROGRESS.md`: "שלוש מיגרציות, אחת לכל משתנה - הסכימה · תיוג בלי כשירות · הפיכה". הנימוק שהצ'אט נתן להן נפל, ו-`measurements.md` רושם: "הנימוק לשלוש מיגרציות - שה-DDL ישנה את מה ש-`_pre` מצלם - נפל מול רשימת העמודות המפורשת ב-`db/23_flip_veg.sql:25-29`." המבנה עומד, בנוסח "אחת לכל משתנה" | `PROGRESS.md` 7ת · 7ת-ג1–ג3 · `measurements.md` |
| 15.09.2026 | **#59 נפתחת - איפה חי במסד ערך סיבים שנקרא מתווית - ומוכרעת ב-7ת-ב, לפני 7ת-ג3** | #13 אינו אומר איפה נכתב הערך שנקרא. ערך שנכתב ישירות ל-`foods.fiber_g` אינו שורד את הריצה הבאה של 04: היא פותחת ב-`TRUNCATE` על `foods` (`db/04_transform.py:320-321`) וממלאת את `fiber_g` רק מ-`total_dietary_fiber` (`:349-350`); `food_curation` אינה ברשימת ה-`TRUNCATE` (`:40-41`) | `open-questions.md` #59 |
| 15.09.2026 | ↩ בוטלה 17.09.2026 · **הבהרה, לא ביטול: 3 הפחמימות עוברות את 25 ואז את 26 (ב-16.09: 26 הופכת מהן רק פחמימה עם תווית). "20+21" בשורת 14.09.2026 "בלוק 7 מתפצל לשני תת-בלוקים" וביומן בלוק 7 נקב בדגם, לא במספרי המיגרציות** | השורה: "בדגם התיוג וההפיכה של 20+21"; היומן, בפסקה "התור אינו הומוגני": "הן צריכות תיוג ואז הפיכה, בדגם 20+21." שתיהן אומרות "בדגם". 25 בדגם 20, ו-26 בדגם 17/19/21/23 - כלשון שורות 7ת-ג2 ו-7ת-ג3 ב-`PROGRESS.md` | `PROGRESS.md` 7ת-ג2 · 7ת-ג3 · `work/2026-09-14-block-7-labels.md` |
| 16.09.2026 | **מיגרציה 24 מוסיפה ל-`food_curation` שלוש עמודות, כולן NULL: `label_source` text · `label_date` date · `fiber_g_label` numeric. CHECK: `label_source` ו-`label_date` שתיהן NULL או שתיהן מלאות. בשורה שנאצרה לפי זהות שלושתן NULL** | השמות `label_source` ו-`label_date` הם כותרת הגיליון של 15.09 (`db/block7_label_sheet.tsv`). date, כי השם אומר תאריך, ו-`_at` בסכימה הזו הוא timestamptz: כל 9 העמודות ב-`public` ששמן מסתיים ב-`_at`, בטבלאות וב-views, הן timestamptz (נמדד ב-7ת-א2). משלים את שורת 14.09.2026 על #45 - "מקור הנתון ותאריכו"; `fiber_g_label` נוספת בהכרעה על #59 שבשורה הבאה. באותו תת-בלוק מתעדכן גם `db/01_food_db_schema.sql`, כלשון שורת 7ת-ג1 | `PROGRESS.md` 7ת-ג1 · `db/01_food_db_schema.sql` |
| 16.09.2026 | **החצי הראשון של #59 נסגר: ערך הסיבים מהתווית, ל-100 ג', חי ב-`food_curation.fiber_g_label`, לארבעת הפריטים הצמחיים בתור בלי `fiber_g` - 1698 · 1826 · 2807 · 8969. 1307 ו-8584 מתהפכים על האלרגנים בלבד, בלי ערך סיבים, והארבעה מתהפכים ב-26 כמו השאר - כולם רק עם תווית** | הכרעת הבעלים ב-16.09 על דיווחי 7ת-א2. 1307 סלמון ו-8584 טונה הם דגים, ובנימוק שסגר את #13 ב-29.08.2026: "בבשר, דג, חלב וביצה אפס סיבים נכון". מי צורך את `fiber_g_label` - `v_menu_foods`, גזירת ה-kcal - הוא החצי השני של #59, מחוץ ל-7ת. מקדים את "מוכרעת ב-7ת-ב" של שורת 15.09.2026, ו-`open-questions.md` #59 מקבלת תוספת מ-16.09.2026 | `open-questions.md` #59 · #13 |
| 16.09.2026 | ↩ בוטלה 17.09.2026 · **שורה עם תווית היא שורה ש-`label_source` שלה בגיליון אינו ריק. `label_source` ו-`label_date` מתמלאים רק לפריט שהבעלים אישר; פריט שנמצא לו דף ויצא - ממצא הרכב, אלרגן מחוץ לשמונה, מוצר יורש - מקבל רק `notes`: הסיבה והכתובת כטקסט, ושאר עמודות הבעלים בשורה שלו ריקות. 26 הופכת רק את השורות עם תווית, ו-N של שער הדלתא הוא מספרן בגיליון המלא, נמדד בפתיחת 7ת-ג3 - לא 34 של `db/block7_label_codes.txt`. 25 מכניסה את שלוש הפחמימות בלי תלות בתווית; פחמימה בלי תווית נשארת `menu_eligible = false` ולא נסקרה, כמו ה-31 היום** | הוכרע ב-16.09 על דיווחי 7ת-א2. שורת 7ת-ג3 של 15.09 קבעה `UPDATE` על 34 ו-N = 34 מקובץ הקודים; `db/_compare_export.py` עוצר כשקוד מקובץ הציפייה לא נוסף לייצוא - "STOP: expected but not added" (`:219-220`) - ויוצא ב-1 (`:251`) | `PROGRESS.md` 7ת-ג2 · 7ת-ג3 · `db/_compare_export.py` |
| 16.09.2026 | **פריט שלא נמצאה לו תווית לפי כללי המקור נשאר לא כשיר, ונקוב ביומן בלוק 7. אין כתיבה ל-`excluded_reason`, וה-enum אינו משתנה** | שורת 7ת-ג3 של 15.09 הניחה `UPDATE` על כל 34, ולא נקבה בפריט שאינו מתהפך. כך יושב היום 680 - `excluded_reason` NULL, וההחרגה ביומן (D6) | `work/2026-09-14-block-7-labels.md` |
| 16.09.2026 | **האוכלוסייה הממתינה - `NOT menu_eligible AND allergens_reviewed_at IS NULL AND excluded_reason IS NULL` - מחזירה אחרי 7ת את 625 · 649 · 683 · 9589 · 1630 · 680, ועוד כל פריט שלא התהפך. כולם נקובים ביומן, ואין כתיבה לאף אחד מהם מלבד שורת האצירה ש-25 מכניסה לפחמימה בלי תווית** | נמדד ב-7ת-א2: 37 - 31 פריטי התור עם שורת אצירה, ואיתם השישה. בדיקת הכיוון ההפוך של 7א (`db/_audit_block7a.py:221-222`) אינה מסננת `excluded_reason`, ובה נכנסו 118 שורות מוחרגות (היומן, S4). 625 · 649 · 683 · 9589 יצאו מתור התווית בשורת 14.09.2026; 1630 ו-680 לא היו בו - 1630 לפי שורת 12.09.2026 על Lupin (#48), 680 לפי שורת 14.09.2026 על #35. 493 מחוץ לאוכלוסייה: `allergens_reviewed_at` מ-30.08.2026 | `work/2026-09-14-block-7-labels.md` · `db/_audit_block7a.py` |
| 16.09.2026 | **7ת-ב רץ בשלושה מקטעי צ'אט, לפי קבוצות הקודים A · B · C: הצ'אט מחפש את התווית בדפי רשת והבעלים מאשר כל פריט. 45–60 דק' לכל מקטע, חסם עליון: A מהקומיט של 7ת-א2 עד פתיחת ב1 · B מהקומיט של ב1 עד פתיחת ב2 · C מהקומיט של ב2 עד פתיחת ב3. הרשומה אומרת שהתווית נקראה מדף רשת, לא מהאריזה** | מחליף את "מהבית" ואת "רשימת הרכיבים כפי שהיא מודפסת" של 15.09, שנכתבו ב-15.09 רק ב-`PROGRESS.md` - בשורות 7ס2 · 7ת-ב וב-📍 - ולכן אין ↩. ב-15.09 7ת-ב היה תת-בלוק אחד, 60–120 דק' נטו. הקבוצות, קודים עם `name_he`, ביומן בלוק 7 | `PROGRESS.md` 7ת · 7ת-ב · `work/2026-09-14-block-7-labels.md` |
| 16.09.2026 | **מקור התווית: דף היצרן קודם; דף קמעונאי או צד שלישי רק כשליצרן אין דף, ורק עם רשימת רכיבים מלאה ושורת "מכיל"; אחרת אין תווית, והפריט נשאר בחוץ ונקוב ביומן** | שורת 7ת-ב של 15.09 (`58be42a`): "מהבית", "רשימת הרכיבים כפי שהיא מודפסת" ו"מקור התווית ותאריכה" - בלי סדר בין מקורות | `PROGRESS.md` 7ת-ב · `work/2026-09-14-block-7-labels.md` |
| 16.09.2026 | **`label_source` - כתובת הדף; כששורה מכסה כמה גרסאות, כמה כתובות, מופרדות ברווח, קו אנכי ורווח. `label_date` - היום שבו הדף נקרא** | שורת 14.09.2026 על #45 קבעה "מקור הנתון ותאריכו", ושורת 7ת-ב של 15.09 (`58be42a`) "מקור התווית ותאריכה" - שתיהן בלי הערך עצמו | `PROGRESS.md` 7ת-ב · `work/2026-09-14-block-7-labels.md` |
| 16.09.2026 | **טעמים: שורת מקור שנוקבת בטעמיה מקבלת את איחוד האלרגנים על פני הטעמים הנקובים; שורה שאומרת "טעמים שונים" או "סוגים שונים" - וכמוה 556, "בטעמים" - את האיחוד על פני כל הגרסאות שהיצרן מציג לקו הזה היום** | הוכרע ב-16.09 על דיווחי 7ת-א2 | `work/2026-09-14-block-7-labels.md` |
| 16.09.2026 | **התאמת שם ברמת הגרסה, בין שורת המקור למוצר בדף. מוצר יורש בשם אחר נשאר בחוץ, ונרשם ביומן כמועמד ל-7מ** | 7מ - מוצרים מסחריים עדכניים, חסום: נתיב הכניסה נמדד ואינו קיים (#56) | `work/2026-09-14-block-7-labels.md` · `open-questions.md` #56 |
| 16.09.2026 | **בדיקת הרכב: kcal מהדף מול `foods.kcal_source`, וחלבון מהדף מול `foods.protein_g`. ממצא - סטייה מעל 12% וגם לפחות 5 kcal ב-kcal, או מעל 12% וגם לפחות 1 ג' בחלבון. הבעלים מכריע לכל פריט, וברירת המחדל - בחוץ. ערכי הדף של פריט שאושר נרשמים בשתי עמודות ש-7ת-ב1 מוסיף לגיליון: `kcal_label` · `protein_g_label`** | 12% ו-5 kcal הם מוסכמת `v_kcal_outliers`: "12% relative" ו-"absolute 5 kcal floor" (`db/01_food_db_schema.sql:375-376`, התנאים ב-`:402-403`). `kcal_source` ו-`protein_g` מלאים בכל 34 (נמדד ב-7ת-א2) | `db/01_food_db_schema.sql` · `PROGRESS.md` 7ת-ב1 |
| 16.09.2026 | **אחרי כל מקטע ריצת רישום - 7ת-ב1 · ב2 · ב3, קלוד קוד, 5–15 דק' כל אחת: שורות המקטע ב-`db/block7_label_sheet.tsv` - `ingredients_label` · `allergens_label` · `fiber_g_label` · `kcal_label` · `protein_g_label` · `label_source` · `label_date` · `notes` - ספירת השורות המלאות בקוד, sha256 לפני ואחרי על התוכן שבגיט, המקטע ליומן, קומיט. תיוג 3 הפחמימות - טבלה ביומן בלוק 7, בסעיף 7ת-ב1, בצורת טבלת התיוג של 8פ-ו ושורת "לכולם" שלה, בלי עמודות בגיליון ובלי קובץ חדש; 25 (7ת-ג2) בונה ממנה את ה-VALUES ובודקת לפי שם. חתימת הגיליון המלא, על התוכן שבגיט, אחרי ב3 היא הבסיס לשערים של 7ת-ג3, ומחליפה את בסיס הגיליון שנרשם ב-15.09** | הגיליון נכתב ב-7ת-א כגיליון הבעלים ל-7ת-ב (`db/_audit_block7t.py:14`). ב-15.09 נרשם ב-`measurements.md` כבסיס לשערים של 7ת-ג3 - 5,175 בתים, sha256 `011b69cd…`, התוכן שבגיט. שש עמודות הבעלים שלו ריקות בכל 34 השורות - נספר בקוד ב-7ת-א2, על התוכן שבגיט ב-`58be42a`. טבלת 8פ-ו: קוד · פריט · prep · whole_only · by_weight · max_g · יחידה · טבעוני, ושורת "לכולם" | `PROGRESS.md` 7ת-ב1–ב3 · 7ת-ג2 · `measurements.md` · `work/2026-09-12-block-8-carb.md` |
| 17.09.2026 | **תערובות להכנה מהירה שמוכנות במים - מנה חמה, מנות אורז ופסטה מתערובת - אינן נכנסות לתפריט. 2807 · 8966 · 8969 בחוץ מכוח ההכרעה הזו, ואין להן שורת אצירה** | הכרעת הבעלים ב-17.09, במקטע A של 7ת-ב (`work/2026-09-14-block-7-labels.md`, סעיף "7ת-ב, מקטע A"). מחליף, בלי ↩, את שליחת השלוש לתנאי תווית ב-8פ-ד: תת-הסעיף "### תווית - 3, לבלוק 7" ב-`work/2026-09-12-block-8-carb.md`, שורת 8פ-ד ב-`PROGRESS.md` ו-`db/block8_carb_label_codes.txt`, שנשאר בריפו. ההכרעה של 8פ-ד לא נכתבה בקובץ הזה, ולכן אין בו שורה לסמן | `work/2026-09-12-block-8-carb.md` · `work/2026-09-14-block-7-labels.md` · `PROGRESS.md` 8פ-ד · 7ת-ג2 |
| 17.09.2026 | **ל-7ת שתי מיגרציות: 24 מוסיפה את עמודות התווית, 25 הופכת. מיגרציית התיוג, שהייתה 25, ריקה, ו-7ת-ג2 בוטל; שאר היקף 7ת-ג3 אינו משתנה מכוח השורה הזו. שורה שבוטלה ב-`PROGRESS.md` מקבלת ⛔, לא ⬜, ותיאור שנפתח ב"בוטל", בתאריך ובסיבה** | אחרי ההכרעה על התערובות אין שורת אצירה להכניס, ומיגרציית התיוג נשארת בלי שורות. מחליף את שורת 15.09.2026 "7ת רץ בשלוש מיגרציות", שסומנה ↩ בוטלה, ואת שורת 15.09.2026 "הבהרה, לא ביטול: 3 הפחמימות עוברות את 25 ואז את 26", שנפלה כולה וסומנה ↩ בוטלה. עד 17.09 לא הייתה ב-`PROGRESS.md` שורה מבוטלת: בשורות הטבלה היו רק ✅ · 🟡 · ⬜ | `PROGRESS.md` 7ת · 7ת-ג2 · 7ת-ג3 |
| 17.09.2026 | **שורה עם תווית היא שורה ש-`label_source` שלה בגיליון אינו ריק. `label_source` ו-`label_date` מתמלאים רק לפריט שהבעלים אישר; פריט שנמצא לו דף ויצא - ממצא הרכב, אלרגן מחוץ לשמונה, מוצר יורש - מקבל רק `notes`: הסיבה והכתובת כטקסט, ושאר עמודות הבעלים בשורה שלו ריקות. מיגרציית ההפיכה היא 25, והיא הופכת רק את השורות עם תווית; N של שער הדלתא הוא מספרן בגיליון המלא, נמדד בפתיחת 7ת-ג3 - לא 34 של `db/block7_label_codes.txt`** | מחליף את שורת 16.09.2026 "שורה עם תווית", שסומנה ↩ בוטלה: המשפט שלה על 25 שמכניסה את שלוש הפחמימות בלי תלות בתווית נפל בהכרעה על התערובות, והמיגרציה שהיא קראה לה 26 היא עכשיו 25. שאר השורה נכתב כאן מחדש, כפי שהוא עומד | `PROGRESS.md` 7ת-ג3 · `db/block7_label_sheet.tsv` |
| 17.09.2026 | **#59 מצטמצמת לשני פריטים: `fiber_g_label` נכתב ל-1698 ול-1826 בלבד** | מארבעת הפריטים הצמחיים בתור בלי `fiber_g`, 2807 ו-8969 יצאו בהכרעה על התערובות, בשורה מ-17.09.2026 שלמעלה. מקום הערך, `food_curation.fiber_g_label`, נשאר כהכרעת 16.09 | `open-questions.md` #59 |
| 17.09.2026 | **בהפיכה, לשורה ש-`allergens_label` שלה בגיליון נוקב ב-Egg, Milk או Fish מוסר התג `vegan`: `tags = array_remove(tags, 'vegan')`. בדיקת V אחרי 25: אף שורה `menu_eligible` עם התג `vegan` אינה נושאת Egg, Milk או Fish** | 10126 ו-1791 נושאים `{vegan}` מ-`db/18_tag_protein.sql` (`:204`, `:210`), מיגרציית התיוג של בלוק 5ז, ובדפי שניהם ברשימת הרכיבים חלבון ביצה. `spike/filters.py:33` מסנן לפרופיל טבעוני לפי התג בלבד. נמדד 17.09.2026 בקריאה בלבד, בשאילתה שנכתבה לרישום: 176 שורות `menu_eligible` עם `vegan`, ואף אחת מהן אינה נושאת Egg, Milk או Fish | `PROGRESS.md` 7ת-ג3 · `db/18_tag_protein.sql` · `spike/filters.py` · `measurements.md` |
| 17.09.2026 | **1792 בחוץ לפי כלל הזהות: סלרי, רכיב בתבלינים שלו, אינו באוצר שמונה התוויות. זה המקרה השני של #48** | בדפים ב-soglowek.co.il: "מכיל גלוטן חיטה, ביצים, סויה, סלרי" (`db/block7_label_sheet.tsv`, `notes` של 1792). האוצר - Egg · Fish · Gluten · Milk · Peanuts · Sesame · Soy · Tree nuts - ופריט שהאלרגן שלו מחוץ לו נשאר לא כשיר (`spec/05-food-db.md` §4). המקרה הראשון היה תורמוס, בשורת 12.09.2026 על Lupin | `open-questions.md` #48 · `work/2026-09-14-block-7-labels.md` |
| 17.09.2026 | **`db/_audit_block7t.py` מסרב לכתוב את `db/block7_label_sheet.tsv` כשהקובץ קיים ובאחת משורותיו עמודת בעלים אינה ריקה - `ingredients_label` · `allergens_label` · `fiber_g_label` · `kcal_label` · `protein_g_label` · `label_source` · `label_date` · `notes`: עצירה, יציאה ב-1, ואף קובץ אינו נכתב, גם לא קובץ הקודים. הגיליון אחרי 7ת-ב3 הוא הבסיס לשערים של 7ת-ג3, כהכרעת 16.09** | בלי השומר, הרצה חוזרת שאף עצירה בה אינה יורה כותבת את הגיליון מחדש ב-16 העמודות של 15.09 ובעמודות בעלים ריקות, ומוחקת את מה ש-7ת-ב1 רשם (`work/2026-09-14-block-7-labels.md`, "סיכון בגיליון"). "כהכרעת 16.09" - שורת 16.09.2026 "אחרי כל מקטע ריצת רישום" | `db/_audit_block7t.py` · `work/2026-09-14-block-7-labels.md` |
| 17.09.2026 | **הבהרה, לא ביטול - ארבע שורות שהכרעות 17.09.2026 משנות בהן סעיף שאינו עיקר ההכרעה.** 14.09.2026 "בלוק 7 מתפצל לשני תת-בלוקים" - הפיצול עומד, והסעיף על 2807 · 8966 · 8969 "בדגם התיוג וההפיכה של 20+21" הוחלף: השלוש בחוץ · 16.09.2026 "החצי הראשון של #59 נסגר" - שני פריטים, 1698 · 1826, וההפיכה ב-25 · 16.09.2026 "האוכלוסייה הממתינה" - אין חריגת הכנסה, כי לשלוש הפחמימות אין שורת אצירה · 16.09.2026 "אחרי כל מקטע ריצת רישום" - אין טבלת תיוג, ו-7ת-ב1 נרשם בלעדיה (`47a8562`) | הכלל שהבעלים קבע ב-17.09: ↩ על שורה שלמה, ונוסח מחדש של מה שעומד, כשהשורה נופלת כולה או כשנופל הסעיף המבצעי שלה; שורת הבהרה בלי ↩ כשהסעיף שנפל אגבי להכרעה שעומדת. לפיו סומנו ↩ שתי השורות מ-15.09.2026, "7ת רץ בשלוש מיגרציות" ו"הבהרה, לא ביטול: 3 הפחמימות עוברות את 25 ואז את 26". שורת 16.09.2026 "שורה עם תווית" סומנה ↩ בהכרעה נפרדת של הבעלים, ונכתבה מחדש בשורה מ-17.09.2026 | `decisions.md` |
| 17.09.2026 | **הבהרה, לא ביטול: בשורת 15.09.2026 "פריט צמחי בתור של 7ת" הסעיף "ל-3 הפחמימות, שאין להן שורה - לפי הקטגוריה" אינו חל עוד - שלוש הפחמימות יצאו מהתור בהכרעה מ-17.09.2026 על תערובות להכנה מהירה. הסמן לפריט צמחי ב-31 השורות עם שורת אצירה עומד** | אותו כלל של 17.09 כמו בשורת ההבהרה שלמעלה: הסעיף שנפל אגבי להכרעה שעומדת, ולכן בלי ↩. אותו סעיף בכותרת `db/_audit_block7t.py` - "the 3 carbs count as plant by category" - הוא היסטוריה ונשאר | `decisions.md` · `db/_audit_block7t.py` |
| 19.09.2026 | **אופן הקריאה במקטעי הרשת של 7ת-ב: הבעלים אינו קורא דפים בדפדפן. פריט שאת דף היצרן שלו הצ'אט אינו יכול לקרוא - אתר שחוסם קריאה אוטומטית, או תווית שקיימת רק כתמונת אריזה - יוצא כ"תווית לא נקראה", ונרשם כמועמד ל-7מ. תמונת תווית מדף היצרן שהבעלים מעלה לצ'אט והצ'אט קורא מותרת, כמו ב-661 ב-18.09.2026, ואינה חובה. חל על מקטע B מ-19.09.2026 ועל מקטע C** | הכרעת הבעלים ב-19.09, במקטע B (`work/2026-09-14-block-7-labels.md`, סעיף "7ת-ב, מקטע B"). במקטע A הבעלים קרא בדפדפן את הדפים ב-osem-nestle.co.il וב-soglowek.co.il והכתיב לצ'אט - כך נרשם ביומן, בסעיף "7ת-ב, מקטע A", ולא בקובץ הזה, ולכן אין כאן שורה לסמן. במקטע B: ב-neto.org.il התוויות רק כתמונות אריזה - 662 · 923 · 9650; tnuva.co.il חוסם - 802 · 8625. התמונה היא קובץ בדף היצרן, ולכן בשורת 16.09.2026 "7ת-ב רץ בשלושה מקטעי צ'אט" עומד הסעיף "הרשומה אומרת שהתווית נקראה מדף רשת, לא מהאריזה" | `work/2026-09-14-block-7-labels.md` · `PROGRESS.md` 7ת-ב |
| 19.09.2026 | **`label_source` לתווית שנקראה מתמונת אריזה בדף היצרן - כתובת קובץ התמונה, בדומיין של היצרן** | משלים, בלי ↩, את שורת 16.09.2026 "`label_source` - כתובת הדף": לתווית שנקראה מדף עומדת כתובת הדף. ב-661 - https://neto.org.il/wp-content/uploads/2025/03/7290002109488.jpg; טיבון ויל הוא מותג בקבוצת נטו. `label_date` שלו 2026-09-18, היום שבו התמונה נקראה | `db/block7_label_sheet.tsv` · `work/2026-09-14-block-7-labels.md` |
| 19.09.2026 | **1805 בחוץ, בהכרעת הבעלים: הקריאה לא הושלמה. kcal והחלבון של האריזה הקמעונאית לא נקראו, והשאלה אם ברשימת הרכיבים יש חלבון חיטה לא הוכרעה. 1805 מועמד ל-7מ** | 1805 המתין מ-17.09, ו-7ת-ב1 רשם ב-`notes` שלו בגיליון "ממתין - מוכרע ב-7ת-ב2" (`47a8562`; ביומן - "הממתין - 1805"). קטע האינדקס ב-soglowek.co.il נוקב ב"מכיל גלוטן חיטה" וברכיב "חלבון חיטה (מכיל גלוטן)", והבעלים קרא ב-17.09 "עלול להכיל גלוטן חיטה"; שתי הקריאות שונות. דף ה-1 ק"ג החזיר 404 ב-18.09.2026 | `db/block7_label_sheet.tsv` · `work/2026-09-14-block-7-labels.md` |
| 19.09.2026 | **הבהרה, לא ביטול: "תווית לא נקראה" לפי כלל הקריאה של 19.09.2026 חל גם על דף יצרן שנקרא ואין בו טקסט תווית - בלי רשימת רכיבים ובלי טבלה תזונתית - ועל דף יצרן שמחזיר 404. הפריט יוצא, ונרשם כמועמד ל-7מ** | הכרעת הבעלים במקטע C, 19.09.2026, פריט-פריט: בלי טקסט תווית - 539 · 556, דף המותג ב-strauss-group.co.il, ו-8690 · 8696, דפי המוצר ב-gad-dairy.co.il; 404 - 1826, somsom.co.il. שורת 19.09.2026 "אופן הקריאה במקטעי הרשת של 7ת-ב" נוקבת בשני מקרים בלבד - אתר שחוסם קריאה אוטומטית, ותווית שקיימת רק כתמונת אריזה - והיא עומדת; השורה הזו מרחיבה את המנייה, ולכן בלי ↩ | `db/block7_label_sheet.tsv` · `work/2026-09-14-block-7-labels.md`, סעיף "7ת-ב, מקטע C" |
| 19.09.2026 | **עוזר הצילום נכנס לריפו כ-`db/_snapshot_ro.py`, בתת-בלוק משלו, 7ת-ג0, לפני 7ת-ג1. הוא מייבא את `SNAPSHOT_SQL` ואת `query()` מ-`db/_audit_block7a.py`, כל משפט בטרנזקציה READ ONLY, ו-`db/` נפתר יחסית לקובץ ולא ל-worktree. מכאן דוחות מצב ושערים מריצים אותו מנתיב הריפו** | מ-16.09 כל דוח מצב תלה בעותק scratch ידני, שנתיב ה-`db/` שלו הצביע על worktree אחד. ב-19.09 היו שלושה עותקים, ואף אחד מהם לא הצביע על ה-worktree של הסשן החמישי, ודוח המצב כתב רביעי. פרומפט הצ'אט של 19.09 נקב בו כנתיב ריפו (`CLAUDE.md` §8, לקח 19.09.2026) | `db/_snapshot_ro.py` · `open-questions.md` #62 · `PROGRESS.md` 7ת-ג0 |
| 19.09.2026 | **הבהרה, לא ביטול: `fiber_g_label` נשארת במיגרציה 24 כפי שהוכרע ב-16.09, מוכנה ל-7מ. אחרי 25 היא NULL בכל השורות, וזה ידוע** | חמש שורות נוקבות ב-`fiber_g_label`, ושורה שישית נוקבת בסעיף שנפל בלי לנקוב בשם; אף אחת מהן אינה מקבלת ↩. שתי שורות 16.09.2026 עומדות, ושתיהן על עמודת המסד - "מיגרציה 24 מוסיפה ל-`food_curation` שלוש עמודות": 24 מוסיפה אותה; "החצי הראשון של #59 נסגר": הערך שנקרא מתווית חי בה. בשורת 17.09.2026 "#59 מצטמצמת לשני פריטים" נפל הסעיף "נכתב ל-1698 ול-1826 בלבד" - עם תוצאת מקטע C ב-19.09.2026 שניהם בחוץ, ולשניהם לא נקרא ערך סיבים מאף מקור; לא בהכרעה, ולכן השורה הזו מבהירה, בלי ↩. כך גם בשורת 17.09.2026 "הבהרה, לא ביטול - ארבע שורות שהכרעות 17.09.2026 משנות": בסעיף שלה על "החצי הראשון של #59 נסגר" נפל "שני פריטים, 1698 · 1826", מאותה סיבה, והשורה הזו מבהירה גם אותו, בלי ↩. שורת 16.09.2026 "אחרי כל מקטע ריצת רישום" ושורת 17.09.2026 "`db/_audit_block7t.py` מסרב לכתוב" עוסקות בעמודת הגיליון ובשומר של `db/_audit_block7t.py`, ואינן זזות. הורדת העמודה הייתה שינוי תוכנית שנוגע בשורות 16.09.2026 ו-17.09.2026 על עמודת המסד ובשורות 7ת-ג1 ו-7ת-ג3 ב-`PROGRESS.md`, עם הסקר של §8 - לעמודה שעונה על החצי הראשון של #59 לכל תווית שתיקרא בעתיד, 7מ בכלל | `open-questions.md` #59 · `PROGRESS.md` 7ת-ג1 |
| 19.09.2026 | ↩ בוטלה 21.09.2026 · **7ת נסגר ומוזג. בלוק 7 נשאר פתוח על 7מ בלבד, חסום על #56 - נתיב הכניסה למוצרים מסחריים לא קיים, שלוש חלופות לא נוסו, והבעלים מכריע על מדידה. 7מ אינו מזיז את הצוואר הצר ואינו חוסם את שער היציאה של 9. הסדר: מקטע החלבון והשומן של 8, ובו #53 לצד #40 · #41 · #48 · #49 → #35 → בלוק 9; 7מ אחרי הכרעת #56** | שורת 12.09.2026 "בלוק 8 נפתח לפני בלוק 7": בלוק 7 מוסיף חלבון, וחלבון ושומן רחוקים מרצפות `pool_health()`, ולכן הוא אינו מזיז את הצוואר הצר; שער היציאה של 9 נמדד על הצוואר הצר. סגירת בלוק 7 בלי 7מ הייתה שינוי תוכנית שנוגע בשורות 12.09.2026 ו-14.09.2026, עם הסקר של §8; בלוק 7 נשאר פתוח, ולכן שורת 14.09.2026 "בלוק 7 מתפצל לשני תת-בלוקים" עומדת, וכך גם שורות "מועמד ל-7מ". הבהרה, בלי ↩: בשורת 12.09.2026 הסעיף "7 רץ בכל שלב לפניו" חל על 7ת, שרץ לפני 9; על 7מ הוא נופל - 7מ רץ אחרי הכרעת #56, ויכול לרוץ אחרי 9. ההכרעה המבצעית של השורה - 8 לפני 7, פחמימה ואז ירק - עומדת | `PROGRESS.md` 7 · 7מ · `open-questions.md` #56 |
| 20.09.2026 | **מקטע החלבון והשומן של בלוק 8 מתפצל לשני בלוקים - 8ש (שומן) לפני 8ח (חלבון).** שורת תת-בלוק נכתבת בפתיחתו, עם הערכה, כמו ב-8פ וב-8י. **יעד זמני:** שומן 37 → ~60 כשירים ונטולי אלרגנים 11 → ~25; חלבון 108 → ~150, טבעוני 18 → ~35, נטולי אלרגנים 48 → ~65; סך ~340, בתוך 300–400 של בלוק 8. המספר הסופי נקבע בתת-בלוק ב של כל בלוק על מדידת א, כמו #47 ב-8פ-ב. **משפחות §5.5 ב-8ש:** זיתים · אבוקדו · שמן זית ואבוקדו · שמנים ניטרליים, ובמידת הצורך טחינה ושומשום וגרעינים אחרים; בחוץ אגוזים וחמאות אגוזים · חמאה וסמנה · מרגרינה וממרחים · מיונז ורטבים. **ב-8ח** כל משפחות §5.5 שיש להן מכסה, בדגש על קטניות וסויה, סייטן, אבקות צמחיות וביצים; איברים פנימיים ותבשילים נשארים במכסה 0. **הצינור לכל בלוק:** מדידת משפחות → הכרעת יעד → מועמדים בשאילתה שמייבאת את `family_of()` מ-07 → סקירה בצ'אט → תיוג → אלרגנים → הפיכה במיגרציה, בדגם 8פ/8י | שומן הוא הצוואר הצר: 37 כשירים, ובמדידת דוח המצב של 20.09 זיתים 0 ושמנים ניטרליים 0 כשירים ואבוקדו 1 - וזיתים ואבוקדו הם שני מקורות השומן נטולי האלרגנים ש-§5.5 ב-`spec/05-food-db.md` מחייב עומק בהם, ו-§5 שם נוקב בשמן זית, באבוקדו ובזיתים. שמן זית ואבוקדו עומד על 3 כשירים. אין על השומן הכרעות מקדימות, והוא קובע את דגם המקטע כמו 5-שומן לפני 6-שומן; זה אותו נימוק שבחר פחמימה ראשונה ב-12.09. ההדרות: אגוזים וחמאות אגוזים נושאת 18 כשירים מול מכסה 7 ואינה צרה; חמאה וסמנה סגורה בהכרעת 4ג - שומן כשיר פרווה בלבד; מרגרינה, מיונז ורטבים הם מוצרים מסחריים, כלומר נתיב 7מ ו-#56. `07_curation_candidates.py` אינו נערך - המועמדים נמדדים בשאילתה, קריאה בלבד, והוא נשאר כלי של בלוקים 3–4 | `PROGRESS.md` 8ש · `spec/05-food-db.md` §5.5 · `work/2026-09-20-block-8-fat.md` |
| 20.09.2026 | **#40 מוכרע ונסגר: מדד הדירוג לחלבון בשאילתת המועמדים של 8ח הוא `protein_g` כצפיפות, ו-`share` נשאר רצפה בלבד. `db/07_curation_candidates.py` אינו נערך** | `share` מודד רזון, ו-#32 מדד שמיון לפי רזון פורץ את רצפת הבטיחות בספייק. בבורר הוא מעדיף חזה הודו, חלבון ביצה ותחליפי בשר 99% על פני חזה עוף ועדשים - 805 עם 30.1 ג' חלבון במקום 19 מ-215 בעוף. החלופה הוצעה ב-02.09 כאפשרות (ג) ולא נבחרה אז. מבלוק 8 המועמדים נמדדים בשאילתה ולא ב-07, ולכן ההכרעה חלה על השאילתה, ו-07 נשאר כפי שהוא עם `share` - משתנה אחד לבלוק | `open-questions.md` #40 · `PROGRESS.md` 8ח |
| 20.09.2026 | **#41 מוכרע ונסגר: מתכון שיש לו רכיב אחד מעבר למים ומלח הוא הצורה המבושלת של חומר גלם, ומדורג בשאילתת המועמדים כחומר גלם ולא כמנה. 07 אינו נערך** | הבורר מטביע מתכון מתחת לכל חומר גלם (הכרעת 02.09), ולכן הצורה המבושלת של חומר גלם יחיד שוקעת אף שהיא בדיוק מה שנאכל. ב-8פ זה נעשה ביד - 2773 קינואה מבושלת ו-10127 קוסקוס מבושל בלי שמן נוספו ידנית ב-8פ-ד, כמו 825, 1206 ו-1638 שנוספו ביד בבלוק 4 - ומכאן זה כלל בשאילתה במקום הוספה ידנית פר-פריט | `open-questions.md` #41 · `PROGRESS.md` 8ח |
| 20.09.2026 | **#53 מוכרע ונסגר: מוסכמת המשפחה נשארת בגרמים כפי שנכתבה ב-11.09, והתקרות האפקטיביות מקובלות. גיליון התיוג מציג לכל פריט גם את התקרה האפקטיבית לפי `db/_effective_ceilings.py`, ומנגנון ההגשה ב-`spike/portions.py` אינו משתנה בבלוק 8** | 8ת-ו מדד בקריאת הפותר שהצמדת התקרות אינה משנה אף כמות מוגשת: פריט `whole_only` מוגש ביחידה אחת, שתיים או שלוש שאינן עוברות את התקרה, ולכן מה שקובע בפועל הוא היחידה ותקרת שלוש היחידות, ו-1,071.1 הגרם של עלות ההצמדה שנמדדו ב-8ת-א הם מספר על הנייר. מה שנשאר הוא שהבעלים יראה מה מוגש בפועל בשעת התיוג, וזה מה שהעמודה בגיליון עושה | `open-questions.md` #53 · `db/_effective_ceilings.py` · `PROGRESS.md` 8ש · 8ח |
| 20.09.2026 | **#48 - כלל למקטע, והשאלה נשארת פתוחה: אוצר האלרגנים נשאר שמונה. פריט שנושא אלרגן מחוץ לשמונה - תורמוס, סלרי, חרדל - יוצא לפי כלל הזהות ונרשם ב-#48; 1630 נשאר בחוץ** | הוספת אלרגן לאוצר אינה תיוג אלא שינוי מוצר: היא נוגעת באשף, ב-`spike/filters.py` ובאפיון, ולכן היא שייכת לשלב המוצר ולא לבלוק אצירה. הכלל הוא מה שמאפשר ל-8ש ול-8ח לרוץ בלי להכריע אותה, כפי ש-8פ ו-8י רצו בלעדיה | `open-questions.md` #48 · `spec/05-food-db.md` §5 |
| 20.09.2026 | **#49 מוכרע ונסגר: המנגנון הוא תיוג ישיר של מתכון לפי רכיביו (כלל 12.09), בלי שורות אצירה לרכיבי הבסיס. `v_recipe_unreviewed_components` ו-`v_recipe_inherited_allergens` נשארים מידע ורשימת בדיקה, ולא שער** | כך עבדו בפועל 6ח, 8פ-ח ו-8י: התיוג הישיר כיסה את המתכונים בלי לפתוח שורה לאף רכיב בסיס. פתיחת שורות נסקרות-בלבד ל-567 רכיבי הבסיס ש-#49 נוקבת בהם הייתה מוסיפה עבודה שאינה משנה תוצאה: ההורשה נדרשת רק כשרכיב נושא אלרגן שהמתכון אינו נושא, ובתיוג ישיר המתכון נושא את אלרגני רכיביו בהגדרה | `open-questions.md` #49 · `spec/05-food-db.md` §3.1 · `PROGRESS.md` |
| 20.09.2026 | **משפחת הזיתים נפתחת מחדש, שישה קודים: 4278 · 4279 · 4283 · 4284 · 4285 · 4288.** 4279, שהוצא מכשירות ב-4ג, חוזר - שורת האצירה שלו קיימת ומתעדכנת; חמשת האחרים נכנסים כשורות חדשות. **היעד הסופי של 8ש: שומן 37 → 43 כשירים, נטולי אלרגנים 11 → 17**, בכפוף לסקירת האלרגנים של 4284, והוא מחליף את היעד הזמני "37 → ~60" משורת 20.09.2026 שלמעלה, שנקבע במפורש כזמני עד המדידה. **4387 חמאה ו-4446 קנולה נשארים בחוץ**, במילות הבעלים מ-05.09: "עדיפות לשומן צמחי איכותי" · "שומן גרוע". שמנים ניטרליים נשארים בחוץ כמשפחה, ואבוקדו נשאר 1 מהמקור | §5 ב-`spec/05-food-db.md` מחייב עומק בזיתים, בשמן זית ובאבוקדו כמקורות שומן נטולי אלרגנים, ומדידת 8ש-א הראתה שאין במקור פריט אחר שממלא את זה: 143 מועמדים מעל רצפת המשפחה ב-15 משפחות השומן, 135 מהם כבר בטבלת "לא (136)" של בלוק 4, ובשש משפחות ההיקף 52 מועמדים ו-51 דחויים - כלומר כל תוספת לבריכת השומן היא פתיחה מחדש של הכרעת משפחה. כלל הבחירה: שורה גנרית (`source = ingredient`) לפני שורת מותג, אחת לכל צורה - גנרי 4278 · ירוקים 4279 · שחורים 4283 · ממולאים 4284 · ללא גלעין 4285 · מושחרים 4288. במשפחה שלוש שורות `ingredient` בלבד, ושלושתן ברשימה; לשחורים, למושחרים וללא-גלעין אין שורה גנרית במקור, ולכן נכנסות שלוש שורות מותג. בחוץ: 4290 ו-4281 כפילויות מותג · 4291 לבלוק 7 מ-30.08 · 8743 חריג `kcal` וכפילות צורה · 4280 ו-9358 מתכונים וכפילות זה של זה · 4287 שורת מותג שצורתה אינה חסרה. שורת 05.09.2026 על שלוש ההוצאות סומנה `↩ בוטלה`, ומה שעומד בה נכתב כאן | `open-questions.md` #57 · `work/2026-09-05-block-4-curation.md` · `db/block8_fat_codes.txt` · `PROGRESS.md` 8ש |
| 20.09.2026 | **#57 נסגר: בריכת השומן נטולת האלרגנים שבקובץ המשרד מוצתה, והמיצוי בפועל הוא פתיחת הזיתים.** עומק נוסף בשומן נטול אלרגנים יבוא רק מ-7מ, אחרי הכרעת #56 | ההצלבה נמדדה ב-8ש-א על כל 15 משפחות השומן, ולא רק על שלוש משפחות ה-p4 ש-#57 נקבה בהן: 143 מועמדים, 135 בטבלת "לא (136)" של בלוק 4, 1872 בכלל הקלייה של 11.09.2026, ושבעה בארבע המשפחות שמכסתן 0. 3224, הקוד ה-136 בטבלה, אינו מועמד כלל - הוא נופל ברצפת ה-`fat_g`. ההשערה של #57, שרוב הבריכה כבר נדחתה במפורש, אוששה. הסגירה הזו משלימה את מה ש-#16 דחה ב-7ב - "יעד עומק השומן בשער היציאה מתעדכן במקומה" - והמספר הוא 17 נטולי אלרגנים מתוך 43 | `open-questions.md` #57 · #16 · #56 · `db/block8_fat_landscape_families.txt` |
| 20.09.2026 | **תיוג משפחת הזיתים - ברירת מחדל אחת לששת הקודים, בלי חריגים:** `category` fat · `kosher` parve · `by_weight` · `max_g` 60 · `prep` 0 · `tags` `{vegan}` · `supp` false · `quality` ריק · `menu_eligible` false עד ההפיכה. **המידה היא גרמים ולא זית בודד** | גיליון 8ש-ג הציע יחידה של זית אחד, 2.9–4 ג', עם `whole_only`; הפותר מגיש שורת `whole_only` באחת, שתיים או שלוש יחידות (#53, נמדד ב-8ת-ו), כלומר תקרה אפקטיבית של כ-9 עד 12 ג' - פחות משני גרם שומן, שאינה מנת שומן. 60 ג' הם כ-15 זיתים, כתשעה גרם שומן וכ-90 קק"ל, וזו גם התקרה ש-4279 נושא מ-3ד - כלומר ההכרעה אינה חדשה לשורה שכבר הייתה כשירה פעם. `by_weight` ו-`whole_only` אינם חופשיים זה מזה: ה-CHECK `whole_only_requires_a_unit` ב-`db/01_food_db_schema.sql` אוסר על שניהם יחד, וכל 154 שורות ה-`by_weight` בטבלה נושאות `whole_only` false. שורת `by_weight` אינה נושאת יחידה כלל, והייצוא מאפס אותה (`spec/05-food-db.md` §5, שורת 31.08.2026 כאן). ב-4279 זה ה-`UPDATE` היחיד - `by_weight` false → true ו-`whole_only` true → false; שאר שדותיו כבר עומדים בברירת המחדל | `db/26_tag_fat.sql` · `work/2026-09-20-block-8-fat.md` · `PROGRESS.md` 8ש-ד |
| 20.09.2026 | **אלרגני ששת הזיתים: `allergens` `{}` לכולם, כולל 4284 "זיתים ירוקים, ממולאים". הכתיבה למסד במיגרציה 27 (8ש-ז), עם `allergens_reviewed_at` של 20.09.2026 לשישה** | חמישה מהשישה אינם מעוררים שאלה - זית הוא זית. 4284 כן, כי המילוי אינו נקוב בשם. שלושה נימוקים הכריעו `{}`: השם אינו נוקב במילוי; זו שורת מקור גנרית ולא שורת מותג שאפשר לקרוא את תוויתה, ולכן מסלול תנאי התווית של בלוק 7 אינו חל עליה; ובמאגרי המקור הזיתים הממולאים הם ממולאים בפלפל אדום, שאינו מבין השמונה. החלופה המחמירה - להוציא את 4284 מהשישה - נדחתה: היא מורידה את המשפחה לחמישה ומחזירה חלק מבעיית העומק שבגללה היא נפתחה מחדש. 4279 נסקר `{}` כבר ב-30.08 ונסקר שוב היום, ו-27 יכתוב לו חותמת חדשה | `db/26_tag_fat.sql` · `open-questions.md` #48 · `PROGRESS.md` 8ש-ה |
| 20.09.2026 | **#42 מוכרע ונסגר: שער היציאה של בלוק 9 הוא "לא נמוך מבסיס שנמדד באותו יום", ולא 92% קבוע.** ביום השער רצות שתי ריצות של 40 ייצורים - קודם על מאגר הזרע ואז על המאגר האמיתי, באותו יום ובאותו קוד - והמודל שבו רצו נרשם ב-`measurements.md`. הקריטריון: המעבר בניסיון ראשון על המאגר האמיתי אינו נמוך מריצת הזרע, ואפס כישלון מלא. השער קשיח, בלי סובלנות; הפרה היא עצירה לסקירה ולא ביטול אוטומטי | ה-92% מ-27.08.2026 נמדד על מודל שאינו רשום, הקוד השתנה מאז, ו-`temperature` אינו נשלח ל-API כלל; ב-03.09.2026 אותה מדידה נתנה 82% ואז 90% באותו יום. הרעש על 40 דגימות לא נמדד מעולם, ופרופיל אחד הוא 2.5 נקודות, ולכן סף קבוע עובר או נכשל על רעש ולא על המאגר. התקדים הוא שער הרגרסיה של 3ז4, הכרעת 03.09.2026, שנמדד לפני ואחרי באותו יום. שתי חלופות נדחו: `temperature: 0` לריצות רגרסיה הוא שינוי התנהגות של המחולל שלא נמדד, ותפריטי ייצור אינם רצים כך - אם רוצים אותו, זה בלוק משלו עם לפני ואחרי ולא הגדרת שער; וסובלנות סביב מספר היא סף קבוע בתחפושת. #47 - 40 הייצורים מול מאגר מסונן - נשאר לפתיחת בלוק 9 | `open-questions.md` #42 · `decisions.md` 03.09.2026 · `measurements.md` 3ז4 · `spec/05-food-db.md` §8 · `PROGRESS.md` שורה 9 ו"הגדרת סיום" |
| 20.09.2026 | **היעד הסופי של 8ח, על מדידת 8ח-א: כשירים ~150 (≥ 145) · טבעוני ≥ 30 · נטול אלרגנים ≥ 65. אף הכרעת משפחה מבלוק 4 אינה נפתחת מחדש.** היעד הזמני "108 → ~150, טבעוני 18 → ~35, נטולי אלרגנים 48 → ~65" משורת 20.09.2026 שלמעלה נקבע במפורש כזמני עד המדידה, ושני סעיפיו הראשונים עומדים כפי שהם; סעיף הטבעוני יורד מ-~35 ל-≥ 30 | מדידת 8ח-א הפוכה למדידת 8ש-א: מתוך 795 מועמדים **616 לא הוכרעו על ידי איש** - 485 בשמונה המשפחות שיש להן מכסה ו-131 ב-24 ו-25 שמכסתן 0 - ורשימת "לא (182)" של בלוק 4 מכסה 179 בלבד. בשומן כל 143 המועמדים היו מוכרעים, ולכן כל תוספת חייבה פתיחה מחדש; כאן אין צורך באף פתיחה. **14 דחיות הקטניות ו-8834:** תא הסיבה של בלוק 4 מונה צורה יבשה וגולמית (1633, 1631, 1637, 1655, 9718), קמחים (1718, 8270), אבקות מתחת לרצפה (1720, 1726) ושבבים יבשים וחריגי `kcal` (1727, 1729, 1731), ו-8834 גלוטן חיטה יבש בנימוק "צורה יבשה" - כולן עומדות בכלל "צורות נאכלות בלבד" של 29.08.2026, ולכן אין מה לפתוח. **1699** - סייטן פרוס בסגנון סלמי - נשאר בתור האימות כפי שבלוק 4 השאיר אותו, והוא פריט של 7מ. **למה ≥ 30 ולא ~35:** ההיצע הצמחי הנאכל במשפחה 21 הוא 34 מועמדים חדשים - 28 בלי אף דגל ושישה שנושאים חריג `kcal` בלבד - ואחרי "אחת לכל צורה" זה כ-28 מנות, כלומר תוספת סבירה של 12–16 טבעוניים; היתר תלוי ב-7מ, ב-1699 ובתחליפי בשר עם תווית, אחרי הכרעת #56. נטול האלרגנים מגיע מעוף ומבקר מבושלים ומקטניות שאינן סויה, ושתי המשפחות האלה נושאות 149 ו-89 מועמדים חדשים | `work/2026-09-20-block-8-protein.md` · `PROGRESS.md` 8ח |
| 20.09.2026 | **גיליון המועמדים של 8ח-ג: חמש משפחות במכסות 17:12 · 18:12 · 19:10 · 20:10 · 21:20, ו-16 · 22 · 23 · 24 · 25 בספירה בלבד. נדחי "לא (182)" אינם מוצגים. ארבעה פסים:** חומרי גלם עם `P4_CAP` 4 לכל p4 · פס מילוי באותו דירוג בלי התקרה · מתכונים מתחת לכל חומר גלם, ומתכון שרכיביו מעבר למים ומלח הם אחד מדורג עם חומרי הגלם (#41) · יבש וגולמי אחרון, ומוצג רק אם המכסה לא התמלאה. הדירוג הוא של 07 עם `protein_g` במקום מדד הרזון (#40); חריג `kcal` הוא מפתח מיון ודגל, לעולם לא הדרה. ברירות המחדל של `quality` וכשרות לפי משפחה, כמו בלוק 5 (11.09.2026): 17 ו-19 בשרי 3 · 18 פרווה 3 · 20 חלבי 3 · 21 פרווה, סויה 2 ושאר הקטניות 1 · 22 פרווה 3 · 16 כפי שהם; החריגים נדונים אחד-אחד ב-8ח-ד | המכסה 21 מוכפלת מ-10 ל-20 כי עומק הצומח הוא המגבלה הכובלת של היעד, והמשפחה נושאת 41 מועמדים חדשים; שאר המכסות הן אלה של §5.5 כלשונן. `quota` ב-07 היא תקרת דגימה לגיליון בריצה אחת - `select_candidates` מעביר אותה ל-`take_with_cap` ומחוץ לשתיהן ול-`write_table` היא אינה מופיעה - ולכן שינוי מכסה לגיליון אינו נוגע בכשירות ואינו משנה את §5.5, שמצוטט ולא נערך. פס היבש והגולמי הוא הפס השלישי של 8פ-ג2 בפחמימה: צורה שאינה נאכלת כמות שהיא אינה מודרת מהגיליון, אבל היא נכנסת אחרונה ורק אם נשאר מקום. אי-הצגת נדחי בלוק 4 היא מה שהכרעת היעד קבעה - אין פתיחה מחדש - ולכן הצגתם הייתה מזמינה סקירה חוזרת של מה שכבר הוכרע. מה שלא נכנס לגיליון נשאר לסבב שני | `db/block8_protein_candidates.tsv` · `spec/05-food-db.md` §5.5 · `PROGRESS.md` 8ח-ג |
| 20.09.2026 | **תיקון להכרעת הגיליון (השורה שמעל): מכסת משפחה 21 עולה מ-20 ל-34.** 34 הם כל 41 המועמדים החדשים של המשפחה פחות שבע הצורות היבשות, כלומר פסים 1 · 2 · 3 מוצגים במלואם ופס 4 נשאר מוסתר. משפחות 17 · 18 · 19 · 20 ללא שינוי | מכסה 20 שווה בדיוק לבריכת פסים 1+2 של 21 - 20 חומרי גלם - ולכן הגיליון הראשון נעצר על גבול הפס, ו-**14 המתכונים לא הוצגו כלל**: ארבע שורות הטופו (1686 · 1688 · 9173 · 1711), כל מנות העדשים והשעועית, ואיתן שתי השורות שנושאות `שמן סויה` בלבד. זה סותר את הבסיס שהיעד נקבע עליו: ≥ 30 טבעוני הוכרע ב-8ח-ב על 34 המועמדים הנאכלים של המשפחה, ו-14 מהם לא היו בגיליון שהסקירה אמורה לרוץ עליו. נימוק שני, מהמסד: לתחליפי הבשר המותגיים שכן הוצגו - 1796 · 1800 · 8865 · 8866 · 9758 · 1797 - אין רכיבים ב-`food_recipe_components`, ולכן תיוג טבעוני לפי #49, שהוא תיוג לפי רכיבים, אפשרי בעיקר על המתכונים שנשארו בחוץ. פס 4 נשאר מוסתר כי צורה יבשה אינה נאכלת כמות שהיא, וזו אותה הכרעה שעמדה בבסיס "אין פתיחה מחדש" של 8ח-ב. **טעות עיצוב של הצ'אט**, נתפסה ב-S1 של 8ח-ג על הפלט - הגיליון הראשון נכתב, נמדד ודווח, והתיקון הוא ריצה שנייה של אותו סקריפט עם מספר אחד שונה | `db/block8_protein_candidates.tsv` · `PROGRESS.md` 8ח-ג · `measurements.md` |
| 21.09.2026 | **הסקירה של 8ח-ד על 78 שורות הגיליון של 8ח-ג: 46 נכנס · 6 תנאי תווית · 26 יוצא.** שבעה כללים: (1) **אחת לכל צורה** - בכפילויות שמן, שמן זית לפני קנולה וגרסת "שמן סויה" לעולם לא; זוג בערכים זהים - אחד; קפוא מותגי יוצא כשיש גנרי ונכנס כשאין (8305) · (2) עוף "נאכל עם עור" נכנס - צורה נאכלת; בקר ועגל "נאכל עם שומן" יוצא - חיתוך בצלחת (616 · 749) · (3) ממרחים ותבלינים יוצאים - מיסו 8264, אנשובי 1147 · (4) נתון חשוד יוצא - 1621, 638; דגל `kcal` לבדו אינו מדיר - 8305 · 8261 · 1629 נכנסו · (5) קו שומן לגבינות 22% - 8609 ו-431 נכנסות, 471 · 426 · 417 יוצאות · (6) נאטו 8265 יוצא על זמינות · (7) חריגים לברירת המחדל של המשפחה: 1667 חלבי (סמנה ברכיבים) ולא טבעוני; 1711 לא טבעוני (ביצה), ונשאר פרווה `quality` 2 | הוכרע בצ'אט על הגיליון, משפחה-משפחה - 21 תחילה, כי היעד תלוי בה, ואז 17 · 18 · 19 · 20 - עם הצעה לכל שורה, ואושר כמוצע. ברירות המחדל של `quality` וכשרות הן אלה של הכרעת הגיליון מ-20.09.2026, והחריגים נדונו אחד-אחד כפי שהיא קבעה. **צפוי אחרי 29:** כשירים 154 · נטול אלרגנים 76 · טבעוני 31, מול היעד של 8ח-ב ≥ 145 · ≥ 65 · ≥ 30 - שלושתם מעליו, ובלי ששת תחליפי הבשר המותגיים (השורה הבאה) | `work/2026-09-20-block-8-protein.md` טבלת 8ח-ד · `db/block8_protein_codes.txt` · `PROGRESS.md` 8ח-ד |
| 21.09.2026 | **ששת תחליפי הבשר המותגיים של משפחה 21 - 1796 · 1797 · 1800 · 8865 · 8866 · 9758 - הם תנאי תווית.** מתויגים במיגרציה 28 - `quality` 2, פרווה, בלי `vegan`, `menu_eligible` false - ואינם נהפכים ב-29. רשימה נפרדת, `db/block8_protein_label_codes.txt`, בדגם `db/block6_protein_label_codes.txt`. עוברים ל-7מ אחרי הכרעת #56, יחד עם 1699 | אין להם רכיבים ב-`food_recipe_components`, ולכן תיוג טבעוני ואלרגנים לפי #49 - שהוא תיוג לפי רכיבים - אינו אפשרי עליהם, ורק התווית יכולה לומר. התקדים: 32 הפריטים של `db/block6_protein_label_codes.txt` - 31 מבלוק 4 ו-561 (12.09.2026). היעד הטבעוני ≥ 30 של 8ח-ב אינו נשען עליהם: הסקירה מביאה 31 בלעדיהם | `db/block8_protein_label_codes.txt` · `work/2026-09-20-block-8-protein.md` |
| 21.09.2026 | **הבהרה, לא ביטול - #53 (שורת 20.09.2026 "#53 מוכרע ונסגר"): התקרה האפקטיבית אינה עמודה בגיליון התיוג. היא מחושבת בצ'אט בשעת התיוג בנוסחת `db/_effective_ceilings.py` - min(floor(max_g / יחידה), 3) × יחידה - ומאומתת בסקריפט אחרי ההפיכה, כשער בסשן 10.** שאר ההכרעה עומדת: המוסכמת בגרמים, התקרות האפקטיביות מקובלות, ו-`spike/portions.py` אינו משתנה | הסקריפט קורא רק ייצוא בפורמט `spike/menu_foods.py` ובוחר רק שורות `menu_eligible`; ל-52 הקודים של 8ח אין שורת אצירה ואין `max_g` עד 28 ו-29, ולכן אינו יכול לרוץ עליהם לפני ההפיכה (נמדד ב-S1b של 8ח-ה). גם `db/15_tagging_sheet.py --compact` אינו מפיק עמודה כזו - 19 עמודות, בלי תקרה אפקטיבית | `open-questions.md` #53 · `db/_effective_ceilings.py` · `PROGRESS.md` 8ח-ו |
| 21.09.2026 | **התיוג של 8ח-ו על 52 השורות (46 + 6), `menu_eligible` false לכולן במיגרציה 28.** (1) **תקרות משפחה כבלוק 5:** בשר, עוף ודג 175 · מעושן 100 · שימורים 130 · קטניות ומנותיהן 200 · טופו וקציצות 150 · חטיף 60 · גבינות 100 · קוטג' 250 (הגביע) · מותגיים 150 · (2) **יחידה גדולה מ-`max_g` - `by_weight`:** 1012 · 751 · 776 · 777 · 9634 · 1336 · 9784 · (3) **יחידה קטנה מדי - `by_weight`:** 1316 סרדין, 18 ג', ששלוש יחידות שלו הן 54 ג'; ושלוש הכפות 520 · 1617 · 1625 · (4) **`whole_only` רק לחתיכה שלמה טבעית עד ~90 ג'** - כנף, שוק, ירך, קציצה, פרוסת גבינה, פרוסת מקרל, קופסה; נתחים, חזה, סטייק, פילה והודו במנות - יחידה בלי `whole_only`, כדגי בלוק 5 · (5) **קטניות בגרמים;** טופו כ-10141, קציצות כ-910 · (6) **`prep`:** בישול 2 · חימום 1 · מוכן 0 · (7) **`supp` false לכולן.** התקרה האפקטיבית לפי נוסחת `db/_effective_ceilings.py` לעשר שורות `whole_only` הכשירות, בטבלה ביומן. 1667 חלבי | **למה יחידה גדולה מהתקרה הולכת ל-`by_weight`, כפי שהקוד עושה (`spike/portions.py:40, 53–57`):** שורת יחידה בלי `whole_only` מוגשת בצעדי `UNIT_STEPS`, מחצי יחידה עד שלוש, ורק הצעדים שאינם עוברים את התקרה נשארים - ביחידות של 183 עד 310 ג' מול תקרה 175 נשארת אפשרות אחת, חצי יחידה. עם `whole_only` הצעדים הם 1 · 2 · 3, אף אחד מהם אינו מתחת לתקרה, והפותר מגיש את הקטן שבהם - יחידה שלמה, מעל התקרה. בשתי הצורות לא התקרה קובעת את הכמות, ו-`by_weight` מגיש בצעדי 5 ג' מ-30 עד `max_g`. התקדימים נקראו מהמסד: 1223 מעושן 100 ו-`prep` 0 · 10141 טופו 150 · 910 קציצות `whole_only` · 1674 חומוס קלוי 60; ותקרות החלבון הקיימות 175 ×59 · 150 ×21 · 100 ×20 · 250 ×9 · 200 ×7 · 60 ×7 · 130 ×2 | `work/2026-09-20-block-8-protein.md` טבלת 8ח-ו ו-8ח-ז · `db/28_tag_protein.sql` · `PROGRESS.md` 8ח-ו |
| 21.09.2026 | **האלרגנים של 8ח-ז על 52 השורות, לפי הטבלה ביומן:** עוף, בקר וקטניות `{}` · דגים `{Fish}` · חלב `{Milk}` · 1688 `{Soy}` · 1711 `{Egg, Gluten, Soy}` לפי רכיביו · 1667 `{Milk}` (סמנה). **שני צמתים:** 8509 `{Fish, Soy}` - שמן סויה בשם, לפי תקדים 11.09.2026 על שמני אגוזים ובוטנים, שהזיקוק שלהם אינו ידוע; 777 `{Egg, Gluten}` - "מצופה או מקומח" בשם, לפי תקדים 675 ושמרנות כלל הזהות. `{vegan}` ל-13 - כל הקטניות ומנותיהן חוץ מ-1711 (ביצה) ו-1667 (סמנה), לפי כלל 17.09.2026. **אפס יציאות #48.** ששת המותגיים: `allergens_reviewed_at` NULL, תגים `{}` בלי `vegan` - הלקח של 10126 ו-1791 | רשימות הרכיבים של 12 המתכונים שבין ה-52 נבדקו - כולם ב-46, ולששת המותגיים אין רכיבים. התקדימים נקראו מהמסד: 675 `{Egg, Gluten}`; 10126 ו-1791, מותגים מן הצומח, נושאים `{Egg, Gluten, Soy}` מהתווית ואין להם `vegan`. `vegetarian` אינו באוצר התגים - התג היחיד בשימוש הוא `vegan` - ולכן שבע השורות שסומנו (v), חמש הגבינות, 1711 ו-1667, מקבלות `{}`. **אחרי 29:** נטול אלרגנים 48 → 76, טבעוני 18 → 31 | `work/2026-09-20-block-8-protein.md` · `db/28_tag_protein.sql` · `PROGRESS.md` 8ח-ז |
| 21.09.2026 | **פורמט הסשן: דוח המצב מתמזג לפתיחת בלוק 9; תת-בלוק אחד עם שתי עצירות - S0 לפני קריאת ה-API הראשונה ו-S1 אחרי שתי הריצות; רשומה אחת לסשן ב-`measurements.md`, נכתבת בסגירה; טבלת כיול חדשה אינה נכתבת; הפרומפט מצטמצם לשערים ולהכרעות** | הוכרע בצ'אט לפני פתיחת הסשן האחד-עשר, ונבחן בו: דוח המצב רץ כפתיחה של 9, בלי עצירה משלו, והסשן נעצר ב-S0 וב-S1 בלבד. בטבלת הכיול של 8ח (`measurements.md`, סגירת 8ח-י) חמשת תת-הבלוקים של קלוד קוד נפלו ב-×0.3–×0.7 מאמצע ההערכה, ואחריהם 8ח-י ב-×0.35 ו-8ס4 ב-×0.21 | `PROGRESS.md` 9 · `measurements.md` |
| 21.09.2026 | ↩ בוטלה 22.09.2026 · **#20 ו-#31 אינם חוסמים את בלוק 9. לשער נבחרה החלופה השלישית של #31 - קבלת הפער ורישומו. שניהם נשארים פתוחים לבלוק תיקון משלהם בשלב ג', לפני ואחרי כמו 3ז4, ומספרי השער מזינים אותו** | #42 (20.09.2026) הגדיר מחדש את השער אחרי התוספות מ-03.09.2026 בשתי השורות - ב-#20 "מכריע לפני בלוק 9, וחוסם אותו", וב-#31 "מכריע לפני בלוק 9"; #31 נושא "חוסם: בלוק 9" מפתיחתו, 31.08.2026. תיקון לפני השער הוא שינוי קוד בתוך בלוק השער - משתנה אחד לבלוק. אותו קוד רץ על שני המאגרים, ולכן כישלון מלא על רצפת השומן במאגר האמיתי הוא בדיוק האות שהשער מודד. נמדד בשער: `FAT_BELOW_SAFETY_FLOOR` 26 פעמים ב-17 פרופילים במאגר האמיתי, ובניסיון האחרון של 8 מ-21 הכישלונות המלאים; בזרע פעם אחת | `open-questions.md` #20 · #31 · `spike/run_generation.py` · `spike/portions.py` |
| 21.09.2026 | **#35 ומיגרציה 30 רצים אחרי בלוק 9, ולא לפניו. מה שעומד משורת 19.09.2026 "7ת נסגר ומוזג" נכתב כאן מחדש: 7ת נסגר ומוזג. בלוק 7 נשאר פתוח על 7מ בלבד, חסום על #56 - נתיב הכניסה למוצרים מסחריים לא קיים, שלוש חלופות לא נוסו, והבעלים מכריע על מדידה. 7מ אינו מזיז את הצוואר הצר ואינו חוסם את שער היציאה של 9; 7מ אחרי הכרעת #56** | 30 נוגעת ב-`excluded_reason` של שורות לא כשירות בלבד - לא ב-`menu_eligible`, לא ב-`v_menu_foods` ולא בייצוא - והשער מודד את המאגר המיוצא. 30 היא שתי טרנזקציות, מחוץ לדפוס של `db/_apply_migration.py`, ודורשת עיצוב משלה; לקח 50 מחייב את ההכרעה ואת `spec/` יחד. שורת 19.09.2026 מקבלת ↩ על השורה כולה, כי הסדר שבה הוא הסעיף המבצעי, ומה שעומד בה נכתב כאן מחדש (הלקח מ-17.09.2026 ב-`CLAUDE.md` §8). מנימוק השורה ההיא עומדים גם: שורת 14.09.2026 "בלוק 7 מתפצל לשני תת-בלוקים" ושורות "מועמד ל-7מ"; ההבהרה על שורת 12.09.2026 - "7 רץ בכל שלב לפניו" חל על 7ת ולא על 7מ; וההכרעה המבצעית של שורת 12.09.2026 - 8 לפני 7, פחמימה ואז ירק. הסקר לפי העובדה לא מצא שורה נוספת שקובעת את הסדר #35 → 9 | `open-questions.md` #35 · `PROGRESS.md` 7 · 7מ |
| 21.09.2026 | **הגדרות שער היציאה של 9, נרשמו מראש ב-S0 לפני קריאת ה-API הראשונה.** (4a) כישלון מלא: `generate()` החזיר `None` והניסיון האחרון נפל בוולידציה או ב-`BAD_JSON` - מעשה המודל או המאגר; קריטריון (b) סופר אותם בריצת האמיתי. (4b) `API_ERROR` בכל ניסיון - 429, 529, timeout, חיבור - הוא כשל תשתית: נרשם בנפרד עם מזהה הפרופיל ואינו נספר ב-(b); אחרי שה-40 מסתיימים הפרופיל רץ פעם אחת נוספת ב-`--profile <id>`, על אותו מאגר ובאותו יום, ותוצאת הריצה החוזרת מחליפה אותו בספירה; סימטרי בשני המאגרים; `API_ERROR` שני בריצה החוזרת הוא עצירה לסקירה. (4c) קריסה לפני שה-40 מסתיימים היא עצירה ודיווח, והבעלים מכריע; אין ריצה חוזרת שקטה; הספירות נגזרות משורות הסטטוס של הפרופילים ולא רק מבלוק הסיכום, והדיווח אומר אם בלוק הסיכום קיים. (4d) קריאות ה-API נגזרות ואינן מודפסות, ונרשמות כנגזרות, עם הסייג של `API_ERROR`. (4e) שלב ב' נסגר רק על שלושת הקריטריונים של `spec/05-food-db.md` §8, ו"הגדרת סיום" ב-`PROGRESS.md` מושלמת לשלושה, עם המספרים שנמדדו | בקוד כל `None` נספר ב-"Failed entirely" (`spike/run_generation.py:154-157`), כולל `API_ERROR`, שאין אחריו ניסיון חוזר (`spike/generator.py:131-135`), ו-`BAD_JSON` בניסיון האחרון; `run_generation.py` אינו מדפיס מספר קריאות. #42 לא הגדיר כישלון מלא. ב-S0 נמצא ש-`spec/05-food-db.md` §8 מונה שלושה קריטריונים ו"הגדרת סיום" שניים. בריצות של 21.09 לא היה אף `API_ERROR`, אף ריצה חוזרת ואף קריסה | `spec/05-food-db.md` §8 · `PROGRESS.md` · `spike/run_generation.py` · `spike/generator.py` |
| 22.09.2026 | **#64 מוכרע ונסגר: כישלון השער אינו בנתונים.** על 24 הפריטים שנדגמו בריצה האמיתית, 20 מ-21 הכישלונות המלאים בני-השגה בהרפיה הרציפה גם תחת U₂ - שני פריטים למשפחה בארוחה - #11 לבדו ב-T2, על משפחת הפחמימה, ואף אחד ב-T1; כל 19 המעברים ב-T3; ועל רשת המנות האמיתית של הפותר קיים תפריט בתוך כל רצועות היום של הוולידטור ל-38 מ-40. המנגנון הוא קוד: `spike/filters.py:149-159` לוקח `items[:n]` לפי סדר הייצוא בפחמימה, בירק ובשומן - 36 מ-40 הפרופילים ראו אותם שישה לחמים (194–330 קק"ל בתקרה), ו-34 מ-40 את שלושת פריטי השקדים ואגוזי ברזיל; בזרע אותו קוד נתן ל-20 מ-40 הפרופילים אורז, פסטה, קוסקוס, בטטה ותפוח אדמה בתקרה של 400–510 ג' (405–524 קק"ל). תקרת החלבון נאכפת ב-40 מ-40. קבוצת רצפת השומן - #1 · 27 · 28 · 34 - היא #20: הרצפה בת-השגה פי 2.3–2.9 בתוך חלון הקק"ל. שבעת הפרופילים עם טוטל זהה בניסיונות רצופים אינם יושבים על אף חסם. חמשת ה-UNKNOWN_FOOD הם המודל משבש שם - ארבעה קיצורים וכפל מילה אחד | נמדד ב-9א, בקריאה בלבד, על `db/block9_db_40.txt` ועל ייצוא 09 טרי; ההגדרות - התקרה האפקטיבית, U∞, U₂, ה-joint והשכבות - בראש `db/_bound_landscape.py`. בודק עצמאי חישב מחדש כל LP בסימפלקס רציונלי מדויק, עם תעודות דואליות ו-Farkas, ומצא 0 הבדלים; תפריטי הרשת, 38 מ-40, שוחזרו בסגירה בסקריפט נפרד. הדוגם, ולא עומק המאגר, קבע מה המודל ראה: מאגר של 44 פחמימות ו-43 שומנים לא נדגם | `db/_bound_landscape.py` · `db/block9_landscape_db_40.txt` · `work/2026-09-21-block-9-gate.md` 9א · `open-questions.md` #64 · #20 · #31 · #63 |
| 22.09.2026 | **התיקון הוא קוד ב-`spike/`, בלי מיגרציה, ובלוק התיקון עובר לשלב ב', לפני הריצה החוזרת של השער.** משתנה אחד לכל צעד, לפני ואחרי כמו 3ז4, בשרשרת באותו יום על המאגר האמיתי: ריצת בסיס טרייה של 40, ואחרי כל משתנה ריצה אחת של 40 שה"אחרי" שלה הוא ה"לפני" של הצעד הבא; השער עצמו, זרע ואז אמיתי, רק בסוף. השרשרת היא באותו יום, ולכן כל סשן שממשיך אותה נפתח בריצת בסיס משלו - ה"אחרי" של סשן 13 אינו ה"לפני" של סשן 14, מאותה סיבה ש-#42 דחה בסיס קבוע. הסדר: (א) #20 - `fat_floor` לפותר; (ב) הדוגם - פחמימה, ירק ושומן נדגמים בגיוון בין תת-משפחות במקום `items[:n]`, והעיצוב ב-S0 של הצעד; (ג) התאמת שמות ב-`validate` או שם תצוגה בנתונים; (ד) משוב הניסיון החוזר. מיפוי `Eggs → Egg` ב-harness בתוך הבלוק, לפני הריצה החוזרת של השער; #63 נשאר פתוח. #31 אינו בשרשרת, ונבחן מחדש על המספרים של צעד (א). מה שעומד משורת 21.09.2026 "#20 ו-#31 אינם חוסמים את בלוק 9" נכתב כאן מחדש: שניהם לא חסמו את המדידה של 9; החלופה השלישית של #31 - קבלת הפער ורישומו - שימשה את השער; לפני ואחרי לבלוק התיקון. מה שאינו עומד: "בשלב ג'" - הבלוק בשלב ב'. השורה ההיא מקבלת ↩ על השורה כולה, כמו שורת 19.09.2026 ב-21.09.2026 | המספרים של 9א: #64 הצביע על קוד, והשער לא יעבור בלי שינוי קוד. משתנה אחד לבלוק (03.09.2026, 3ז4); השרשרת שומרת כל השוואה באותו יום, ב-n+1 ריצות לכל סשן במקום 2n. הסדר לפי ודאות האפקט: ארבעה פרופילים ודאיים ל-#20; הדוגם הוא המנוף הגדול ביותר (36/40 · 34/40); השמות 5 מ-98 קריאות; הניסיונות החוזרים 7 פרופילים. הסקר לפי העובדה מצא שורה אחת שנוקבת בשלב של בלוק התיקון - שורת 21.09.2026 על #20 ו-#31; שורות 03.09.2026 על 3ז4 ועל #35 עומדות, והשרשרת נשענת עליהן | `open-questions.md` #20 · #31 · #63 · #66–#70 · `spike/filters.py` · `spike/run_generation.py` · `spike/portions.py` · `PROGRESS.md` 9ג |

---

## 10.09.2026 - המזווה שלי

הרקע: שלושה שינויים שהוצעו ונדונו. שניים נדחו, אחד התכנס לפיצ'ר "המזווה שלי". השורות "חינם כולל" ו"מנוי כולל" למעלה אינן מבוטלות אלא מורחבות, ראה כאן.

| נושא | ההחלטה | מודול |
|---|---|---|
| המזווה שלי | המשתמש מנהל רשימת מזונות שיש לו בבית. המחולל **מעדיף** אותם - העדפה, לא סינון קשיח. המאגר המלא נשאר זמין, כך שהמספיקות לא נפגעת | `spec/03-functional.md` |
| רק מהבית | אופציה נפרדת, כבויה כברירת מחדל, בשלב המזווה ובמסך המזווה. במצב זה המזווה הוא סינון קשיח שעובר את בדיקת המספיקות; כשלא מספיק, ההודעה מציעה להוסיף פריטים למזווה או לחזור ל"העדפה" | `spec/03-functional.md` |
| מזווה באשף | שלב 8, אחרי "האוכל שלך". התפריט הראשון נבנה רק אחרי שהמזווה הושלם. השלב מגיע מלא מראש (סל ברירת מחדל + מה שסומן כאהוב בשלב 7) ונסגר בלחיצה אחת; מזווה ריק חוקי | `spec/03-functional.md` |
| רשימת קניות | יוצאת מרשימת ההקפאה. ההפרש בין פריטי התפריט הפעיל למזווה. גלויה **לכל החבילות, כולל חינם** (ב-`02-business.md` הייתה רשומה למנוי בלבד) | `spec/10-release.md` |
| שינוי במזווה | ניתן לעדכון בכל עת, בכל החבילות. אם התפריט מכיל פריטים שאינם במזווה: האפליקציה מציעה לעדכן את רשימת הקניות ומסמנת במסך התפריט אילו ארוחות כוללות מוצרים חסרים. הסימון נשאר כל עוד הפער קיים. **אין** הצעה לעדכן תפריט | `spec/03-functional.md` |
| עדכן תפריט | מנוי בלבד. ניתן ללחיצה רק כשעברו 24 שעות מהייצור האחרון, כולל התפריט הראשון. בלי לחיצה התפריט נשאר. מייצר את התפריט כולו מחדש עם העדפת המזווה הנוכחי. **לא נספר** ב-12 בקשות השינוי. בחינם נעול וגלוי | `spec/02-business.md` |
| חינם כולל (תוספת) | מזווה · רשימת קניות · סימון ארוחות עם מוצרים חסרים | `spec/02-business.md` |
| מנוי כולל (תוספת) | "עדכן תפריט" מהמזווה, פעם ב-24 שעות, מחוץ למכסה | `spec/02-business.md` |
| רישום מזון שנאכל מחוץ לתפריט | **נבחן ונדחה.** קיזוז של שאר היום מנוגד לעקרון אי-הענישה, וזו לוגיקת האיזון שהוקפאה | `spec/07-safety.md` |
| תפריט חדש כל יום | **נבחן ונדחה** כברירת מחדל. הוחלף ב"עדכן תפריט" יזום | `spec/02-business.md` |
| היקף | MVP: שלב המזווה באשף · העדפת מזווה במחולל · רשימת קניות. v1: מסך עריכת מזווה · הצעת עדכון רשימת קניות וסימון ארוחות חסרות · "עדכן תפריט" | `spec/10-release.md` |
