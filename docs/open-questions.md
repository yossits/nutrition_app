# שאלות פתוחות

**מה זה:** מה שעדיין לא הוכרע, מי צריך להכריע, ומה זה חוסם.
**עודכן:** 14.09.2026

שאלה שנסגרת עוברת ל-`decisions.md` ונמחקת מכאן. זה הקובץ היחיד בתיעוד שכן מתרוקן.

---

## חוסם השקה מסחרית

| # | השאלה | מי מכריע | הערה |
|---|---|---|---|
| 1 | מיצוב כ"מאמן לאורח חיים בריא" **או** צירוף דיאטן/ית קליני/ת כאחראי/ת מקצועי/ת? | עו"ד + בעלים | **ההמלצה: השנייה.** פותרת את הבעיה המשפטית וגם משדרגת אמינות. ראה `spec/08-legal.md` |
| 2 | שם המוצר ומיתוג — קשור למותג "יוסי צרפתי" הקיים או נפרד? | בעלים | תלוי בהכרעה על #1. המילה "תזונה" בשם היא סיכון |

---

## חוסם החלטות מוצר

| # | השאלה | מי מכריע | הערה |
|---|---|---|---|
| 3 | ספק סליקה — ישראלי או Paddle? | בעלים | השאלה הגיאוגרפית נסגרה 28.08.2026, ראה #11. נותרה בחירת הספק |
| 4 | תמחור 49 ₪ — לא נבדק מול השוק | בעלים | לא חוסם MVP. חוסם v1 |
| 5 | תמיכה — מי עונה למשתמש משלם? | בעלים | לא חוסם MVP (בודקים בלי תשלום). חוסם v1 |

---

## חוסם ביצוע

| # | השאלה | מי מכריע | הערה |
|---|---|---|---|
| 6 | בונה לבד או עם עזרה? | בעלים | קובע את הערכות הזמן ב-`spec/10-release.md` |
| 7 | עיצוב — מעצב/ת חיצוני/ת או להמשיך מאב-הטיפוס הקיים? | בעלים | חוסם את שלב ג'. ראה `spec/12-ui-ux.md` |
| 20 | **המבחן הכושל הקבוע.** `[TEST FAILED] valid menu → FAT_BELOW_SAFETY_FLOOR` ב-`run_spike.py` נכשל ב-HEAD ואינו רגרסיה של 3ג — אומת מול העתק נקי של `git show HEAD:spike/*`. מבחן שנכשל בקביעות ו"ידוע ככושל" הוא הדרך שבה שער רגרסיה מאבד את ערכו | בעלים | **להכריע:** לתקן, לתעד כמצופה, או להסיר. **חוסם: לא.** נפתח לפני שער הרגרסיה של שלב ג' **תוספת 03.09.2026:** מכריע לפני בלוק 9, וחוסם אותו — המקרה הקונקרטי הראשון הגיע. ב-3ז4ב, אחרי הסרת מסנן ה-`price`, פרופיל #35 הפך ל-runnable ונכשל בשלושת הניסיונות על `FAT_BELOW_SAFETY_FLOOR: 66g vs 67g minimum` — פער של גרם אחד. זו הירייה הראשונה של הרצפה בריצת ייצור. `run_generation.py` עדיין אינו מעביר `fat_floor` לפותר, ולכן הפותר לא ניסה לכוון אליה והוולידטור פסל בדיעבד |
| 22 | **`whole_only_requires_a_unit` נחלש.** האילוץ הוא `CHECK (NOT whole_only OR NOT by_weight)`. מאז ש-`by_weight` הפך nullable ב-3ד, שורה עם `whole_only = true` ו-`by_weight = NULL` מחזירה `false OR NULL` = NULL, ו-Postgres מקבל NULL כמסופק — כלומר האילוץ אינו תופס אותה. **אין נתיב לייצור:** הצירוף אפשרי רק בשורה שאינה `menu_eligible`, ושורה כשירה חייבת `by_weight NOT NULL` לפי `eligible_requires_menu_fields` | בעלים | לא תוקן במכוון — הוספת `by_weight IS NOT NULL` הייתה מפילה כתיבה חלקית באמצע האצירה. **חוסם: לא** |
| 21 | **מחלקת המכלים שהמקור מתייג לא עקבי.** המקור קורא לאותו אובייקט פיזי קופסה על טונה משומרת ו-שקית או אריזה על גבינה לבנה ועל טונה סטארקיסט. 906 ו-905 הוכשרו ב-3ג; 900 · 903 · 908 לא. נמדד דרך `choose_unit()` על כל המאגר: 95 פריטים בלי יחידה קבילה שיש להם שורת 900, 903 או 908 — 29 נוחתים על כף, 66 על משקל, 17 עם חלבון ≥ 15 ג' ו-51 עם שומן ≥ 10 ג'. שבעת המקרים השגויים שנסקרו ב-3ג נשענים על 900 ו-903 בלבד; 908 צנצנת מופיע על חמאות אגוזים, שם הכף היא היחידה הנכונה, ונכלל בהיקף בלי ראיה נגדו הדגל `container_21?` ב-`11_curation_sheet.py` ממומש בשני סעיפים, אבל הראשון ("קיבל יחידה מ-900/903/908") אינו יכול לירות כל עוד MENU_UNIT מדרג אותם כ-(None,"אריזה"). הכשרתם תדליק אותו בלי שינוי בגיליון. | בעלים | שלוש דרכים: `UNIT_BY_JUDGEMENT` פר פריט · הכשרת 900 ו-903 בטבלה, שפותחת 386 שורות שרובן באמת שקית · או כשירות מותנית בגרמים ובהיעדר חלופה. **חוסם: לא.** ב-3ד מטופל פר פריט אם אחד מהם נבחר. נפתח כשאלת כלל בבלוק 4 **תוספת 30.08.2026:** ל-1874 (גרעיני חמניות טחונים, חמאת גרעיני חמניות) יש שורת הגשה **אחת בלבד** — `צנצנת`, קוד 908, 200 ג'. נמדד ב-3ד2 מול `food_servings`. הכשרת 908 לא תפתור אותה: אין כף להעלות, והצנצנת עצמה תיתן מנה של 200 ג' חמאת גרעינים. שלוש החלופות למעלה אינן ממצות — יש מקרה רביעי, שבו הנתיב הוא `by_weight` + `max_g` ולא יחידה כלל. **תוספת 13.09.2026 (8י-ח):** בירק הדפוס נמדד, ואינו חריג פר-פריט. מ-74 פריטי הירק, `choose_unit()` מציע יחידה שאינה מנה ב-20 - חסה רומאית "עלה 11 ג'", אפונה סינית "יחידה 3.4 ג'", כרובית "יחידה בינונית 226 ג'", כרוב ניצנים מבושל "יחידה בינונית 10 ג'". כל 20 סומנו `whole_only = false`, והן 20 מתוך 21 הפריטים שהגיליון הציע להם יחידה ספירה והתיוג לא סימן `whole_only` - העשרים ואחד, 3774, עבר ל-`by_weight` - וכל 17 ה-`whole_only = true` תואמים - כלומר המדרג נכון בדיוק כשהיחידה הספירה היא המנה, ושגוי כשהיא פיסה. 20 רשומות ב-`UNIT_BY_JUDGEMENT` הן שינוי ברשומות קיימות בייצוא, כלומר #51, ולכן הן נכנסות לתת-הבלוק של #50 ו-#51 אחרי 8י ולא ל-8י-ט. `whole_only` ו-`max_g` אינם תלויים ביחידה ונכתבו נכון **תוספת 14.09.2026 (8ת-א):** נמדד על כל 264 הכשירים ב-`db/_audit_units.py`: 22 שורות נושאות יחידה ספירה - דרגה 1 או 6 ב-`MENU_UNIT` - עם `whole_only = false`, כולן ירק ואפס בשאר הקטגוריות. 20 עם `by_weight = false`, והן נעבדות ב-8ת-ה ב-`UNIT_BY_JUDGEMENT`. 3774 כרוב אדום ו-3807 חסה אמריקאית, `by_weight = true`, אינן נעבדות: 09 מאפס את היחידה בשורת `by_weight`, ולכן רשומה ב-`UNIT_BY_JUDGEMENT` לא תשנה דבר. 3807 נרשם כאן לראשונה. אחרי שהשיפוטים ייכנסו העשרים יישאו יחידת מנה, והשאלה אם `whole_only` צריך אז להיות true נפתחת עליהן - משתנה שני, מחוץ להיקף **תוספת 14.09.2026 (8ת-ה):** עשרים השיפוטים נכנסו ל-`UNIT_BY_JUDGEMENT` של 09 - שורת הכוס או המנה במקום פיסה, מסלול "כוס או מנה לירק קצוץ ומבושל" של הכרעת התיוג מ-13.09 - ושער `--expect-changed` על 20 הקודים, שדה `unit` בלבד, יצא 0. הצעת `whole_only` בגיליון התיוג (15) עברה מ-true ל-false בכל העשרים, ועכשיו היא מסכימה עם האצירה בכולם - הראיה הישירה לממצא 8י-ח, שהמדרג נכון כשהיחידה הספירה היא המנה ושגוי כשהיא פיסה. נשארים פתוחים: מחלקת המכלים 900 · 903 · 908, והשאלה אם `whole_only` צריך להיות true בפריטים שנושאים עכשיו יחידת מנה |
| 24 | **סשן קלוד קוד לכל בלוק, או סשן מתמשך.** היום כל הבלוקים רצים בסשן אחד. שתי תוצאות נמדדו ב-3ד1: הרפלוג נותן חותמת פתיחה פעם אחת לסשן ולא לבלוק, ולכן בלוק בעל קומיט יחיד אינו ניתן למדידה מהגיט; ונוסחים שהוחלפו נשארים בהקשר, ולכן פרומפט חייב לנקוב בטקסט המוחלף בשמו | בעלים | סשן לבלוק פותר את שניהם בלי מנגנון. המחיר: `CLAUDE.md` נטען מחדש והבנת הריפו מצטברת מאפס. **חוסם: לא** **נקודת נתונים 03.09.2026:** עשר הרצות קלוד קוד בסשן אחד, worktree אחד, ענף אחד, ארבעה קומיטים, על פני עשר שעות שעון קיר וסך 1:04:00 עבודה. ארבע עצירות, כולן דווחו והוכרעו בצ'אט. לא נצפתה בעיית הקשר, אבל שלב התכנון שלפני 3ז1 (כשעה) אינו מופיע בשום שעון של הבלוק |
| 25 | **כפות בלתי אפשריות בזרעים.** 1880 (גרעיני חמניות בלי קליפה, קלויים, עם מלח) נושאת `כף = 2.7 ג'`, בעוד 9631 — אותו גרעין, קלוי, ללא מלח — נושאת 13. 1869 (גרעיני דלעת) נושאת `כף = 3.7` ו-`כפית = 1.5`. נמדד ב-3ד2 מול `food_servings`. סתירה פנימית במקור, לא שיפוט. **המשמעות:** כדי להגיע ל-15 ג' שומן מ-1880 הפותר נדרש ל-11 כפות — מדד "כמויות אבסורדיות" ב-`measurements.md`. `max_g` אינו חוסם זאת: הוא חוסם גרמים, לא מספר יחידות | בעלים | **שלוש דרכים:** סף גרמים-ליחידה לפי משפחה כשער אוטומטי · שיפוט פר פריט באצירה · או התעלמות מודעת ורישום. **חוסם: לא** — שני הפריטים לא נבחרו ל-3ד2. נפתח בבלוק 8 |
| 26 | **אין שחזור לאצירה מחוץ ל-Supabase.** `food_curation` שורדת `TRUNCATE ... RESTART IDENTITY CASCADE` — ההחלטה מ-28.08 עובדת ומבחן הקנרית הוכיח אותה. אבל היא שורדת *בתוך* המסד, ואין בריפו שום ארטיפקט שמשחזר אותה. היום 8 שורות; בסוף שלב ב' 300–400, שהן ימי שיפוט אנושי שאין להם מקור שני | בעלים | **שלוש חלופות:** גיבוי Supabase מתוזמן · קובץ seed **מיוצר מהמסד** בסגירת בלוק, כמו `09_export_menu_foods.py` · או קבלת הסיכון ורישומו ב-`spec/11-risks.md`. **מה כבר נשלל:** seed מתוחזק ביד — מקור אמת שני שיסטה, אותו נימוק שהפיל את טבלת "מה נשאר" ב-29.08. **חוסם: לא.** גדל בכל בלוק |
| 27 | **`complete` מודד כיסוי נתונים, לא שלמות חלבון.** נמדד: 805 חזה עוף צלוי נושא **אפס** שורות חומצות אמינו ו-44 שורות תזונה; 8193 לב הודו נושא 9 ו-60. 8712 מי גבינה נושא 5 שורות תזונה בסך הכול. חזה עוף ומי גבינה הם חלבונים מלאים בכל אמת מידה תזונתית. §5.1 משתמש ב-`complete` לסידור הדגימה לאומניבור, ולכן הדגל מייבא הטיית כיסוי-נתונים של המקור אל תוך הפרומפט | בעלים | **חוסם: לא** |
| 28 | **"לא מבושל" הוא אח של `dry?` בלי דגל.** §5.0.2 מכסה צורה יבשה מול צורה מוכנה. גולמי מול מבושל היא אותה הבחנה במילה אחרת ואין לה מסנן. שלושה מ-65 מועמדי החלבון נושאים "לא מבושל" בשם — 989, 8195 ו-1574 — וביניהם 1574 חלבון ביצה גולמי שהגיליון הציע במקום 1575 המבושל. בנוסף 1562 "ביצה שלמה עם קליפה" היא מוסכמת שקילה ולא מזון, ו-1132 סורימי "לתעשייה" הוא חומר גלם | בעלים | **חוסם: לא** **תוספת 02.09.2026:** בין 48 הנכנסים ב-3ו, 11 גולמיים לפי שם — "לא מבושל" או "אדום אדום". דגל `raw?` נכנס ל-3ו2. **תוספת 02.09.2026 (ב):** הדגל קיים — `raw?` מ-3ו2ב ו-3ו2ד: "לא מבושל", "טרי" ו"קפוא"-בלי-"מבושל" במשפחות הבשר; 18 שורות בגיליון של בלוק 4. מה שנשאר פתוח: מה האצירה עושה עם שורה גולמית — דוחה, או מעדיפה את הצורה המבושלת. **תוספת 05.09.2026:** בבלוק 4 הבעלים דחה כל צורה גולמית בעוף, בבשר, בדגים ובקטניות, ואישר בכוונה שתיים בביצים (1561, 1574) — `prep` יסמן בישול. בפועל: גולמי נדחה, אלא אם הבעלים מכריע אחרת |
| 29 | **מי קובע מה זו "יחידה סבירה", ולפי מה.** בפרוסת הרוחב (8 פריטים) הבעלים מכריע פריט-פריט. בבלוק 8 (300–400 פריטים) שיפוט ידני אינו בר-ביצוע. שלוש חלופות: תקרת גרמים ליחידה פר קטגוריה · יחס בין היחידה ל-`max_g` · המשך שיפוט ידני עם רשימת חריגים | בעלים | **מכריע לפני בלוק 8. חוסם: לא.** הכלל מספק את "המקרה הרביעי" שנרשם ב-#21 ב-30.08 — הנתיב `by_weight` + `max_g` כשאין יחידה להעלות. אינו סוגר את #21 |
| 30 | **האם `servings` מסונן לשורת `by_weight`.** התיקון מ-31.08 מאפס את `unit` ואינו נוגע ב-`servings`, ולכן המודל רואה ל-3807 חסה `עלים = 20.0g` ועלול להרכיב סביב עלים. שלוש חלופות: לסנן `servings` לשורות `by_weight` · להשאיר ולהסתמך על הפותר · להציג גרמים בלבד | בעלים | **לא מוכרע לפני הקנרית במכוון** — משתנה שני מטשטש כישלון. **חוסם: לא.** להיבחן על פלט 3ה **תוספת 31.08.2026 — נבחן ולא נסגר.** בריצה 2 המודל בחר את 3807, והפותר נחת על **55.0 ג' בדיוק**. שורות ההגשה של 3807 הן `עלים = 20.0` · `כוסות קצוץ = 55.0` · `יחידות גדולות = 514.0`, כלומר 55.0 הוא ערך שורת ההגשה השנייה במדויק. הניסוח שיצא תקין — גרמים, כמתחייב מ-`by_weight` — אבל ההתאמה מחשידה. **לא הוכרע** אם המודל עגן על שורת ה-`servings` שהוא רואה, או שהפותר נחת שם עצמאית: `_options()` ל-`by_weight` מחזיר כפולות של 5 מ-30 ועד `max_g`=100, ו-55 הוא אחד מהן. **#30 מתחזקת, אינה נסגרת** |
| 31 | **`feasible()` בודק את הכיוון ההפוך.** הוא שואל אם יעד החלבון יקר מדי בקלוריות — `kcal_needed = day_protein / best.protein * best.kcal` מול 92% מהתקציב — ואינו שואל אם אפשר למלא את הקלוריות. נמדד ב-3ה5 על #66: הוא החזיר `True` ברווח פי ארבעה (697 מול 2,844), בזמן שאספקת 167 ג' חלבון מטונה דורשת 544 ג' מול תקרת מנה של 151 ג', כלומר 3.6 מנות מול 3 ארוחות. הוא מניח גרמים בלתי מוגבלים ואינו מכיר `max_g`, `whole_only` או מספר ארוחות. 5 מ-36 הפרופילים שעוברים את השערים נמצאים בתקרה או מעליה בהופעה יחידה | בעלים | **חוסם: בלוק 9** — שער היציאה נמדד ב-40 ייצורים, ופרופיל שעובר שער ולא ניתן לסיפוק מזייף את המספר. שלוש חלופות: שער תקרה חדש לצד `feasible` · הרחבת `feasible` שתכיר תקרות · או קבלת הפער ורישומו **תוספת 03.09.2026:** המקרה הקונקרטי הראשון — #35 עבר את `feasible()` ונכשל על רצפת השומן, 66 מול 67, בשלושת הניסיונות. `feasible()` בודק חלבון מול קלוריות ואינו מכיר את רצפת השומן כלל; מכריע לפני בלוק 9. |
| 32 | **מה בוחר בין שתי רמות שומן של אותו מזון.** נפתח מהחלטת הקוטג' מ-31.08: 3% ו-5% ייאצרו שניהם. **אין היום מנגנון** — `eligible()` אינו מכיר רמת שומן, ו-`sample_for_prompt` ממיין חלבון לפי `supp`, `quality` ואז רזון, כך ששתי הרמות ייבדלו רק בטייברייקר הרזון. **מיון לפי רזון כבר נוסה בספייק ופרץ את רצפת הבטיחות ב-5 תפריטים** — ראה מדידת 27.08. הבעיה גדלה בבלוק 8: היא חלה על כל מזון שיש לו יותר מרמת שומן אחת | בעלים | **מכריע לפני בלוק 8.** קשור ל-[#27] — `quality` אינו מבחין בין השתיים. **חוסם: לא** |
| 33 | **`resolve()` נופל על הפרש של תו אחד.** בריצה 2, ניסיון 2 נכשל ב-`UNKNOWN_FOOD`: המודל כתב את 2659 כ-`ללא תוספת שומן בבישול` והמאגר מחזיק `ללא תוספת שמן בבישול`. **המקור עצמו אינו עקבי** — 2721, 3459 ו-9793 באמת כותבים `שומן`, כך ש-2659 הוא היחיד מבין ארבעת פריטי הפחמימה שכותב `שמן`, והמודל יישר לצורה הרווחת. `resolve()` מטפל בסוגריים ובאותיות גדולות בלבד, לא בקרבה. המחיר נמדד: ניסיון אחד מתוך שלושה בוזבז | בעלים | שלוש חלופות: התאמה מקורבת ב-`resolve()` · נרמול השמות במאגר, שהוא שינוי נתוני מקור · או אזכור מפורש בפרומפט. **חוסם: לא**, אבל הוא אוכל ניסיונות מתוך `MAX_ATTEMPTS` |
| 34 | **`max_g` נאכף פר-ארוחה ולא פר-יום.** `portions._options()` הוא המקום היחיד שקורא אותו, והפותר מחזיק שורה לכל מופע בכל ארוחה. פרומפט המחולל מתיר פריט בעד שתי ארוחות (כלל 8) ואף שורת קוד לא אוכפת גם את זה. **נמדד בריצה 2:** שמן זית 30.0 ג' פעמיים = 60.0 ג' ליום מול `max_g`=40, כלומר 150% מהתקרה היומית, כששתי המנות בנפרד חוקיות | בעלים | **מכריע לפני בלוק 8**, כי הוא קובע מה `max_g` בכלל אומר לפני שנקבעים 300–400 ערכים. קשור ל-[#29] ול-[#25]. **חוסם: לא** |
| 35 | **`excluded_reason` — אין ערך ל"אינו מוצר מדף".** ל-enum `exclusion_reason` שני ערכים בלבד: `ffq` ו-`non_protein_nitrogen`. 493 יושב היום עם `menu_eligible = false` ו-`excluded_reason` NULL, ולכן הנימוק חי ב-[`decisions.md`](decisions.md) בלבד. **הסיכון הוא בבלוק 8:** אם `is_excluded` בבורר יקרא את השדה, 493 עשוי לחזור ולהיות מוצע. זו המחלקה הראשונה מסוג "המוצר אינו קיים בשוק", ויהיו רבות כמותה | בעלים | **מכריע לפני בלוק 8. חוסם: לא** **תוספת 05.09.2026:** בלוק 4ג יוסיף שלושה מופעים (4279, 4387, 4446). ובנוסף — 318 פריטים שנסקרו ונדחו בבלוק 4 אינם רשומים במסד אלא ב-`work/2026-09-05-block-4-curation.md` בלבד; בבלוק 8 הבורר יציע אותם שוב. מכריע לפני בלוק 8 **תוספת 05.09.2026 (ב):** 4ג בוצע — 4279, 4387, 4446 יושבים `menu_eligible = false` עם `excluded_reason` NULL. ארבעה מופעים במסד, עם 493 **תוספת 17.09.2026 (7ת-ב1-2):** קטגוריית החרגה שלישית אחרי שני ערכי ה-enum קיימת בהכרעה - תערובות להכנה מהירה שמוכנות במים (2807 · 8966 · 8969). לשלוש הפחמימות אין שורת אצירה, ולכן לא נכתב להן דבר; ערך מועמד כשה-enum יורחב (`decisions.md`, 17.09.2026) |
| 36 | **`id` בייצוא הוא מיקומי.** `enumerate(foods, 1)` על שאילתה ממוינת, ולכן כל כתיבת אצירה ממספרת מחדש את זנב הקובץ. ב-3ח: 25 שורות `id` מתוך 89 שורות שהשתנו. נמדד שאף מודול ב-`spike/` אינו קורא את השדה — `filters`, `portions`, `engine`, `generator` ו-`food_source`, אפס התאמות — ולכן אין נזק היום. בבלוק 8, עם 300–400 רשומות, כל כתיבה בודדת תייצר רעש על פני כל הקובץ ואף diff לא יהיה קריא | בעלים | האם להחליף ל-`source_code`. **חוסם: לא** **11.09.2026:** שער הדלתא (`measurements.md`, בלוק 5) משווה לפי `source_code` ומתעלם מ-`id` - השאלה נעקפת ונשארת פתוחה כשאלת קוד ב-09 בלבד |
| 37 | **ארבעה סקריפטי `db/` לא נבדקו תחת `psycopg` 3.3.5.** בלוק 3י הוכיח את נתיב הייצוא נייטרלי תחת 3.3.5. `03_load_source.py`, `04_transform.py`, `07_curation_candidates.py` ו-`11_curation_sheet.py` לא הורצו. `04_transform.py` הוא היחיד מביניהם שכותב ל-`foods`, ולכן הוא זה שחשוב שירוץ תחת שער | בעלים | **לפני בלוק 4. חוסם: לא** **תוספת 02.09.2026:** 07 ו-11 הורצו ב-3ו תחת ה-`.venv` של הפרויקט, `psycopg` 3.3.5, ארבע ריצות, exit 0. נותרו 03 ו-04. **תוספת 03.09.2026:** 09 הורץ תחת 3.3.5 שלוש פעמים — ב-3ז2 ופעמיים ב-3ז4ג (לפני המיגרציה ואחריה), exit 0 בכולן, ופלט זהה בית-בית בשתי האחרונות. 07 ו-11 הורצו שוב ב-3ז2 וב-3ז4ג. **נותרו 03 ו-04 בלבד**, ו-`04_transform.py` הוא היחיד שכותב ל-`foods` **תוספת 04.09.2026:** 07, 09 ו-11 רצו שוב תחת 3.3.5 ב-3ז' (ארבע ריצות של 07/11, שתיים של 09), exit 0 בכולן. נותרו 03 ו-04 |
| 38 | **נוסחת שער הרגרסיה חיה מחוץ לריפו.** שתי שורות הפקודה שמייצרות את 4,150 ואת 6,376 אינן ב-`docs/`, אינן בהיסטוריית הגיט, ואותרו ב-3י רק מתמליל סשן Claude Code. שער שהנוסחה שלו אינה בריפו יכול לאבד את משמעותו בלי שאיש ישים לב | בעלים | האם להנציח אותן, ובאיזו צורה. **חוסם: לא** **תוספת 02.09.2026:** שני מופעים נוספים — הערכת הזמן של 3ו (20–30 דק') נאמרה בצ'אט בלבד ו-`measurements.md` נשען עליה; והגיליון של בלוק 4 חי מחוץ לגיט, ורק `PROGRESS.md` מצביע עליו עם sha256. **תוספת 03.09.2026:** חצי מהשאלה נסגר. **נוסחת מסלול הזרע נמצאה ונרשמה** ב-`measurements.md` — `cd spike; PYTHONIOENCODING=utf-8 <python> -B run_spike.py > <file>`; בלי משתנה הקידוד מתקבל 4,148 ולא 4,150. שורת 40 הייצורים הייתה רשומה כל הזמן ב-`spec/09-architecture.md:197`. **המדד 6,376 יצא לגמלאות** ואינו דורש נוסחה. **מופע חדש:** התלות ב-`PYTHONIOENCODING` עצמה אינה בשום מקום בריפו, וכך גם התלות ב-CRLF ובספריית העבודה. השאלה שנשארת: האם נוסחת שער נרשמת כתיעוד או כסקריפט שניתן להריץ **תוספת 04.09.2026:** שני מופעים נוספים. (1) הכלל שמסתיר את `.claude/worktrees/` יושב ב-`.git/info/exclude:7` — מקומי, לא בריפו; שיבוט טרי לא יכיר אותו. (2) מדד `--source db` של `run_generation.py` תלוי ב-`spike/menu_foods.py`, שאינו בגיט ושונה בין worktrees: 26 פריטים ב-`curation-decision-sheet-d95a3f` (31.08) מול 27 ב-`block-3d2-protein-section-6632bd`, ובריפו הראשי אין קובץ כלל — `d2c3fe9f…` מול `fd893a7e…`. הכלל מ-03.09 סגר את המקרה בריצה טרייה של 09; השאלה — תיעוד או סקריפט — נשארת, ועכשיו עם תלות שלישית: איזה worktree **תוספת 05.09.2026:** מופע נוסף, ונסגר באותו יום — מריץ המיגרציות של CLAUDE.md §5 חי ב-`_scratch` בלבד, קידד קשיח את נתיב ה-SQL והועתק לכל מיגרציה (`apply_13.py` ב-3ז4ג → `apply_14.py` ב-4ג). ב-4ה נכנס לריפו כ-`db/_apply_migration.py`, קומיט `c7e804c`. השאלה הכללית — תיעוד או סקריפט — נשארת, ועכשיו יש לה תקדים אחד לכיוון "סקריפט" **תוספת 12.09.2026:** מופע נוסף - sha256 של קובץ נתונים בריפו תלוי ב-`autocrlf`: `db/block8_carb_landscape_p4.txt` נכתב ב-LF ונרשם `4996be9b…`, והעותק בדיסק עשוי לצאת CRLF. החתימה מוגדרת על תוכן האינדקס, `git show <commit>:<path>`, לא על העותק בדיסק **תוספת 13.09.2026:** מופע נוסף - שער ה-`--source db` (53 · `fb55ecb5…`) הוא stdout ב-UTF-8; בלי `PYTHONIOENCODING=utf-8` הקונסולה כותבת cp1252 ומתקבל 51 · `6183e3b8…`. אותה תלות של מסלול הזרע. `run_generation.py` יוצא 1 בהגדרה, חסר מפתח, וה-stdout הוא הנמדד |
| 39 | **טבלת "Where each of the twelve stopped" ב-`11_curation_sheet.py` מודפסת רק כשאף אחד מהשנים-עשר לא נבחר** (`if not hit`, שורה ~640). מאז 3ו 1699 נבחר, הטבלה נעלמה, ומקומם של 11 האחרים אינו מדווח. התנהגות שקדמה ל-3ו | בעלים | להדפיס תמיד, עם שורת SELECTED למי שנבחר. **חוסם: לא** |
| 40 | **מדד הדירוג לחלבון: `share` או `protein_g`.** `share` הוא חלק החלבון מהקלוריות, כלומר רזון, ו-#32 כבר מדד שמיון לפי רזון פרץ את רצפת הבטיחות בספייק. בבורר הוא מעדיף חזה הודו, חלבון ביצה ותחליפי בשר 99% על פני חזה עוף ועדשים: 805, 30.1 ג' חלבון, במקום 19 מ-215 בעוף. החלופה: `protein_g` כצפיפות, עם `share` כרצפה בלבד. הוצע ב-02.09 כאפשרות (ג) ולא נבחר | בעלים | **מכריע לפני בלוק 8.** 3ו2 אינו נוגע במדד. **חוסם: לא** **תוספת 12.09.2026:** נדחה למקטע החלבון של בלוק 8. 8פ (פחמימה) אינו נוגע ב-07, שאין לו ענף פחמימה - המועמדים נמדדים בשאילתה |
| 41 | **מתכון של רכיב יחיד הוא צורה מבושלת של חומר גלם, והבורר מטביע אותו כמנה.** 25 כאלה במשפחות עם מכסה (17 בדגים), אחרי התעלמות ממלח ומים; בלעדיה 8. חידוד שנמדד ב-3ו2ג החליף 3 מ-119 ולא הזיז `raw?`, ונדחה. במאגר של 300–400 המשקל עשוי להשתנות | בעלים | **מכריע לפני בלוק 8.** בלוק 4 רשאי להוסיף ביד: 825, 1206, 1638. **חוסם: לא** **תוספת 05.09.2026:** 825, 1206, 1638 נוספו ביד ואושרו בבלוק 4 **תוספת 12.09.2026:** נדחה למקטע החלבון של בלוק 8, כמו #40 **תוספת 13.09.2026:** שני מופעים בפחמימה - 2773 קינואה מבושלת ו-10127 קוסקוס מבושל בלי שמן קיימים רק כמתכון של רכיב יחיד, ונוספו ביד ב-8פ-ד כמו 825 ו-1206. ההכרעה הכללית נשארת למקטע החלבון של 8 |
| 42 | **הגדרת שער היציאה: 92% קבוע, או "לא נמוך מבסיס שנמדד באותו יום".** `PROGRESS.md` ו-`decisions.md` #7 מגדירים את שער היציאה כ"40 ייצורים ב-92%". המספר הזה נמדד ב-27.08 על מודל שאינו רשום. 03.09 נמדד פעמיים על אותו מסלול זרע: 82% לפני השינוי, 90% אחריו. הרעש עצמו לא נמדד מעולם, ו-`temperature` אינו נשלח ל-API כלל, ולכן שתי ריצות על אותם 40 פרופילים אינן חייבות לתת אותו מספר | בעלים | **מכריע לפני בלוק 9.** שלוש חלופות: סף מוחלט חדש · "לא נמוך מבסיס אותו יום" כפי שנעשה ב-3ז4 · או `temperature: 0` לריצות רגרסיה, שהוא שינוי התנהגות בפני עצמו. **חוסם: לא** |
| 43 | **הערה בקוד שנושאת תאריך — דפוס רצוי או לא.** ההערה שנכתבה ב-3ז3 מעל שורת `Food source` היא הראשונה שמתארכת שינוי התנהגות: `Unconditional since 03.09.2026`. תאריך בהערה מספר למי שקורא מתי הדבר השתנה בלי לחפש ב-`git blame`, אבל הוא גם טקסט נוסף שיכול להתיישן | בעלים | להכריע אם זה דפוס. אם כן — היכן הוא נדרש ואיפה מיותר. **חוסם: לא** |
| 44 | **`db/10_menu_fields_check.sql` התיישן והוחלף על ידי 13, ואין דרך לראות זאת בקובץ.** 10 עדיין מחיל את גרסת שלושת השדות של `eligible_requires_menu_fields` כולל `price`, וההערות שלו (שורות 6, 10, 25, 60) מנמקות דרישה שהמוצר הפך. 01 מפנה אליו בשורה 215 כמי ש"מחיל אותו על מסד חי". מיגרציה שהוחלה אינה נערכת — זה כלל — אבל קורא שמגיע ל-10 אינו יודע שהוא היסטוריה | בעלים | האם לסמן מיגרציות שהוחלפו, ואיך: שורת כותרת "superseded by 13" בלי לגעת בגוף · טבלה ב-`spec/05` · או כלום. **חוסם: לא** |
| 47 | **רף העומק של בלוק 8 מול "רק מהבית".** בברירת המחדל המזווה הוא העדפה והמאגר המלא זמין; ב"רק מהבית" המחולל רואה רק את המזווה. כמה פריטים צריך במאגר כדי שמזווה של בית רגיל - סדר גודל 25–30 פריטים - יעבור את רצפות `pool_health()` (חלבון 4 · פחמימה 3 · ירק 3 · שומן 2) בלי הודעה | בעלים | להכריע בפתיחת בלוק 8: יעד לפי קטגוריה, ואיך 40 הייצורים של בלוק 9 מודדים גם מאגר מסונן. **חוסם: לא** **תוספת 12.09.2026:** מוכרע ב-8פ-ב, על מדידת 8פ-א - קבוצות p2 ו-p4 עם ספירות פחמימה בשני רפים זמניים, ב-`measurements.md` וב-`db/block8_carb_landscape_p4.txt` **תוספת 12.09.2026 (8פ-ב):** חלק הפחמימה נסגר - כשירים ≥ 30, נטולי גלוטן ≥ 10, ≥ 2 בכל משפחת מצרך (`decisions.md` 12.09). ירק ב-8י באותו עיקרון. השאלה על 40 הייצורים מול מאגר מסונן עוברת לפתיחת בלוק 9, עם #42 **תוספת 13.09.2026:** חלק הפחמימה נסגר בפועל מעל היעד - 44 כשירות ו-17 נטולות גלוטן מול 30 ו-10. חלק הירק מוכרע ב-8י-ב, על מדידת 8י-א **תוספת 13.09.2026 (8י-ב):** חלק הירק נסגר - כשירים ≥ 25, `no_prep` ≥ 10, ≥ 2 בכל משפחה עם מכסה (`decisions.md` 13.09). הקריטריון השני הוא הכנה ולא אלרגנים, כי בירק האלרגנים ריקים מעצם זהות המזון. נשאר פתוח: 40 הייצורים מול מאגר מסונן, בפתיחת בלוק 9 עם #42 **תוספת 13.09.2026 (8י-י):** סעיף "≥ 2 בכל משפחה" תוקן ל"≥ 2 בכל משפחה שיש בה יותר מקוד p6 אחד במאגר". הסתירה נמצאה בתחזית S3 לפני `--apply`: משפחה 48 אפונה וגזר מגיעה ל-1, כי ל-p4 7311 יש p6 יחיד במאגר ומכסתה נקבעה ל-1 באותה הכרעה שקבעה את ה-≥2. נדחתה החלופה להוסיף את 3646 - אותו מצרך באותו p6, והוספה כדי לספק שער היא שינוי נתונים כדי להתאים למדידה |
| 48 | **Lupin (תורמוס) באוצר האלרגנים.** האוצר הוא שמונת אלרגני הספייק - מה שהאשף מציע בשלב 7 והסינון חוסם. תורמוס הוא אלרגן בסימון חובה באיחוד האירופי; 1630 תורמוס מבושל אושר בבלוק 4 ונשאר לא כשיר עד ההכרעה. הוספה נוגעת באשף, ב-`filters.py` ובאפיון | בעלים | להכריע לפני בלוק 8: להוסיף Lupin לאוצר או להשאיר את 1630 בחוץ. **חוסם:** 1630 בלבד **תוספת 12.09.2026:** נדחה למקטע החלבון והשומן של בלוק 8; 8פ נפתח בלעדיו **תוספת 17.09.2026 (7ת-ב1-2):** מקרה שני - סלרי, רכיב בנקניקיות מן הצומח של זוגלובק (1792), מחוץ לשמונה; 1792 יצא מ-7ת לפי כלל הזהות (`decisions.md`, 17.09.2026). סלרי גם בגרסת "בסגנון סיני" של קו תבשילי האורז של אסם (2807), ו-2807 יצא מ-7ת בהכרעה על התערובות. חרדל הוא המועמד הסביר לשלישי, לפי הבעלים |
| 49 | **רכיבי בסיס בלי שורת אצירה - ההורשה עיוורת להם.** 13 מ-14 רכיבי 8 המתכונים אינם ב-`food_curation`, ולכן `v_recipe_inherited_allergens` לא רואה אותם ו-`v_recipe_unreviewed_components` יחזיר אותם אחרי 19. ב-6ח המתכונים מתויגים ישירות לפי רכיביהם, וה-view אינו 0 בכוונה | בעלים | להכריע בפתיחת בלוק 8, יחד עם 567 רכיבי הבסיס: שורות נסקרות-בלבד לרכיבים (בלי קטגוריה, לא כשירות) או מנגנון אחר. מיגרציה 19 וידאה (12.09): 21 שורות, כולן רכיבי השמונה - זה המצב העומד. **חוסם: לא** **תוספת 12.09.2026:** נדחה למקטע החלבון והשומן של בלוק 8; 8פ נפתח בלעדיו. אם מועמד פחמימה יהיה מתכון, הכרעת 12.09 חלה - תיוג ישיר לפי רכיביו |
| 50 | **תקרה שאינה כפולה שלמה של היחידה בשני כשירים.** 8348 לחם מחיטה מלאה - פרוסה 30 ג', `max_g` 92; 3459 תפוח אדמה מבושל - יחידה בינונית 120 ג', `max_g` 242. שניהם `whole_only`. הכלל מ-30.08: תקרה שאינה כפולה שלמה של היחידה שוברת את `whole_only`; התקרות נלקחו מ-`MAX_G` של הספייק. נצפה ב-8פ-ו, 13.09.2026 | בעלים | האם הפותר עוצר על 3 פרוסות ו-2 יחידות בלי לגעת בתקרה, או שהתקרה צריכה לרדת ל-90 ול-240. **חוסם: לא** **תוספת 13.09.2026 (8י-א):** חמישה מופעים ולא שניים - 3663 עגבניה (יחידה 160, תקרה 322) · 3793 מלפפון (82, 166) · 3837 פלפל (109, 220), 2.01 · 2.02 · 2.02 יחידות. **מוכרע לחדשים ב-8י-ב:** תקרה של פריט `whole_only` היא כפולה שלמה של היחידה, מוחל ב-8י-ז. חמש הקיימות מתוקנות בתת-בלוק נפרד אחרי 8י, יחד עם #51 **תוספת 14.09.2026 (8ת-א):** נמדד על כל 264 הכשירים ב-`db/_audit_units.py`: 38 שורות `whole_only = true` שהתקרה שלהן אינה כפולה שלמה של היחידה, מתוך 80 - חלבון 29 · שומן 4 · פחמימה 2 · ירק 3. **ההיקף אינו מוכרע.** חמש השורות שכאן נושאות חתימה אחת - כפולה שלמה של היחידה ועוד 2.0 ג' בדיוק - ו-805 חזה עוף צלוי נושא אותה חתימה (130 = 2 × 64 + 2), ולכן הגבול הוא חמש או שש. ההיקף נדחה למדידת 8ת-ו: האם `whole_only` כופה על הפותר יחידות שלמות. אם לא, הכלל מ-30.08 שתקרה שאינה כפולה שלמה "שוברת את `whole_only`" נשען על הנחה שלא נמדדה, ו-#50 צריך ניסוח מחדש ולא תיקון. 28 שורות מוסכמת המשפחה עוברות ל-#53. **המשפט "התקרות נלקחו מ-`MAX_G` של הספייק" שלמעלה נסתר במדידה:** אף אחת מחמש השורות אינה בטבלת `MAX_G` של `spike/foods.py`, 12 רשומות לפי שם, ואף אחת אינה נכתבת בשורה ב-`db/*.sql` - הן נאצרו לפני שהמיגרציות נשמרו בריפו **תוספת 14.09.2026 (8ת-ו):** נמדד בקריאת הקוד, בלי ריצות. `whole_only` נקרא במקום אחד - `spike/portions.py:53`, ב-`_options()` - ומגביל את האפשרויות ל-1, 2 או 3 יחידות, מסוננות ב-`max_g` (55–57); כל כמות נצמדת לאפשרות (180) וזזה אחר כך רק בין אפשרויות (201–260). לכן תקרה שאינה כפולה שלמה אינה ניתנת להשגה - הכמות הגבוהה היא הכפולה השלמה שמתחתיה - וההצמדה למטה אינה משנה אף כמות שהפותר מגיש, בכל 38 השורות. המשפט "תקרה שאינה כפולה שלמה של היחידה שוברת את `whole_only`" (`spec/05-food-db.md:164`, 30.08) אינו מתאר מנגנון שקיים; הוולידטור אינו בודק אף אחד מהשדות, והפרומפט אינו רואה אותם. #50 צריך ניסוח מחדש ולא תיקון. לא נסגר **נסגר 14.09.2026 (8ת-ז) - לא פגם.** חמש השורות נשארות כפי שהן, ואין מיגרציה. המדידה שסגרה אותו: הצמדת התקרה למטה אינה משנה אף רשימת אפשרויות ואף כמות מוגשת - 8ת-ו קרא ש-`whole_only` נאכף ב-`spike/portions.py:53` והתקרה ב-55–57 רק מסירה אפשרויות שמעליה, ו-`db/_effective_ceilings.py` מדד ב-8ת-ז שבכל 80 שורות `whole_only` המקסימום של `portions._options()` הוא הכפולה השלמה שמתחת לתקרה, עד שלוש יחידות. הכלל שעליו נשען, שתקרה שאינה כפולה שלמה "שוברת את `whole_only`", אינו מתאר מנגנון שקיים; שורת §5.0 ב-`spec/05-food-db.md` נוסחה מחדש לפי מה שהקוד עושה |
| 51 | **ל-`db/_compare_export.py` אין `--expect-changed`.** הכלי יוצא 0 רק כשכל רשומה משותפת זהה, ולכן כל שינוי מכוון ברשומה קיימת - שיפוט יחידה, תיקון `max_g`, שינוי `prep` - נרשם אצלו ככישלון ויוצא 1. ל-`--expect-added` יש מקבילה מדויקת בקודים; לשינוי אין. נצפה ב-8פ-ט2, 13.09.2026, על 3962 | בעלים | להוסיף `--expect-changed` שמקבל קודים ושמות שדות, או להשאיר את ה-exit כהגדרת הכלי ולפטור אותו בשער פר-מקרה. **חוסם: לא** **תוספת 13.09.2026:** נקשר ל-#50 - תיקון חמש התקרות הקיימות הוא בדיוק שינוי מכוון ברשומות קיימות, ולכן שני הפתוחים נסגרים בתת-בלוק אחד אחרי 8י **תוספת 14.09.2026 (8ת-ב):** מוכרע: `--expect-changed` מקבל קודים ושמות שדות. הרזולוציה נבחרה כי שינוי בשדה שלא נצפה חייב להמשיך להיכשל, וזה מה ש-#52 היה - `servings` של 2828 השתנה בלי שום שינוי בנתונים. נבנה ב-8ת-ד **תוספת 14.09.2026 (8ת-ד) - נסגר.** `db/_compare_export.py --expect-changed PATH`: רשומה בשורה - `source_code`, TAB, ושמות שדות מופרדים בפסיק - בכללי קובץ הקודים של `--expect-added`. רשומה ברשימה חייבת להשתנות בדיוק בשדות שלה: שדה שאינו ברשימה נכשל ונקוב בשמו, שדה ברשימה שלא השתנה נכשל, וקוד ברשימה שאינו בשני הייצואים נכשל. שורה פגומה, קוד או שדה פעמיים, שדה שאף רשומה אינה נושאת, `id`, וקוד בשני הדגלים עוצרים לפני ההשוואה. הסוויטה `db/_compare_export_suite.py`: 21 מקרים עוברים, ובהם 3962 `unit` יוצא 0 ו-#52 - 2828 שהשתנה ב-`servings` כשהוא רשום ל-`unit` - יוצא 1 ונוקב ב-`servings`; הקריאות הקיימות זהות בית-בית לכלי ב-`c668e42` |
| 52 | **הייצוא של 09 אינו דטרמיניסטי: שתי שורות הגשה באותו משקל מסודרות כרצון המתכנן.** `SQL_SERVINGS` ממיין לפי `source_code, grams` בלבד. ב-8י-י, אחרי ה-`--apply` של מיגרציה 23, `_compare_export.py` יצא 1 על 2828 קורנפלקס: `[('כוסות', 30.0), ('מנות קטנות', 30.0), …]` הפך ל-`[('מנות קטנות', 30.0), ('כוסות', 30.0), …]`, בלי שום שינוי בנתונים. הרחבת רשימת הקודים שינתה את תוכנית השאילתה. 4 מ-264 הפריטים המיוצאים חשופים: 2005 (500 ג' ×2) · 2828 · 2838 · 9595 (30 ג' ×2). ההיקף אינו השער בלבד - `spike/menu_foods.py` הוא מה שהמודל קורא, סדר שורות ההגשה נכנס לפרומפט, ושער הרגרסיה של 40 הייצורים נשען על שחזוריות | בעלים | **מוכרע ב-8י-כ:** שובר שוויון ב-`ORDER BY` של 09. נדחו: קבלת exit 1 כמו ב-#51, שמשאירה שלושה תיקו חיים; והשוואה חסרת-סדר ב-`_compare_export.py`, שאינה פותרת את בדיקת החתימה בית-בית. **חוסם:** רישום הבסיס לשער הבא **מוכרע ונסגר 13.09.2026 (8י-כ):** שובר שוויון ב-`ORDER BY` של `SQL_SERVINGS` - `s.mida_code`, מפתח שלישי אחרי `f.source_code, s.grams`. שלוש ריצות של 09 אחרי התיקון זהות בית-בית. השינוי מול הייצוא שאחרי מיגרציה 23 נגע ב-3 פריטים ובשדה `servings` בלבד - 2828 · 2838 · 9595. מארבעת התיקו רק שלושה מגיעים לשדה `servings`: ב-2005 השוויון הוא בין 100 יחידה ל-900 אריזה, ו-900 אינה יחידת תפריט; לכן "שלושה תיקו חיים" שלמעלה הם שניים שיכלו להפיל שער - 2838 ו-9595. ה-`EXPLAIN` מאשר את המשפט על תוכנית השאילתה: עם 190 הקודים שלפני 8י-י - `Incremental Sort` מעל `Nested Loop` על האינדקס `(food_id, label_he)`; עם 264 - `Sort` מעל `Hash Join` ו-`Seq Scan` על `food_servings`. בשתיהן המפתח `f.source_code, s.grams` אינו קובע סדר בין שורות באותו משקל. שלוש ריצות של 09 ללא שינוי נתנו שלוש פעמים `36f84dd8…` - הסדר לא זז בין ריצות על אותו מסד ואותה רשימה, הוא זז כשהרשימה גדלה. `ORDER BY` נוסף שמפתחו אינו ייחודי בסכימה: `SQL_FOODS` ממיין לפי `source_code::int`, והייחודיות בסכימה היא על הטקסט; נמדדו 264 שורות · 264 קודי טקסט · 264 ערכי int, בלי התנגשות. גם המפתח החדש אינו ייחודי בסכימה - הייחודיות ב-`food_servings` היא `(food_id, label_he)` - ונמדדו 0 כפולים של מזון · משקל · `mida_code` ו-0 `mida_code` ריק ב-9,864 השורות. שניהם דווחו ולא תוקנו. **שלוש תצפיות נלוות, נרשמו ולא תוקנו (8י-כ):** הייצוא נכתב ב-CRLF כי `write_text` רץ בווינדוס, ולכן הבסיס הרשום תקף לפלטפורמה ולא רק למסד - ריצה בלינוקס תיתן חתימה אחרת על אותו מסד. ו-`SQL_FOODS` ממיין ב-`ORDER BY source_code::int`, שאינו ייחודי בהגדרה - "01" ו-"1" ייפלו לאותו ערך - ונמדד סגור היום בלבד: 264 שורות · 264 קודי טקסט · 264 ערכי int. גם שובר השוויון החדש עצמו אינו נאכף בסכימה; הייחודיות על `food_servings`, מלבד המפתח הראשי `id` ואינדקס חלקי על `food_id` כש-`is_default`, היא `(food_id, label_he)` בלבד, ונמדדו 0 כפולים של מזון · משקל · `mida_code` ו-0 `mida_code` ריק ב-9,864 השורות |
| 53 | **מוסכמת המשפחה נכתבה בגרמים ונמסרת ביחידות, ולכן אינה נמסרת כפי שנכתבה.** טבלת הבעלים מ-11.09 ביומן בלוק 5 קבעה `max_g` פר משפחה בגרמים - 175 לבשר, לעוף ולדג · 150 לביצה · 45 ברירת מחדל לאגוזים שלמים · 15 חריג - בלי תלות ביחידה. הפותר מגיש פריט `whole_only` ביחידה אחת, שתיים או שלוש שאינן עוברות את התקרה (`spike/portions.py:53–57`, נמדד ב-8ת-ו), ולכן תקרה של 175 הופכת לתקרה אפקטיבית של 90 עד 150 ג' לפי יחידת הפריט, ב-7 ערכים שונים על 21 השורות (`db/_effective_ceilings.py`, 8ת-ז). ה-150 של הביצים נמסר כ-114 · 116 · 148.5, ה-45 של המקדמיה כ-7.8, וה-15 של אגוזי ברזיל כ-13.2. ב-23 משורות `whole_only` תקרת שלוש היחידות נמוכה מהתקרה הרשומה - שם מגבילה רשימת הצעדים, לא הכרעת הבעלים. **ההיקף: 28 שורות** - 26 ב-`18_tag_protein.sql` (5ז) ו-2 ב-`16_tag_fat.sql` (5ה); הערכים 175×21 · 150×5 · 45 · 15, והיחסים 1.167–17.308. אותה מוסכמת יושבת גם על שורות שהתקרה בהן כפולה שלמה במקרה ולכן אינן בהיקף - 1564 ביצה (150, יחידה 50) ו-1845 פקאן (45, נמסר כ-9). חמש שורות 3ד/3ח (805 · 1267 · 8840 · 1854 · 3223) אינן בהיקף: הן קדמו לטבלת המשפחות, וארבע מהן נגזרו מהיחידה - 3223 בשמו ב-`decisions.md` 30.08, ושלוש מוסקות. **שייך למקטע החלבון והשומן של בלוק 8, לצד #40 ו-#41** | בעלים | **השאלה לבעלים: האם התקרות האפקטיביות האלה מקובלות, או שהמנגנון צריך להשתנות.** השאלה אינה עוד איזו מוסכמת גוברת: 8ת-ו מדד שהצמדת התקרות אינה משנה אף כמות מוגשת, ולכן עלות ההצמדה שנמדדה ב-8ת-א - 0.8–81 ג' לשורה, סך 1,071.1 על 28 השורות ו-22 מעל 20 ג' - היא מספר על הנייר. מה שקובע בפועל: היחידה, תקרת שלוש היחידות, ו-`max_g` שנאכף לכל מופע בארוחה ולא ליום (#34). נמדד ב-8ת-א, 8ת-ו ו-8ת-ז, 13–14.09.2026. **חוסם: לא** |
| 54 | **`_compare_export.py` קורס על שדה עברי כש-stdout מנותב בלי `PYTHONIOENCODING`, וכותרת הקובץ טוענת "Output is ASCII only".** נמדד ב-8ת-ד על הפיקסצ'ר של E1: `UnicodeEncodeError` ו-exit 1 מיד אחרי שורת `gone:`, ברגע ששדה עברי נכנס לשורת `changed`. קיים גם בכלי שלפני 8ת-ד (`c668e42`) - לא נוצר שם. הטענה בכותרת שגויה מאז שנכתבה: `unit` ו-`servings` נושאים עברית, ושורת `changed` מדפיסה `repr` שלהם. **בנוסף:** שורת ה-usage בהודעות STOP התארכה ב-8ת-ד ולא נבדקה מול הכלי הישן | בעלים | **לא חוסם.** `PYTHONIOENCODING=utf-8` כבר חובה ל-`--source db` ומכסה גם כאן; 8ת-ה רץ כך. התיקון: לכפות UTF-8 על stdout בכלי, או לתקן את הכותרת ולהשאיר את הכלל החיצוני. שער לתיקון: השוואת stdout ו-stderr ישן מול חדש, ובה ארגומנט שגוי |
| 55 | **`spike/portions.py:57` מגיש מעל התקרה כשהצעד הקטן ביותר כבר מעליה.** כשאף אפשרות אינה עומדת ב-`max_g`, `or [min(opts)]` מחזיר את האפשרות הקטנה ביותר, והפריט מוגש מעל התקרה - יחידה אחת לפריט `whole_only`, חצי יחידה לכל פריט אחר בעל יחידה (`UNIT_STEPS` מתחיל ב-0.5). נמדד ב-8ת-ז ב-`db/_effective_ceilings.py` על הייצוא הנוכחי (`a1d462af…`): אפס שורות `whole_only`, ושלוש שורות שאינן `whole_only` מתוך 235 כשירות בעלות יחידה ו-`max_g` - 446 גבינה בולגרית 5% שומן, כולל קוביות, פיראוס תנובה (protein, קופסה 250 ג', הצעד הקטן 125 ג' מול `max_g` 100) · 1900 פרג, זרעונים (fat, קערית לפתן 46 ג', הצעד הקטן 23 ג' מול `max_g` 15) · 8598 גבינת פטה 5% שומן, מחלב כבשים, פיראוס (protein, קופסה 250 ג', הצעד הקטן 125 ג' מול `max_g` 100). אותן שלוש בייצוא שלפני 8ת-ה (`a1958384…`). **3776 כרובית לא הייתה המקרה הזה:** היא אינה `whole_only`, והאפשרות הקטנה שלה הייתה חצי יחידה, 113 ג', מתחת לתקרה 200 - היחידה 226 הייתה מעל התקרה, אבל הנפילה לא הופעלה, ו-8ת-ה לא תיקן כאן דבר. **תצפיות מקריאת 8ת-ו, לא פריטים נפרדים:** `portions.py:236` ו-`:254` קוראים ל-`opts.index(min(opts, ...))`, שנופל על רשימה ריקה - ופריט `by_weight` עם `max_g` מתחת ל-30 מקבל רשימה ריקה (48–49); וההערה ב-`09_export_menu_foods.py:367`, שהקובץ `menu_foods.py` המיוצר הוא מה שהמודל קורא, אינה מדויקת - הפותר קורא אותו, ו-`generator.py:62–89` מראה שהפרומפט אינו נושא לא `unit`, לא `whole_only` ולא `max_g` | בעלים | להכריע מה מוגש כשהצעד הקטן של פריט מעל התקרה שלו. **חוסם: לא** |
| 56 | **נתיב הכניסה למוצר מסחרי עדכני.** אין היום בקוד מסלול להוסיף ל-`foods` שורה לצד קובץ המשרד: 03 טוען קובץ `foods` אחד (`db/03_load_source.py:255-258`) ומרוקן את `src_foods` לפני כל טעינה (שורה 106), ו-04 בונה את שורות `foods` רק מ-`src_foods` (`db/04_transform.py:319-362`). החלפה או מיזוג של קובץ המשרד, והכנסה ידנית למסד, אפשריים ואינם מסלול. נמדד ב-7א וב-7ג; ההכרעה שנשענה על קובץ מקור חמישי בוטלה (`decisions.md` 14.09.2026) | בעלים | **שלוש חלופות, אף אחת לא נוסתה:** להרחיב את 03 לשני קובצי `foods` - בקוד כפי שהוא, קריאה שנייה ל-`load_foods` מוחקת בשקט את הקובץ הראשון ב-`TRUNCATE src_foods` (שורה 106); בלי ה-TRUNCATE, הקובץ השני מתנגש כבר בשורה הראשונה על `src_foods.row_num`, `int PRIMARY KEY` שממוספר מ-1 בכל קובץ (`db/01_food_db_schema.sql:49`, `db/03_load_source.py:107`); וקוד מסחרי ששווה לקוד של המשרד ייפול ב-04 על `foods.source_code UNIQUE` (`db/01_food_db_schema.sql:101`) · טבלת `src_*` נוספת ו-INSERT נוסף ב-04 · טבלה נפרדת שמתאחדת ב-view - ואז שורת אצירה למוצר מסחרי נראית יתומה ל-`v_curation_orphans`, שבודק מול `foods` בלבד. הבעלים מכריע, על מדידה. **חוסם:** 7מ בלבד **תוספת 19.09.2026 (7ת-ד):** 7ת נסגר ומוזג; 7מ ממתין להכרעה הזו, ובלוק 7 פתוח על 7מ בלבד. #56 אינה חוסמת את 8 ואת 9 (`decisions.md`, 19.09.2026) |
| 57 | **מיצוי בריכת השומן נטולת האלרגנים.** 11 השומנים הכשירים נטולי האלרגנים יושבים בשלוש משפחות p4 - 4310 · 6310 · 8210 - ובהן 71 שורות ב-`foods` בלי שורת אצירה: 23 · 28 · 20 בהתאמה. נמדד ב-7א (`db/_audit_block7a.py`, סעיף A4.3). ההשערה שרובן כבר נדחו במפורש בבלוק 4 ובכלל הקלייה של 5ד לא נבדקה ואינה נרשמת כממצא | בעלים | **השער:** הצלבת 71 השורות מול טבלאות ה"לא" ביומן `work/2026-09-05-block-4-curation.md` - "### לא (136)" במקטע השומן ו-"### לא (182)" במקטע החלבון - **וגם** מול §5.5 ו-#23: בלוק 4 הציג רק שורות מעל רצפת השומן ("כל המשפחה מעל הרצפה (`fat_g ≥ 10`)", שם); ב-6310, 27 מ-28 השורות בלי שורת אצירה אינן אבוקדו, ושאר p2 63, מחוץ ל-p6 631050 אבוקדו, הוא פירות - משפחה 38 ב-§5.5 של `spec/05-food-db.md`, "אינם נאצרים (#23)"; וכלל הקלייה (`decisions.md` 11.09.2026) הוציא במפורש קוד אחד, 1872. הבעלים מכריע אחרי ההצלבה. **חוסם: לא** - נדרש ליעד עומק השומן שמתעדכן בסגירת #16 |
| 58 | **שינוי הרכב אצל היצרן אחרי שהתווית נרשמה.** החצי השני של #45: מה קורה כשיצרן משנה את הרכב המוצר אחרי שהתווית שלו נרשמה. ההכרעה על החצי הראשון (`decisions.md` 14.09.2026) רושמת את מקור הנתון ואת תאריכו, ואין בה מנגנון אימות מחדש | בעלים | **חוסם: לא** היום. שאלת עדכניות נתונים שנוגעת ל-v1 |
| 59 | **איפה חי במסד ערך סיבים שנקרא מתווית.** #13 נסגר ב-29.08.2026: `COALESCE(0)` עומד, ו"נוספה בדיקת אצירה: פריט צמחי עם `fiber_g` ריק שנכנס ל-`menu_eligible` מאומת מול תווית". הבדיקה אינה אומרת איפה נכתב הערך שנקרא. העמודה היחידה לסיבים היא `foods.fiber_g` (`db/01_food_db_schema.sql:120`); 04 פותח ב-`TRUNCATE food_recipe_components, food_nutrients, food_servings, foods, nutrients RESTART IDENTITY CASCADE` (`db/04_transform.py:320-321`) וממלא את `fiber_g` רק מ-`total_dietary_fiber` של `src_foods` (`:349-350`), ולכן ערך שנכתב ישירות ל-`foods.fiber_g` אינו שורד את הריצה הבאה. `kcal` נגזר ב-`db/05_derive_kcal.sql:79-81`, ושוב בכל ריצה של 04 (`db/04_transform.py:432-434`), כ-`protein_g*4 + fat_g*9 + carb_g*4 + COALESCE(fiber_g,0)*2 + COALESCE(al.value,0)*7` - `fiber_g` ריק נספר כאפס. ביומן בלוק 4, בשורת 1826: "לא ייכתב ככשיר בלי ערך סיבים". תקדים D2 (`work/2026-09-14-block-7-labels.md`): נתון התווית נרשם בעמודות חדשות ב-`food_curation`, ש-04 אינו מרוקן (`db/04_transform.py:40-41`). הפריטים הצמחיים מבין 34 הפריטים של 7ת שבלי `fiber_g` - נמדדים ב-7ת-א, ב-`db/block7_label_sheet.tsv` | בעלים | **מוכרעת ב-7ת-ב, לפני 7ת-ג3** **תוספת 16.09.2026 (7ת-א2):** החצי הראשון - איפה חי הערך - נסגר בהכרעת 16.09: `fiber_g_label` ב-`food_curation`, עמודה שמיגרציה 24 מוסיפה, לארבעת הפריטים הצמחיים בתור בלי `fiber_g` - 1698 · 1826 · 2807 · 8969. החצי השני - מי צורך את הערך, `v_menu_foods` וגזירת ה-kcal - נשאר פתוח, מחוץ ל-7ת. "מוכרעת ב-7ת-ב, לפני 7ת-ג3" מוחלף בתוספת הזו (`decisions.md`, 16.09.2026) **תוספת 17.09.2026 (7ת-ב1-2):** צומצמה לשני פריטים - 1698 · 1826. 2807 ו-8969 יצאו מ-7ת בהכרעה על התערובות, ו-`fiber_g_label` נכתב לשניים בלבד (`decisions.md`, 17.09.2026) **תוספת 19.09.2026 (7ס6):** אחרי 7ת-ב אין שורה עם תווית שצריכה `fiber_g_label` - 1698 ו-1826 יצאו, ולשניהם לא נקרא ערך סיבים מאף מקור; `fiber_g_label` ריק בכל 34 שורות הגיליון, ומיגרציה 24 כפי שהוכרעה תוסיף עמודה שתישאר NULL כולה. הבעלים מכריע לפני 7ת-ג1: להשאיר את העמודה ב-24, מוכנה ל-7מ, או להוריד אותה - שינוי תוכנית שנוגע בשורות 16.09.2026 ו-17.09.2026 ב-`decisions.md` ובשורות 7ת-ג1 ו-7ת-ג3 ב-`PROGRESS.md`, עם הסקר של §8 **תוספת 19.09.2026 (7ת-ג0):** הבעלים הכריע: העמודה נשארת ב-24 כפי שהוכרע ב-16.09, מוכנה ל-7מ; אחרי 25 היא NULL בכל שורות `food_curation`, וזה ידוע. הסעיף "הבעלים מכריע לפני 7ת-ג1: להשאיר את העמודה ב-24, מוכנה ל-7מ, או להוריד אותה" בתוספת 19.09.2026 (7ס6) נענה, ומוחלף בתוספת הזו. החצי השני - מי צורך את הערך, `v_menu_foods` וגזירת ה-kcal - נשאר פתוח, מחוץ ל-7ת (`decisions.md`, 19.09.2026) |
| 60 | **תג `vegan` שנקבע לפי זהות הפריט, לא לפי תווית.** אילו שורות `menu_eligible` עם התג `vegan` הן מוצרים מסחריים שהאלרגנים שלהם נסקרו בבלוק 6 לפי זהות המרכיב ולא לפי תווית. `spike/filters.py:33` מסנן לפרופיל טבעוני לפי התג בלבד. ב-10126 וב-1791 התג נכתב בתיוג של בלוק 5ז (`db/18_tag_protein.sql`), והדף נוקב בחלבון ביצה. מדידה בקריאה בלבד, והבעלים מכריע אם הן צריכות מעבר תווית | בעלים | **חוסם: לא** - לא ב-7ת; ההכרעה מ-17.09.2026 על התג `vegan` בהפיכה מכסה את תור התווית (`decisions.md`) **תוספת 19.09.2026 (7ת-ד):** מדידה בלבד. אחרי 25 ירד התג `vegan` מ-1791 ומ-10126, על Egg בתווית. על כל הטבלה 0 שורות `menu_eligible` עם `vegan` נושאות Egg, Milk או Fish, ו-176 שורות `menu_eligible` נושאות `vegan`. השאלה על שאר המוצרים המסחריים נשארת פתוחה |
| 61 | **שורת "מכיל" בדף צד שלישי, כשאין אלרגן ברכיבים.** כלל המקור (`decisions.md` 16.09.2026) מקבל דף קמעונאי או צד שלישי רק כשליצרן אין דף, ורק עם רשימת רכיבים מלאה ושורת "מכיל". למוצר שאין ברכיביו אלרגן אין שורת "מכיל" גם על התווית שלו - כך ב-661, שנקרא מתמונת האריזה ב-18.09.2026. השאלה: האם התנאי חל על דף צד שלישי גם אז. עלתה ב-8741: לטירת צבי אין אתר, ו-foodiepedia.co.il מביא רשימת רכיבים מלאה ל"פסטרמה הודו בדבש" 400 ג', בלי שורת "מכיל". במקטע B 8741 יצא לפי הכלל כפי שהוא, והשאלה לא הוכרעה (`db/block7_label_sheet.tsv`, `notes` של 8741) | בעלים | **מוכרעת לפני 7ת-ב3 או ב-7מ** **תוספת 19.09.2026 (7ת-ב3):** לא נדרשה במקטע C - שלושת הפריטים שהגיעו לרשימת רכיבים בדף צד שלישי או קמעונאי, 1307 · 1698 · 1826 - האחרון בקטע האינדקס בלבד - נושאים ברכיבים אלרגן מהשמונה, Fish · Soy · Tree nuts, ולכן אינם המקרה ש-#61 שואלת עליו. נשארת פתוחה, מוכרעת ב-7מ. "מוכרעת לפני 7ת-ב3 או ב-7מ" מוחלף בתוספת הזו |

---

## פתוח לגרסה עתידית

| # | השאלה | הערה |
|---|---|---|
| 8 | רכישת משתמשים — אין סעיף באפיון | יש קהל מאמנים קיים מהשירות של דפי הנחיתה, שלא מנוצל |
| 9 | מסלול B2B למאמנים | מוקפא, אבל נשאר על השולחן. הקהל כבר קיים |
| 10 | מדרגת תמחור שלישית | **לא לפני** שיהיו נתוני המרה אמיתיים מחינם למנוי. ראה `spec/02-business.md` |
| 11 | שוק אמריקאי | אינו שאלת שפה אלא שאלת מאגר. מתוכנן לגרסה עתידית ולא חוסם עכשיו. דורש מאגר מזון אמריקאי נפרד, לא תרגום של הקיים |

---

## נסגר בשיחת 19.09.2026

| השאלה | ההכרעה |
|---|---|
| #62 - עוזר הצילום מחוץ לריפו | **נסגר ב-7ת-ג0: העוזר בריפו, `db/_snapshot_ro.py`.** מייבא את `SNAPSHOT_SQL`, `SNAPSHOT_NAMES` ו-`query()` מ-`db/_audit_block7a.py`; כל משפט בטרנזקציה READ ONLY; `db/` נפתר מהקובץ עצמו, ולא מ-worktree מסוים; `--expect` אופציונלי. אומת ב-worktree `state-report-read-only-83d703`, מנתיב הריפו ומחוץ לריפו - אותו צילום כמו בדוח המצב של הסשן החמישי. מכאן פרומפטים נוקבים בו בנתיב הריפו; בעותקי ה-scratch לא נגעו. ההחלטה והנימוק ב-[`decisions.md`](decisions.md), 19.09.2026 |

## נסגר בשיחת 14.09.2026

| השאלה | ההכרעה |
|---|---|
| #45 - תנאי התווית: איפה התווית נרשמת, ומה קורה כשהיצרן משנה הרכב | **החצי הראשון הוכרע ב-7ב:** שתי עמודות חדשות ב-`food_curation` - מקור הנתון ותאריכו; המיגרציה נכתבת ב-7ת. החצי השני, שינוי הרכב אצל היצרן, עבר ל-#58. ההחלטה והנימוק ב-[`decisions.md`](decisions.md), 14.09.2026 |
| #16 - אילוץ בחירה לשומן בבלוק 7, או עדכון יעד עומק השומן בשער היציאה | **הוכרע ב-7ב: בלי מכסת שומן לבלוק 7; יעד עומק השומן בשער היציאה מתעדכן במקומה.** נמדד ב-7א: 11 נטולי אלרגנים מ-37 שומנים כשירים, והאלרגיה הבודדת הגרועה, אגוזי עץ, משאירה 20 מול רצפת 2 ב-`pool_health()`. מיצוי הבריכה נטולת האלרגנים פתוח כ-#57. **לא הוכרעו כאן:** קריטריון הבחירה של 50 המוצרים לפי §6, שהיקפו נדון ב-7מ, והתוספת מ-30.08.2026 על עומק החלבון הטבעוני מול רצפה של 4 - לאף אחד מהשניים אין היום שאלה פתוחה משלו. ההחלטה והנימוק ב-[`decisions.md`](decisions.md), 14.09.2026 |

## נסגר בשיחת 11.09.2026

| השאלה | ההכרעה |
|---|---|
| #46 - שורת סיכום קריאה בכישלון אסרציה במריץ | **נסגר ב-5ב.** try/except על `psycopg.Error` בלבד, שורה אחת אחרי הרולבק, exit 1; `mask_dsn` הוסר. הסוויטה של 4ה רצה שוב, ו-14 נפלה ב-V0 בשני המצבים עם השורה. ההחלטה והנימוק ב-[`decisions.md`](decisions.md), 11.09.2026 |

## נסגר בשיחת 05.09.2026

| השאלה | ההכרעה |
|---|---|
| #17 — מילוי מחדש אחרי דחייה בבלוק 4 | **חלופה (ג): מילוי ידני מהדירוג המלא, בלי שינוי קוד.** הסקירה רצה מול המסד ולא מול הגיליון. ההחלטה והנימוק ב-[`decisions.md`](decisions.md), 05.09.2026 |
| #18 — מכסת משפחת הסייטן | **9851 לבדו.** 8834 נדחה כצורה יבשה. ב-[`decisions.md`](decisions.md), 05.09.2026 |
| #19 — סיווג #17 | **מתייתר** עם סגירת #17 |

## נסגר בשיחת 30.08.2026

| השאלה | ההכרעה |
|---|---|
| #23 — פרוסת רוחב בבלוק 3ד | **4 פריטי `carb` ו-4 פריטי `veg` לצד ה-25, סה"כ 33.** `fruit` ו-`drink` אינם נאצרים בשלב הזה. `pool_health()` נמדד ב-12 פריטים ולא 60. ההחלטה והנימוק ב-[`decisions.md`](decisions.md) מ-30.08.2026 |

---

## נסגר בשיחת 29.08.2026

| השאלה | ההכרעה |
|---|---|
| #13 — 384 פריטים עם `fiber_g IS NULL` | **`COALESCE(0)` עומד** (המספר בפועל 387 אחרי החזרת המצרכים). נוספה בדיקת אצירה: פריט צמחי עם `fiber_g` ריק שנכנס ל-`menu_eligible` מאומת מול תווית. נמדד: 180 בשומן/חלבון, 20 ב-`v_kcal_outliers`, 5 בשתיהן |
| #15 — כלל `carb_g` ריק בבשר ובדג | **המבחין תקף ומאומץ.** נמדד על 245 פריטים; הזנב השלילי מוסבר במקדמים ולא בפחמימה. 6 בלתי-מוסברים, 3 מהם כבר בתור האצירה. **נוסח הכלל תוקן באותו יום** — התנאי "בלי שורות ב-`food_recipe_components`" ירד, ונוסף תנאי עצירה ב-`04_transform.py`. ראה [`decisions.md`](decisions.md), שורת "תיקון לנוסח כלל #15" |

---

## נסגר בשיחת 28.08.2026

| השאלה | ההכרעה |
|---|---|
| #12 — הפרדת שדות התיוג מ-`foods` | **אפשרות (א).** טבלת `food_curation` נפרדת על `source_code`, בלי FK, מצטרפת ב-`v_menu_foods`. הורצה מול הייצור והתחייבה; מבחן הקנרית עבר. ההחלטה והנימוק ב-[`decisions.md`](decisions.md), שורות 28.08.2026. המימוש ב-`db/06_split_curation.sql` |
| #14 — ארבעת המצרכים שהוחרגו (638, 1134, 9691, 9784) | **החזרה עם `carb_g = 0`.** שיפוט תזונתי חתום. ההחלטה והנימוק ב-[`decisions.md`](decisions.md) |

---

## נסגר בשיחת 27.08.2026

| השאלה | ההכרעה |
|---|---|
| Open Food Facts כמקור למוצרים מסחריים | **לא.** ODbL עם share-alike. טבלה ידנית של ~50 מוצרים במקום |
| האם הספייק יקרא ישירות מ-Postgres | **לא.** קובץ מיוצר, כדי לשמור על הרצה בלי מפתח ובלי רשת |
| היכן חי סטטוס בתיעוד | ב-`PROGRESS.md` בלבד |
