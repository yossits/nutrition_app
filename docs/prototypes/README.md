# אבות טיפוס

| קובץ | מה זה |
|---|---|
| `menu-screen.html` | מסך התפריט הראשי — לפתוח בדפדפן |

**זהו המסך היחיד שמעוצב כרגע.** אפיון UI/UX מלא לא בוצע. ראה `docs/project-status-he.md` סעיף 5ב למלאי המסכים החסרים (כ-16 מסכים ומצבים).

`menus.html` שנוצר על ידי `spike/show_menus.py` הוא פלט בדיקה ולא אב-טיפוס עיצובי — הוא חסום ב-`.gitignore`.
