# nutrition_app

אפליקציית ליווי תזונה אישי. המשתמש בונה פרופיל, המערכת מחשבת יעדים ומייצרת תפריט מותאם, ומעדכנת אותו לאורך התוכנית.

**סטטוס:** אפיון פונקציונלי סגור · ליבה טכנית מוכחת · ממשק לא נבנה

---

## ⚠️ לפני הכל — מפתחות API

**אף פעם לא לשמור מפתח API בתוך קובץ בריפו.** בוטים סורקים את זרם האירועים הציבורי של GitHub ומוצאים מפתחות תוך דקות.

המפתח נמסר כמשתנה סביבה בלבד:

```bash
# Windows
set ANTHROPIC_API_KEY=sk-ant-...

# Mac / Linux
export ANTHROPIC_API_KEY=sk-ant-...
```

ה-`.gitignore` חוסם `.env` וקבצי מפתחות. אם מפתח נחשף אי פעם — לבטל אותו מיד ב-console.anthropic.com ולהנפיק חדש. מחיקת הקומיט **לא** מספיקה; הוא נשאר בהיסטוריה.

---

## מבנה

```
nutrition_app/
├── docs/
│   ├── product-spec-he.md          # מסמך האפיון המלא — מקור האמת
│   ├── project-status-he.md        # סטטוס, יומן החלטות, צעד הבא
│   └── prototypes/
│       └── menu-screen.html        # אב-טיפוס מסך התפריט
└── spike/                          # הליבה — מוכחת ונמדדת
    ├── foods.py                    # מאגר זרע: 63 פריטים
    ├── engine.py                   # BMR · TDEE · מאקרו · רצפות בטיחות
    ├── filters.py                  # סינון · מספיקות · דגימה · ולידטור
    ├── portions.py                 # פותר-מנות + ניסוח עברי
    ├── generator.py                # קריאת API + לולאת ניסיון חוזר
    ├── profiles.py                 # 100 פרופילי בדיקה
    ├── run_spike.py                # בדיקת הליבה — ללא מפתח
    ├── run_generation.py           # מדידת שיעור מעבר — עם מפתח
    └── show_menus.py               # מייצר menus.html לבדיקה ויזואלית
```

**להתחיל מ-`docs/project-status-he.md`.** שם יומן ההחלטות והצעד הבא.

---

## הרצה

```bash
pip install -r requirements.txt

cd spike
python run_spike.py                 # ליבה דטרמיניסטית, ללא מפתח

set ANTHROPIC_API_KEY=sk-ant-...
python run_generation.py 40         # שיעור מעבר מול ה-API
python show_menus.py 8              # יוצר menus.html לפתיחה בדפדפן
```

---

## הארכיטקטורה בשורה אחת

> **המודל מרכיב. הקוד מחשב. הקוד מאמת.**

| שלב | מי |
|---|---|
| חישוב יעדים ורצפות בטיחות | קוד |
| סינון המאגר לפי הפרופיל | קוד |
| בחירת מאכלים לארוחה | **AI** |
| קביעת הגרמים | קוד |
| אימות מול היעדים | קוד |

הפרדה זו היא מה שהופך את המוצר לאמין. מודל שפה מרכיב ארוחה טוב ומחשב חשבון גרוע — כשביקשנו ממנו גם לחשב, שיעור המעבר היה 0%.

**וזה גם קו הבטיחות:** אלרגן שלא נכנס לפרומפט לא יכול להופיע בפלט. ההגנה בשכבת הסינון, לא בבקשה מהמודל.

---

## תוצאות מדודות

| מדד | ערך |
|---|---|
| מעבר ולידציה בניסיון ראשון | **92%** (40 ייצורים) |
| סטיית קלוריות | 0.0–0.2% חציוני |
| סטיית חלבון | 0.1–0.9% חציוני |
| הפרות רצפת בטיחות | **0** — מובטח מבנית |
| כמויות אבסורדיות | **0** מתוך 1,510 |
| דליפות אלרגן | **0** |
| עלות לתפריט | ~2,100 טוקנים · 8.4 שניות |

---

## הצעד הבא

1. **ייבוא מאגר משרד הבריאות** — `data.gov.il`, מאגר `nutrition-database`. רישיון פתוח
2. **אפיון UI/UX** — לא בוצע. כ-16 מסכים, אחד מעוצב
3. **בדיקה משפטית** — חסם לפני השקה מסחרית

פירוט מלא ב-`docs/project-status-he.md`.

---

## הערה

הפרויקט אינו תחליף לייעוץ רפואי או תזונתי מקצועי.
