# בלוק 9 - שער היציאה · יומן ההכרעות

**נפתח:** 21.09.2026 · **סטטוס יושב ב-`../PROGRESS.md`, לא כאן.**
**מה זה:** הרשומה של שער היציאה משלב ב' - ההגדרות שנרשמו מראש, המתכון, שתי הריצות וההכרעה - כדי שאבחון הכשל יתחיל ממנה ומהפלטים שבריפו.

## שער היציאה - 9, 21.09.2026

**השער, כלשון שורה 9 ב-`PROGRESS.md`:** "שער היציאה - `v_pool_depth` · 40 ייצורים על מאגר הזרע ואז 40 על המאגר האמיתי באותו יום, והמעבר בניסיון ראשון על האמיתי אינו נמוך מריצת הזרע, עם אפס כישלון מלא (#42, 20.09.2026) · `v_eligible_missing_tags` = 0". ההגדרות נרשמו מראש ב-S0, לפני קריאת ה-API הראשונה, ונכתבו ל-`../decisions.md` בשורה אחת של 21.09.2026: כישלון מלא הוא `None` שהניסיון האחרון שלו נפל בוולידציה או ב-`BAD_JSON` (4a); `API_ERROR` הוא כשל תשתית, נספר בנפרד ורץ שוב פעם אחת (4b); קריסה היא עצירה (4c); קריאות ה-API נגזרות (4d); שלב ב' נסגר רק על שלושת הקריטריונים של `../spec/05-food-db.md` §8 (4e).

**המתכון.** ייצוא טרי של 09 אל ה-scratch - 263,823 בתים · 319 רשומות · `2966e65c…`. כל ריצה: cwd בתיקיית ה-scratch שבה יושב הייצוא, `-m run_generation`, `PYTHONPATH` אל `spike/` של הריפו הראשי, `PYTHONUTF8=1`, הפניה מ-Git Bash (לקחים 53 ו-54). המפתח נקרא מ-`D:/workspace/nutrition_app/.env` בנתיב מוחלט, דרך `db/_env.parse_env_file`, ומוצב רק בסביבה של תהליך-הבן; הוא אינו מודפס, אינו נכתב ואינו מופיע בשורת פקודה. הדגלים: `40 --trace` בשתי הריצות, בלי `--show-menu`; הזרע ואז האמיתי, `--source db`. לפניהן בדיקה מקדימה, `--profile 1 --trace` על הזרע: שתי קריאות, עבר בניסיון 2, וה-workspace תקין.

**המודל והפרמטרים.** `claude-sonnet-4-6`, `max_tokens` 4000, `MAX_ATTEMPTS` 3, `timeout` 90; `temperature` אינו נשלח - `MODEL` ב-`../../spike/generator.py:19`, `MAX_ATTEMPTS` ב-`:21`, `timeout` ב-`:102`, `max_tokens` וגוף הבקשה בלי `temperature` ב-`:105`. הקוד `daf1b90`, זהה בשתי הריצות.

**החיזוי, בלי רשת.** בזרע 89 runnable ובאמיתי 91; 40 המזהים הראשונים בכל מאגר, בסדר של `run_generation.py`, נשמרו ב-`../../db/block9_predict_seed.txt` וב-`../../db/block9_predict_db.txt`, והחפיפה ביניהם 37. בשתי הריצות 40 המזהים שרצו זהים לחיזוי.

**שתי שורות הסיכום.**

- **זרע:** runnable 89 · ניסיון ראשון **35/40 (87.5%)** · ולידציה 40/40 · כישלון מלא 0 · `API_ERROR` 0 · קריאות, נגזרות, 45 · טוקנים 108,102 / 14,194 · 8.3 שנ' בממוצע · 5:33
- **אמיתי:** runnable 91 · ניסיון ראשון **7/40 (17.5%)** · ולידציה 19/40 · כישלון מלא **21** · `API_ERROR` 0 · קריאות, נגזרות, 98 · טוקנים 321,525 / 54,599 · 21.1 שנ' בממוצע · 14:05

**ההכרעה.** (a) ניסיון ראשון על האמיתי לא נמוך מהזרע - ✘, 7/40 מול 35/40. (b) אפס כישלון מלא על האמיתי - ✘, 21. (c) המצב לא זז - ✔: הצילום 480 · 319 · 154 · 43 · 44 · 78 · 319 · 0 · 0 exit 0 אחרי הריצות, הייצוא `2966e65c…` ללא שינוי, `v_pool_depth` זהה. (d) `v_eligible_missing_tags` = 0 - ✔. לפי `spec/05-food-db.md` §8: קריטריון 1 ✔ - `v_pool_depth` מול טבלת 8פ-א, 12.09.2026: חלבון 105 · 47 · 18 · 42 · 29 → 154 · 76 · 31 · 67 · 38, שומן 37 · 11 · 37 · 37 · 37 → 43 · 17 · 43 · 43 · 43; טבלת השרידות של 27.08 אינה ניתנת לשחזור, כי הסקריפט שהפיק אותה אינו בריפו בשום קומיט. קריטריון 2 ✘. קריטריון 3 ✔ - שמונת הכשירים ב-`v_kcal_outliers` עוקבו לסקירה שהכריעה אותם: 2064 · 2838 · 8342 ב-`decisions.md` 13.09.2026 (8פ-ט), 9440 ו-9851 ביומן בלוק 4 (05.09.2026), 1629 · 8261 · 8305 בכלל (4) של 8ח-ד (21.09.2026). **שלב ב' נשאר פתוח.**

**הכשל במספרים, בלי השערה.** 21 הכישלונות המלאים: #1 · 5 · 7 · 11 · 13 · 14 · 15 · 23 · 24 · 27 · 28 · 29 · 30 · 31 · 33 · 34 · 36 · 38 · 42 · 43 · 44, בכולם שלושה ניסיונות שהגיעו לוולידטור. ה-Counter על הניסיון האחרון: `KCAL_OFF` 14 · `PROTEIN_OFF` 13 · `FAT_BELOW_SAFETY_FLOOR` 8 · `FAT_OFF` 2. שגיאת הפותר החציונית על כל הניסיונות: בזרע 0.2% בקלוריות ו-0.4% בחלבון, באמיתי 5.9% ו-7.4%. באמיתי `KCAL_OFF` מתחת ליעד ב-43 מ-52, `PROTEIN_OFF` מעל היעד ב-33 מ-47, `FAT_OFF` מתחת ליעד ב-13 מ-13. כל פרומפט באמיתי נושא 24 פריטים, כי בייצוא אין פרי ושתייה; בזרע 23–29. השאלה פתוחה כ-#64.

**הממצא ל-#20.** במאגר האמיתי `FAT_BELOW_SAFETY_FLOOR` הופיע 26 פעמים ב-17 פרופילים, ובניסיון האחרון של 8 מ-21 הכישלונות המלאים; השומן מתחת לרצפה בחציון 9 ג', ובקיצון 25 ג'. בזרע פעם אחת, פרופיל 15 בניסיון 1, והוא עבר אחריו. `run_generation.py:139` קורא ל-`solve(picks, mt, _t)` בלי `fat_floor`. #20 ו-#31 הוכרעו כלא חוסמים את 9 ונשארים לבלוק תיקון בשלב ג'.

**אותו מזהה, פרופיל אחר.** `spike/profiles.py` דוגם את `dislikes` מתוך `NAMES`, ולכן זרם ה-PRNG מתפצל לפי המקור מפרופיל 1: אף אחד מ-37 המזהים המשותפים אינו אותו פרופיל בשני המאגרים (#63). השער משווה סכומים ועומד.

**החתימות.** קובצי ה-scratch, `D:/workspace/_scratch/block-9-gate/`, בתים · sha256 · שורות, כפי שנכתבו (CRLF): `seed_40.out` 11,250 · `0a5e70fe` · 182 · `db_40.out` 23,322 · `d66c3c0a` · 358 · `predict_seed.txt` 327 · `568b3b40` · 5 · `predict_db.txt` 329 · `8c357899` · 5 · `probe_seed_p1.out` 785 · `28884ddc` · 18; קובצי ה-`.err` של שלוש הריצות ריקים. ארבעת הראשונים נכנסו לריפו בתוכן זהה, ב-`../../db/block9_seed_40.txt` · `block9_db_40.txt` · `block9_predict_seed.txt` · `block9_predict_db.txt`; ה-blob הוא LF (`core.autocrlf=true`), ולכן חתימתו היא של התוכן בלי ה-CR, באותו סדר: 11,068 · `c3e3e7a7` · 22,964 · `7e7c08ad` · 322 · `728459dd` · 324 · `22e3e080`.

**מה עובר הלאה.** #64 - אבחון בקריאה בלבד על `../../db/block9_db_40.txt` לפני כל שינוי קוד, מוכרע בשיחה הבאה. #20 ו-#31 - בלוק תיקון בשלב ג', לפני ואחרי. #65 - סקירת בעלים של שלושת חריגי ה-kcal שנכנסו על הדגל. #63 - סט פרופילים שאינו תלוי במאגר. #35 ומיגרציה 30 - אחרי 9. `../../README.md:5` ו-`../spec/10-release.md:46` מיושנים על ה-92%. 7מ חסום על #56.
