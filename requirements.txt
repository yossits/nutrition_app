# The spike deliberately has almost no dependencies - everything except the
# API call is plain Python. Keep it that way for as long as possible.
requests>=2.31.0
openpyxl

# 3.1 is where the psycopg3 API these scripts use settled; block 1 was run and
# verified against 3.3.4 on 29.08.2026.
psycopg[binary]>=3.1
